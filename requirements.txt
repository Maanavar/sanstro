# Pinned runtime lock for reproducible builds (used by the Dockerfile and CI).
# Regenerate after changing pyproject deps:
#   pip install . && pip freeze --exclude-editable > requirements.txt
# then re-apply the markered swisseph lines below.
#
# Direct deps are declared with ranges in pyproject.toml; this file pins the full
# resolved tree so two installs a month apart are identical.

alembic==1.18.4
annotated-types==0.7.0
anthropic==0.109.1
anyio==4.13.0
APScheduler==3.11.2
bcrypt==5.0.0
certifi==2026.5.20
cffi==2.0.0
charset-normalizer==3.4.7
click==8.4.0
colorama==0.4.6
cryptography==50.0.0
distro==1.9.0
# ecdsa carries the Minerva timing side-channel (GHSA-wj6h-64fc-37mp / CVE-2024-23342)
# with no upstream fix. It is a transitive dep of python-jose; we only sign/verify with
# HS256 (HMAC), so ecdsa is never on the signing path. The advisory is ignored in the
# pip-audit CI step for that reason. Removing it requires dropping python-jose (see
# REFACTOR_PLAN 1.2: migrate to pyjwt).
ecdsa==0.19.2
fastapi==0.136.1
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httptools==0.7.1
httpx==0.28.1
idna==3.15
jiter==0.15.0
Mako==1.3.12
MarkupSafe==3.0.3
packaging==26.2
passlib==1.7.4
pillow==12.3.0
psycopg2-binary==2.9.12
pyasn1==0.6.4
pycparser==3.0
pydantic==2.13.4
pydantic-settings==2.14.2
pydantic_core==2.46.4
python-dotenv==1.2.2
python-jose==3.5.0
PyYAML==6.0.3
redis==5.3.1
reportlab==4.5.1
rsa==4.9.1
six==1.17.0
sniffio==1.3.1
SQLAlchemy==2.0.49
# Bumped 1.0.0 → 1.3.1 on 2026-09-02, clearing five advisories (PYSEC-2026-161,
# -248, -249, -2280, -2281) that were being suppressed with --ignore-vuln in CI.
# 1.3.1 is the highest fix version among the five, and it is deliberately the
# floor rather than latest: the known route.path breakage is FastAPI 0.141.1 with
# Starlette 1.6.0, and this stops short of both. Take it higher only with a
# reason, and re-read the tripwire below first.
#
# The note that used to live here said `fastapi==0.136.1` constrained the
# starlette range, so clearing these meant bumping FastAPI in the same change.
# That was wrong and made this look twice as risky as it is: FastAPI 0.136.1
# requires `starlette>=0.46.0`, with NO upper bound. FastAPI is untouched here
# and the blast radius is one package. Read the constraint, don't recall it.
#
# The route-shape risk has a tripwire: EXPECTED_CHART_ID_ROUTES in
# tests/test_chart_access_guard.py, pinned to an exact count so a partial drop
# fails rather than quietly shrinking the chart-ownership coverage. On 1.3.1 it
# is still 53 routes across 13 modules.
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.15.0
tzdata==2026.2
tzlocal==5.3.1
uvicorn==0.47.0
watchfiles==1.2.0
websockets==16.0

# Swiss Ephemeris: bindings differ by Python version (matches pyproject markers).
pyswisseph==2.10.3.2; python_version < '3.14'
swisseph-ffi==1.0.0; python_version >= '3.14'
