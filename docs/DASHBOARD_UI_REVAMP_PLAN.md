# Dashboard UI/UX Revamp — Master Plan & Progress Log

> **Status:** 🟡 Phases 1–3 (Today, `cal-panch`, `cal-monthly`) all signed off, plus the trackpad-overscroll fix and the `.cd-footer` gap-fix confirmed working. Phase 4 (`family` screen) built and confirmed. Phase 5 (`family-member`) built. A user browser pass over Phases 3-5 found a real Phase-5-introduced regression (a CSS variable redirect that made selected-tab text invisible app-wide) and a `cal-monthly` highlight bug — both fixed and confirmed working, see Progress Log 2026-07-06. Phase 6 (`explore` hub) built; its Explore-unreachable-under-Nova bug (missing tabnav entry) and Phase 7 (`explore-moolam` — personalised Nakshatram profile) both **confirmed working in the browser** 2026-07-06. Phase 8 (`explore-sevvai` — personalised Dosham profile) built; all 10 of the original Design Inventory screens now built, but a browser pass found the breadcrumb and several content-card links in both Phase 7 and Phase 8 unexpectedly opened the marketing site — fixed by removing all marketing link-outs from both detail screens, see Progress Log 2026-07-06 (breadcrumb fix). Phase 9 (`life-areas`, first of the 4 newly-discovered screens, §3.2) built — turned out to be mostly a re-skin (Classic's `LifeAreaCard` reused verbatim), plus tier grouping and a goal-focus strip as the two genuinely new pieces; the other 4 sub-tabs (Predictions/Yogas & Doshams/Remedies/Full report) are deliberately deferred, Classic-styled. Phase 10 (`plan`, second of the 4 newly-discovered screens) built — the mockup's static export only expands the "Goals" pill (same one-sub-tab-captured pattern as Phase 9), so the Goals section was built to spec (goal chips, a new "Nearest supportive window" hero card and chronologically-sorted "Supportive windows" list — both derived from the existing event-windows engine, no new astrology — plus a fresh-Nova-token Life Event Log card); Classic's Life Events/What-If/Best Dates & Muhurta/Decisions sub-tab bodies were extracted into standalone components (`PlanEventsPanel`/`PlanWhatIfPanel`/`PlanMuhurtaPanel`/`PlanDecisionsPanel`) so Nova could reuse them verbatim instead of duplicating ~250 lines of stateful JSX — a behavior-preserving refactor, zero functionality change. Design direction is now the "Nova" look (see [§2](#2-design-source)), an **additive** third theme, not a brand pivot. `transits` (third of the 4 newly-discovered screens) resolved 2026-07-06 — reading the mockup in full showed it isn't a separate destination at all: it's the Plan tab's own "🪐 Transits & Dasha" toggle state, its top nav has no "Transits" pill, and the user separately confirmed removing it from Nova's nav links — folded into `dashboard-plan-tab-nova.tsx` as a second view (`NovaTransitsView`, §6.10) instead of a standalone tab; the "Transits" pill is now hidden from the shared tabnav specifically under Nova, Classic's own separate Transits tab is untouched. Phase 11 (`journal`, the last of the 4) built 2026-07-06 (§6.11) — unlike Phases 9/10, all three of its sub-tabs (Write/Entries/Reflections) needed a fresh Nova-token rebuild rather than any deferred-Classic-styled reuse, since `dashboard-journal-tab.tsx`'s token palette mixes already-redirected and deliberately-reverted custom properties in a way that would have shipped illegible (not just unstyled) text; also fixed a real pre-existing Tamil-text-corruption bug in `dashboard-shadow-prompts.tsx` found while reading it end to end. All 14 mockup screens are now built. `/dashboard/v2` (the old Today-tab A/B preview) has since been fully retired and deleted (§4.6). Of the post-14-screens punch list, item 1 (Life Areas' 4 deferred sub-tab panels) is re-skinned (§6.8.1) and item 2 (Plan's 4 deferred sub-tab panels — Life Events/What-If/Best Dates & Muhurta/Decisions) is also re-skinned (§6.9.1), both Progress Log 2026-07-06. A follow-up 4-item punch list (Deep Dive completeness / Tools tab's first Nova pass / a white-background-leak audit / header-footer-navbar consistency) was completed 2026-07-06: Deep Dive completeness (§7) is done — all 6 previously-missing pieces folded into Today Nova's Deep Dive; Tools (`tools`/`tools-porutham`) got its first Nova pass (§6.12) — the one tab that had never had a `uiVariant` branch at all, now built with a Nova Porutham result view and a Nova hub; the white-background-leak audit found and fixed 12 files with real "invisible text" or literal-cream-leak bugs (including the long-flagged `dashboard-charts.tsx` chart-grid gap from Phase 5, 5 Deep Dive panels unreused-since-Phase-1, and the `life-mode-picker.tsx` modal reachable from any page); header/footer/navbar inconsistency was confirmed explained by Tools being unbuilt plus the topbar notifications dropdown's own leak, not a new regression. Full details in the 2026-07-06 Progress Log entries. **Working tree not yet committed as of this update** — see Progress Log for the planned commit grouping.
> **Last updated:** 2026-07-08 (Nova navbar restructure + day-ribbon legibility + Morning Guidance dedup + Deep Dive cross-links). Since this line was last written: 2026-07-07 closed the marketing-link-out/week-ahead-perf/floating-button-overlap punch list (all 3 verified live), and 2026-07-08 restructured the Nova navbar into a topnav+subbar, fixed the Week Ahead chart's identical-bar-color bug, collapsed 3 duplicated Morning Guidance editors into 1, and added Deep-Dive-to-Transits/Life-Areas links — see Progress Log for both.
> **Owner:** senthilkumarsivaraman
> **How to use this file:** This is the single source of truth for this initiative. Any coding agent (Claude Code, a subagent, a human) picking up this work at any point should read this entire file before writing code. Update the **Progress Log** at the bottom every time you complete a step, make a decision, or hit a blocker — dated entries, most recent last. Do not start implementation on a screen until its row in the [Gap & Mapping Table](#gap--mapping-table) is filled in and, per the working style below, cosmetic ambiguity is either resolved or explicitly deferred.

---

## 0. Scope (confirmed 2026-07-05)

The Vinaadi codebase has three distinct surfaces. **This initiative touches #2 only.**

| # | Surface | In scope? | Root |
|---|---------|-----------|------|
| 1 | Marketing / public site pages (SEO landing pages, tools, learn, temples, natchathiram, dosham, pariharam, pricing, etc.) | ❌ Out of scope | `web/app/**` (everything using `PublicNav`/`PublicFooter`/`clarity-shell`, e.g. `web/app/family/page.tsx` is a **marketing** page, not the real Family feature) |
| 2 | **Dashboard — signed-in user pages** | ✅ **In scope** | `web/app/dashboard/**`, `web/components/dashboard-*.tsx`, `web/hooks/*` that feed them |
| 3 | Mobile app | ❌ Out of scope | `mobile/**` |

Corollary: `packages/design-tokens/` is **shared** between web and mobile (`dist/web/tokens.css` and `dist/mobile/tokens.ts` are both built from the same `tokens.json`). Any token change made for this revamp must not visually regress mobile — prefer **additive new tokens** over redefining existing ones, unless a token rename/removal is explicitly approved (check `mobile/` usage before removing any token). Same caution applies to `web/components/dashboard-ui.tsx` if anything in it is imported outside the dashboard.

The admin console (`web/app/admin`, `web/components/admin-console.tsx`) is a separate internal tool, not part of the end-user dashboard — **treat as out of scope unless the user says otherwise.**

---

## 1. Mission (verbatim from the originating directive)

Act as a senior product team of one: **world-class Product Designer + UX/UI Architect + Software Architect + Full-Stack Developer + UI Developer**, revamping the signed-in dashboard end-to-end to match a new design direction, while preserving and correctly wiring up every existing functionality — including features that exist only in the new designs and are not yet coded.

**Product context:** Vinaadi, a Tamil Vedic astrology (Jothidam) platform. Core domains: birth chart generation (Swiss Ephemeris + Lahiri Ayanamsa), Panchangam, Dasha-Gochara-Ashtakavarga timing, varga charts, Jaimini methodology, nakshatra/porutham marriage compatibility, family dashboard, Prashna, Muhurtham.

**Design direction:** Tamil palm-leaf and kolam aesthetic — kolam-dot day-arc SVG, normalized signal bars, unified Panchangam ribbon, bookmark-style member tabs, a three-font system (Fraunces / Inter / IBM Plex Mono), new colors. These new designs are **visual/interaction specs only** — some implied functionality does not exist in code yet.

### Non-negotiable principles

1. **All ages, all literacy levels.** Target user ranges from a tech-savvy 20-something to a grandparent checking a Panchangam. Every screen: legible, low-jargon (or jargon explained inline), high-contrast, large tap targets, forgiving of mis-taps. Never sacrifice clarity for aesthetic minimalism.
2. **Homepage (dashboard Today tab) must be genuinely useful, not just pretty.** Surface real, live, relevant info at a glance (today's Panchangam, active Dasha period, upcoming significant transits/events, family member quick-status) — not just navigation tiles.
3. **No functionality regression.** Every calculation, data flow, and interaction that exists today must survive the revamp, even if its old UI is deleted.
4. **No functionality left unimplemented.** Every interaction/feature implied by the new designs (even if only sketched in a mockup) must be either fully implemented or explicitly flagged as a stub with a TODO and a clear reason.
5. **Preserve the calculation engine untouched unless asked.** Swiss Ephemeris / Lahiri Ayanamsa / Panchangam / Dasha logic is domain-sensitive and previously hand-verified (see memory: `feedback_astrology_calc_accuracy`, `project_thirukanitham_audit_2026-07`) — do not refactor astrological calculation logic while doing a visual revamp unless a UI change requires a data-shape change.

### Working style

- Work incrementally, **screen by screen** — never a single giant rewrite.
- Pause and ask when: an astrological calculation's correctness is ambiguous, a new design implies data that doesn't exist yet, or a UI pattern could be read two different ways.
- Otherwise, use good default judgment and keep moving — don't block on cosmetic decisions.

---

## 2. Design source

**Resolved 2026-07-05 — access was unblocked manually, not via `DesignSync`.** `DesignSync.get_project` still requires interactive `/design-login`, unavailable in this environment. Instead, the user downloaded the Claude Design project's HTML exports directly and placed them at:

```
D:\sanstro\Astrology App Dashboard Redesign\
```

**This folder is gitignored (`.gitignore`) and must never be committed or referenced by path from shipped code.** Treat it as read-only reference; extract values into real tokens/components, don't import from it.

Contents, in order of authority:
- **`Vinaadi Prototype.dc.html`** — **the primary, latest, most-complete mockup.** 10 static screens as sibling `data-screen` blocks: `today`, `cal-panch` (Panchangam detail), `cal-monthly` (Calendar), `family`, `family-member` (Jathagam detail), `explore` (hub), `explore-moolam` (Nakshatram profile), `explore-sevvai` (Dosham detail), `tools` (hub), `tools-porutham` (result). One shared top-bar + context-strip chrome, **no left sidebar** — build against this file.
- **`Design Tokens.dc.html`** — full token sheet for the same look: colors, type scale, spacing, radius, border-alpha steps, component patterns (chips, buttons, data-label pattern).
- **`Dashboard Redesign.dc.html`** — an earlier design-review iteration. 8 of its screens are verbatim-identical to Vinaadi Prototype (confirms those are final). Has 2 extra, superseded Today variants with features later dropped (a "9/10 signals agree" confidence tile, a "next 3 days" trend strip, an inlined best-dates card). **Historical reference only — do not resurrect these unless asked.**
- **`design_handoff_dashboard_revamp/README.md` + `dashboard-2a.html`** — an earlier, more detailed prose spec for just the Today screen; superseded by Vinaadi Prototype's `today` screen but useful prose cross-reference for interaction notes.

The look itself is branded **"Nova"** internally (implementation naming, see `useUiVariant.ts`) — a single fixed dark ("dark astronomical") theme: deep plum backgrounds (`#221a2c`/`#2c2338`/`#2e2440`), cream text (`#f3ecdd`), gold accent (`#d4af5f`/`#e7c87e`), Cormorant Garamond (display) / Source Serif 4 (prose) / system-ui (nav/labels/data) / Noto Sans Tamil (script, already loaded app-wide).

### Blockers — both resolved 2026-07-05

1. ~~Design content not readable via `DesignSync`~~ — resolved via manual download (see above), not via "Send to Claude Code Web" as originally planned.
2. ~~Font-system / brand-language conflict~~ — resolved: **Nova is an additive third look, not a v2 revision or brand pivot.** The existing `packages/design-tokens/DESIGN_CONSTITUTION.md` ("Warm Cosmic Minimalism": Playfair Display/Inter/JetBrains Mono) is **untouched** — that's "Classic," selectable via a new Look toggle in Settings (Classic/Nova), separate from the existing Light/Dark/System toggle. Nova's tokens live in a new web-only file (`web/app/dashboard/dashboard-nova.css`), not in the shared `packages/design-tokens` package — see Progress Log 2026-07-05 for why (no light/dark pairing to derive, mobile is out of scope, `build.js` has no generic per-key emission).

**New decision, also 2026-07-05:** the existing unshipped `/dashboard/v2` Today-tab preview (see [§4.6](#note-existing-v2-preview)) is **retired**, superseded by Nova's Today screen — not kept as a third parallel look.

---

## 3. Design Inventory

*Screen-by-screen inventory of `Vinaadi Prototype.dc.html`, confirmed 2026-07-05 (sections, data fields, colors, and which existing dashboard component/hook each maps to). Full detail lives in the Phase 1 section of the implementation plan; summary here for quick reference.*

| # | `data-screen` | Maps to existing tab/route |
|---|---|---|
| 1 | `today` | `personal` tab — `dashboard-personal-tab.tsx` + `dashboard-personal-hero.tsx` + `dashboard-personal-overview.tsx` |
| 2 | `cal-panch` | `calendar` tab, daily view — `dashboard-calendar-tab.tsx` |
| 3 | `cal-monthly` | `calendar` tab, monthly view — `dashboard-calendar-tab.tsx` |
| 4 | `family` | `family` tab — `dashboard-family-tab.tsx` (incl. existing Synastry Matrix/Panel for "family bonds") |
| 5 | `family-member` | Family member detail / Jathagam — `dashboard-jadhagam-report-panel.tsx`, `dashboard-vargas-panel.tsx` |
| 6 | `explore` | `explore` tab, hub — `dashboard-explore-tab.tsx` |
| 7 | `explore-moolam` | Explore → Nakshatram profile detail (new shared detail-page shell, generalizing `dashboard-personal-overview.tsx`'s Nakshatra card + `dashboard-yoga-dosham-panel.tsx`) |
| 8 | `explore-sevvai` | Explore → Dosham profile detail (same shared shell as above) |
| 9 | `tools` | `tools` tab, hub — **corrected 2026-07-06 (§6.12):** `tool-card.tsx`/`tools-grid.tsx` are dead code (no importers); the real Tools tab is inline "cd-tools-v3" JSX in `dashboard-workspace.tsx` |
| 10 | `tools-porutham` | Porutham result — **corrected 2026-07-06 (§6.12):** `porutham-panel.tsx` only; `friendship-result-card.tsx` belongs to the out-of-scope marketing page `web/app/tools/friendship-compatibility/`, not the dashboard |

### 3.1 Coverage policy — superseded 2026-07-05

Earlier assumption (2026-07-05, same day) was that the mockup only covered a partial subset. Once `Vinaadi Prototype.dc.html` was actually read in full, it turned out to cover all 10 of the dashboard's main screens/tabs (table above) — coverage is effectively complete, not partial. The extrapolation policy below is kept for any screen/sub-view genuinely outside those 10 (e.g. a settings sub-page), but in practice almost everything has a direct mockup source now:

1. **Explicitly-designed screens** (the 10 above) get built exactly to spec, adapted to real data per the Phase 1 mapping table.
2. **Anything left over with no mockup** still gets revamped by extrapolating Nova's established tokens/primitives (`dashboard-nova.css`, `dashboard-ui-nova.tsx`) rather than being left in Classic or invented independently.
3. Where extrapolation requires a judgment call, follow the working style: keep moving for cosmetic-only calls, ask when it's ambiguous enough that two reasonable people would build it differently or implies data/functionality that doesn't exist yet.

### 3.2 Four more mockup screens discovered 2026-07-06 — §3.1's "complete coverage" is stale

While reading the mockup file fresh for Phase 6, `grep data-screen=` turned up **14** blocks, not the 10 documented above and in §2's file listing — 4 more exist: `life-areas`, `plan`, `transits`, `journal` (file has grown to 2777 lines, up from whatever it was when §3 was written 2026-07-05). These map directly onto tabs already in the §4.3 component inventory (`dashboard-life-areas-tab.tsx`, `dashboard-plan-tab.tsx`, `dashboard-transits-tab.tsx`, `dashboard-journal-tab.tsx` — the "Explore umbrella" tabs), so no new mapping guesswork is needed, but §3.1's claim that "coverage is effectively complete" is no longer accurate — there are 4 more explicitly-designed screens waiting once the Explore-umbrella tabs' own Nova passes come up. Not read in full yet; do that before starting whichever of those tabs is built next, per the working style ("read the mockup screen in full first").

---

## 4. Current Codebase Map — Dashboard (Scope #2)

### 4.1 Routes

| Route | File | Purpose |
|---|---|---|
| `/dashboard` | `web/app/dashboard/page.tsx` → `<DashboardWorkspace />` | Main tabbed workspace (see §4.2) |
| `/dashboard/chart-generate` | `web/app/dashboard/chart-generate/` | Chart generation flow |
| `/dashboard/daily-score` | `web/app/dashboard/daily-score/` | Daily score detail |
| `/dashboard/goals` | `web/app/dashboard/goals/` | Life-goals feature |
| `/dashboard/porutham` | `web/app/dashboard/porutham/` | Marriage compatibility tool (dashboard-embedded variant) |
| `/dashboard/reports` | `web/app/dashboard/reports/` | Paid report purchase/delivery |
| `/dashboard/wrapped` | `web/app/dashboard/wrapped/` | Annual "Wrapped"-style recap |

`web/app/dashboard/layout.tsx` just sets metadata (title, `robots: noindex`) — no shared chrome there; all chrome lives in `DashboardWorkspace`.

### 4.2 Primary navigation (from `web/components/dashboard-left-rail.tsx`)

The left rail defines these top-level tabs (type `Tab` in that file):

```
personal | calendar | family | tools | explore | settings
(+ hidden: onboarding, qa [dev-only, NODE_ENV !== "production"])
```

Rail order: **Today** (personal) → **Panchangam** (calendar) → **Family** → **Tools** → **Explore** (an umbrella that lights up for `transits | plan | life-areas | journal | explore`) → *(spacer)* → **Reports** link (`/dashboard/reports`) → **Settings** (pinned bottom).

### 4.3 Component inventory (`web/components/dashboard-*.tsx` + supporting)

First-pass grouping by likely tab, from file naming and the orchestrator's dynamic imports in `dashboard-workspace.tsx`. **Not yet individually verified — verify per-screen during the screen-by-screen build, per working style.**

| Tab / Area | Components (file names, `.tsx` implied) |
|---|---|
| Shell / shared | `dashboard-workspace.tsx` (orchestrator + state), `dashboard-hero.tsx`, `dashboard-left-rail.tsx`, `dashboard-ui.tsx` (shared primitives — likely the closest thing to an existing component library for this surface), `life-mode-picker.tsx`, `member-chip.tsx`, `mode-badge.tsx`, `day-strip.tsx`, `icons.tsx`, `dashboard-ask-vinaadi.tsx` / `dashboard-ask-vinaadi-widget.tsx` (AI chat, likely global) |
| Today / Personal (`personal`) | `dashboard-today-tab.tsx`, `dashboard-today-tab-v2.tsx` (preview — **to be deleted**, see §4.6), `dashboard-personal-tab.tsx`, `dashboard-personal-hero.tsx`, `dashboard-personal-overview.tsx`, `dashboard-daily-snapshot.tsx`, `dashboard-activity-timing-card.tsx`, `dashboard-retrospective-panel.tsx` — **corrected 2026-07-05:** `dashboard-decision-panel.tsx`/`dashboard-event-windows.tsx` actually belong to the Plan tab, `dashboard-prediction-panel.tsx`/`dashboard-remedies-panel.tsx`/`dashboard-shadow-prompts.tsx` actually belong to Life-areas/Journal tabs — moved below, were mis-grouped here |
| Panchangam (`calendar`) | `dashboard-calendar-tab.tsx`, `panchangam-date-picker.tsx`, `panchangam-share-card.tsx` |
| Family (`family`) | `dashboard-family-tab.tsx`, `dashboard-edit-member-modal.tsx`, `dashboard-edit-profile-modal.tsx`, `birth-profiles-manager.tsx`, `dashboard-guest-chart-modal.tsx` |
| Tools (`tools`) | **Corrected 2026-07-06 (§6.12), this row was wrong on 3 counts:** `dashboard-muhurta-picker.tsx`/`dashboard-muhurtham-naal.tsx` actually belong to Plan (confirmed by Phase 10); `friendship-result-card.tsx` belongs to the out-of-scope marketing site, not the dashboard; `dashboard-prasna-widget.tsx` is Today tab's Prasna trigger (§7), not a Tools-tab feature. Tools' real components: `chart-generate-inline-panel.tsx`, `porutham-panel.tsx`, `dashboard-annual-wrapped.tsx`, `dashboard-retrospective-panel.tsx` (`dashboard-rectification-wizard.tsx` exists but is wired into Setup/onboarding, not Tools — Tools' own rectification tool was explicitly removed, see §6.12) |
| Explore umbrella: Transits | `dashboard-transits-tab.tsx` |
| Explore umbrella: Plan | `dashboard-plan-tab.tsx` (uses `usePlanData` hook), `dashboard-decision-panel.tsx`, `dashboard-event-windows.tsx` |
| Explore umbrella: Life Areas | `dashboard-life-areas-tab.tsx`, `life-area-card.tsx`, `dashboard-life-event-log.tsx`, `dashboard-life-events.tsx`, `dashboard-prediction-panel.tsx`, `dashboard-remedies-panel.tsx` |
| Explore umbrella: Journal | `dashboard-journal-tab.tsx` (uses `useJournalData` hook), `dashboard-shadow-prompts.tsx` |
| Explore umbrella: Explore | `dashboard-explore-tab.tsx` |
| Chart / deep-dive detail (surfaced from Today + reports) | `dashboard-charts.tsx`, `dashboard-chart-explanation.tsx`, `dashboard-vargas-panel.tsx`, `dashboard-dasha.tsx`, `dashboard-ashtottari-dasha-panel.tsx`, `dashboard-kalachakra-dasha-panel.tsx`, `dashboard-yogini-dasha-panel.tsx`, `dashboard-shadbala-panel.tsx`, `dashboard-yoga-dosham-panel.tsx`, `dashboard-synastry-panel.tsx`, `synastry-matrix.tsx`, `dashboard-varshaphala-panel.tsx`, `dashboard-jadhagam-report-panel.tsx`, `astro-symbols.tsx`, `thirukanitham-badge.tsx` |
| Annual Wrapped (`/dashboard/wrapped`) | `dashboard-annual-wrapped.tsx`, `wrapped-share-card.tsx` |
| Setup / onboarding (`onboarding`) | `dashboard-setup-tab.tsx` |
| Settings (`settings`) | `dashboard-settings-session-tab.tsx` |
| QA (dev-only) | `dashboard-qa-tab.tsx` |
| Sharing (cross-cutting) | `dashboard-share-card.tsx`, `panchangam-share-card.tsx`, `wrapped-share-card.tsx` |
| Feedback | `dashboard-feedback-modal.tsx` (+ `.css`) |
| Misc shared UI (used across tabs, not dashboard-prefixed) | `collapsible-section.tsx`, `drawer-panel.tsx`, `place-combobox.tsx`, `guide-cards.tsx`, `guide-detail-page.tsx`, `guide-traditional-notes.tsx`, `tool-card.tsx`, `tools-grid.tsx` |

### 4.4 Data hooks (`web/hooks/*`)

| Hook | Feeds |
|---|---|
| `useSession.ts` | Auth/session state |
| `usePersonalData.ts` | Today/Personal tab |
| `useFamilyData.ts` | Family tab (exports `MemberChart` type) |
| `usePlanData.ts` | Plan tab |
| `useJournalData.ts` | Journal tab |
| `useMonthlyPanchangam.ts` | Panchangam/calendar tab |
| `useBirthProfileForm.ts` | Chart generation / profile edit flows |
| `useGuestStore.ts` | Guest (pre-signup) chart state |
| `useStreak.ts` | Streak/gamification indicator |
| `useTheme.ts` | Light/dark theme |

### 4.5 Styling

- `web/app/dashboard/dashboard.css` — main dashboard stylesheet
- Tokens: `packages/design-tokens/tokens.json` → built to `packages/design-tokens/dist/web/tokens.css` (imported in `web/app/layout.tsx`) and `dist/mobile/tokens.ts`. Regenerate via `node packages/design-tokens/build.js`.
- `web/app/globals.css` — web-specific extensions layered on top of the generated tokens (score colors, planet colors, chart-cell states, panel/parchment tints). Comment at top of file: *"No hardcoded hex... only web-specific, non-token values belong here."*

### <a name="note-existing-v2-preview"></a>4.6 Note: an existing redesign is already mid-flight — retired 2026-07-05, deleted 2026-07-06

Per memory `project_dashboard_v2_redesign` (2026-07-02): a "daily briefing" redesign of the Today tab was already built as an A/B preview at `/dashboard/v2`, with 3 altitude bands (Briefing → Your world → Deep dive) and its own `.t2-*` CSS. **Decided 2026-07-05: retired, superseded by Nova's Today screen** (Nova covers Today plus 9 more screens with one coherent design system — no reason to keep 3 parallel looks). **Deleted 2026-07-06** (all 14 mockup screens built, Today Nova long since signed off — no reason to keep the fallback comparison any longer): removed `web/app/dashboard/v2/page.tsx`, `web/components/dashboard-today-tab-v2.tsx`, `web/app/dashboard/v2/today-v2.css` (and the now-empty `web/app/dashboard/v2/` directory), and the `todayVariant`/`TodayVariant` prop path from `dashboard-workspace.tsx` (the dynamic import, the type, the prop itself — `DashboardWorkspace` now takes no props — the `DashboardTodayTabV2` render branch, and the `todayVariant !== "v2"` gate on the household strip, which collapsed to just `uiVariant !== "nova"`). Grepped first to confirm no other consumer (`web/app/dashboard/page.tsx` already called `<DashboardWorkspace />` with no props). `tsc`/`eslint` clean after.

---

## 5. Process

1. **Audit** (this document, in progress) — Functionality Inventory (§4 above) + Design Inventory (§3, blocked) + Gap & Mapping Table (§6). Present back for confirmation before implementation, unless told to proceed straight to build.
2. **Design System Extraction** — resolve the font/brand conflict (§2 blocker 2), then extend `packages/design-tokens` (additively) with: confirmed typography scale, color tokens (respecting kolam/palm-leaf identity), spacing/radius/shadow/motion tokens, and core primitives (cards, bookmark-style member tabs, ribbons, gauges/signal bars, day-arc SVG component) as reusable components in `web/components/dashboard-ui.tsx` or a new shared primitives module — not one-off inline styles.
3. **Component & Screen Rebuild**, in order:
   1. Design tokens + shared primitives
   2. Screens, sequenced by whatever the Design Inventory turns out to contain (Today tab first is the likely default, given the existing `/dashboard/v2` head start — confirm against the mockup).
   
   For each screen: (a) what's reused from old code as-is (logic/data only, re-skinned), (b) what's net-new UI, (c) what's net-new functionality requiring new state/data/API work, (d) accessibility check (contrast, tap target size, screen-reader labels, keyboard nav).
4. **Implementation standards:** typed components, accessible (semantic HTML/ARIA), responsive (mobile-first web layout, not to be confused with the separate native mobile app which is out of scope). Reuse existing data-fetching/business logic; loading + empty states on every data-driven component (astro calcs can be slow — never a blank screen). If a new design implies a feature with no backend yet (e.g. a "signal bar" needing an uncomputed score), stub it clearly, propose the simplest correct calculation or data source, and **ask before inventing astrological logic**. Comment non-obvious astrological UI decisions (e.g. why a dosha shows a specific severity color).
5. **Verification** — after each screen: run the app, visually diff against the design reference, confirm every relevant Functionality Inventory item still works, confirm every relevant Design Inventory item is implemented or explicitly flagged as a pending stub with a reason. End with a changelog: ported as-is / rebuilt / net-new / stub.

Cross-cutting reminder from `CLAUDE.md`: routes, query params, and response shapes are a shared contract across `app/api/`, `packages/shared/src/api/`, `mobile/src/api/`, and `web/`. `web/` mostly bypasses the shared client today (direct `apiFetchJson(...)` calls) — that's grandfathered; don't add new bypass call sites going forward. If this revamp touches any endpoint the mobile app also calls, update all four locations in the same change.

---

## 6. Gap & Mapping Table

*Populated 2026-07-05 for Phase 1 (Today tab). Remaining screens (Phase 2+) will be populated as each is built — see the implementation plan's Phase 2+ section for the named build sequence.*

| Mockup section (Today) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Greeting hero + score dial | `dashboard-personal-hero.tsx` data (`personalDailyGuidance`) + `NovaScoreDial` | `dashboard-today-tab-nova.tsx` section 1 | **Built** |
| Panchangam band | `personalDailyGuidance`/`panchangam` data, same fields as classic's `CollapsibleSection` | `dashboard-today-tab-nova.tsx` section 2 | **Built** |
| One-focus + remedy | `personalDailyGuidance.actionSuggestion` + `.remedy` (same source as `dashboard-daily-snapshot.tsx`/`dashboard-personal-overview.tsx`) | `dashboard-today-tab-nova.tsx` section 3 — merged into one row per the note below | **Built** — "Add to my day" and remedy "✓ Done" omitted, no backing feature exists for either (see Progress Log) |
| Day timeline ribbon | `panchangam.kalam.rahuKalam` + `.nallaNeram[]` | `dashboard-today-ribbon-nova.tsx` — new segmented-bar visual (real data, dynamic range/segment count, not hardcoded) | **Built** |
| Ask + decide | Resolved 2026-07-05: user chose to add a small backend field over downgrading the UI or stubbing it (see Progress Log) | `dashboard-today-decide-nova.tsx` — Ask Vinaadi teaser (opens the existing FAB, doesn't duplicate its chat) + a 4-activity Decide grid backed by a new additive `dateResult` field on `/api/v1/activity-timing` | **Built** |
| Anticipation row | `peyarchiUpcoming[]`, `personalSani`, `weekAhead` | `dashboard-today-glance-nova.tsx` (`DashboardTodayAnticipationRowNova`) | **Built** — mockup's "Chandrashtama · in 6 days" forward-countdown chip omitted, no such calculation exists (only "is it active today," not "days until next occurrence") |
| Glance row | `familyAggregate`, `personalChartSummary`/`dasha`/`dashaAntar`, `lifeAreas` | `dashboard-today-glance-nova.tsx` (`DashboardTodayGlanceRowNova`) | **Built** |
| Deep-dive teaser | Existing detail panels reused verbatim, wrapped in `collapsible-section.tsx` | `dashboard-today-tab-nova.tsx` section 8 | **Built**, complete — see §8 (2026-07-06, Deep Dive completeness pass) for the 6 pieces folded in after the initial Phase 1 pass |
| Porutham "Dasa sandhi" / "Papasamyam" cross-checks (Phase 2, screen 10) | — | **Genuine stub** — no existing calculation found anywhere in the codebase | Flag with TODO, do not invent the astrological logic |

Already-existing features confirmed during research — **treat as re-skins, not stubs**: the "4 free questions today" meter (`dashboard-ask-vinaadi-widget.tsx`, backed by `/api/v1/ask-vinaadi/daily-status`), family "bonds" pairwise compatibility (`dashboard-family-tab.tsx`'s synastry sub-tab, `synastry-matrix.tsx`), the "why this prediction" Dasa/Panchangam/Transit breakdown (`dashboard-daily-snapshot.tsx`, `guidance.scoreBreakdown`), streak gamification (`useStreak.ts`), and Life Areas scoring (`dashboard-life-areas-tab.tsx`).

### 6.1 Phase 2 gap & mapping table (`cal-panch` — Calendar tab, daily Panchangam view)

*Populated 2026-07-05. `CalendarTab` (`dashboard-calendar-tab.tsx`) turned out to already be written almost entirely with `var(--color-*)`-driven inline styles (not Classic's hardcoded hex) for its daily-view content (`DayTimeline`, Five Limbs rows via `.cd-detail-spec-row`, `LunarTithiBadge`) — those Nova already re-themes for free via the same cascade mechanism Phase 1 relied on, so they're reused verbatim (exported, not reimplemented). Sub-widgets that instead read Classic-only hardcoded custom properties (`--panel-warm-tint`, `--cl-brand-edge`, `--chart-d9-active-bg`, `--cal-*`, etc. — confirmed via `globals.css` to be literal hex with no Nova override) were rebuilt fresh with Nova tokens instead of patched, to avoid growing the gap-fix list in `dashboard-nova.css` indefinitely.*

| Mockup section (cal-panch) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Page header (date, Tamil date, weekday·paksha·nakshatra meta, Panchangam/Monthly toggle) | `formatHeaderDate`/`getTamilMonthDate`/`activeLimb` (exported from Classic) | `dashboard-calendar-tab-nova.tsx` header block | **Built** |
| Day at a glance headline + sunrise/sunset + location override | Same `panchangam` fields; `PlaceCombobox` reused as-is (Classic-styled dropdown — minor, accepted cosmetic mismatch, see below) | New Nova card chrome | **Built** |
| Day arc (SVG) | `DayTimeline` (Classic, exported) — already 100% `var(--color-*)`-driven | Reused verbatim, no changes | **Built** |
| Auspicious (Nalla Neram / Gowri Nalla Neram) | `panchangam.kalam.nallaNeram` / `.gowriNallaNeram`, `gowriPeriodLabel` | `NovaAuspiciousCard` — Classic's `AuspiciousSlotGroup` reads non-Nova-safe tokens, so this is a fresh Nova-token implementation of the same data | **Built** |
| Avoid (Rahu Kalam / Yamagandam / Kuligai) | `panchangam.kalam.*` | `NovaAvoidRow` (fresh Nova-token version, same data) | **Built** |
| Today's Nakshatra card | `nakActive`/`panchangam.nakshatra` | Inline in orchestrator | **Built** |
| Chandrashtamam card | `chandrashtamaAffectedNatalRasi`, `formatChandrashtamaWindowSummary`, `moonRasiFromNakshatra`, `rasiName` (all exported, reused) | Inline in orchestrator, Nova-token styling | **Built** |
| Today's Events (festivals, world observance) | `festivalTags`/`festivalIcon` (exported) | `NovaFestivalRow` + `novaFestivalTagTone`/`novaFestivalTagLabel` (fresh Nova tag badge coloring — Classic's `festivalTagTone` uses non-Nova-safe `--cl-festival-*` tokens) | **Built** |
| Gowri Nalla Neram Details (14-slot grid) | `panchangam.kalam.gowriPanchangam`, `gowriCategoryLabel`/`gowriPurposeLabel`, `timeWindowsOverlap` (exported) | `NovaGowriDetailGrid` (fresh Nova-token version, same data/logic) | **Built** |
| Today's Significance | Same fallback logic as Classic (`festivals[0].name` → `subhaMuhurtham.reason` → generic line) | Inline in orchestrator | **Built** |
| Panchangam · Five Limbs (11 rows incl. Soolam/Lagnam/Nethiram/Jeevan/Amirdhadhi Yogam) | Same row-array construction as Classic; `.cd-detail-spec-row` CSS class (already `var()`-driven, safe to reuse) | Row array duplicated verbatim in the Nova file (same i18n calls, same fields) | **Built** — carries forward the pre-existing "5L" tag on every row unchanged (present identically in Classic and in the mockup itself; its purpose is unclear — looks like a leftover placeholder never wired to real data — flagged here, not invented or removed) |
| Hora Table (running-hora highlight + full list) | `panchangam.hora`, `DASHA_COLORS` | `NovaHoraRow` (fresh Nova-token version of Classic's `HoraRow`, same auto-scroll-into-view behavior) | **Built** |
| Monthly toggle (screen 3, `cal-monthly`) | `MonthlyCalendarView` + `DayDetailDrawer` (exported from Classic, unchanged) | **Deliberately deferred** — reused Classic-styled (light/cream) verbatim inside the Nova tab so the toggle stays fully functional; Phase 3 re-skins this screen next | Functional, not yet Nova-styled |

**Known minor cosmetic mismatch, accepted:** `PlaceCombobox` (the location-override search dropdown) renders with Classic's light/cream input styling even under Nova, since it reads Classic-only hardcoded tokens (`--chart-cell-default`, `--panel-earth`, `--panel-hover`). Low-traffic, secondary feature (changing which city's panchangam you're viewing) — noted, not fixed in this pass.

### 6.2 Phase 3 gap & mapping table (`cal-monthly` — Calendar tab, monthly grid) — pulled forward

*Populated 2026-07-05. Originally planned as a separate later phase (Monthly reused Classic's `MonthlyCalendarView` verbatim in the meantime); pulled forward in the same session after the user's Phase 2 sign-off screenshot showed the Monthly toggle still rendering Classic's cream/light colors. Unlike `cal-panch`, essentially none of `MonthlyCalendarView`'s markup was safe to reuse verbatim — its `monthlyTheme` object and every highlight/legend color reads Classic-only literal-hex custom properties (`--cal-*`, `--panel-cream-light`, confirmed via `globals.css` to have no `[data-ui="nova"]` override anywhere) rather than the `--color-*` tokens Nova redefines.*

| Mockup section (cal-monthly) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Page header + Panchangam/Monthly toggle | Shared with `cal-panch` header | Reused from `dashboard-calendar-tab-nova.tsx`'s existing header (no duplication — same component renders both sub-views) | **Built** |
| Month nav (‹ July 2026 · Aani & Aadi ›) | `tamilMonthOnly`, `MONTH_LABELS_EN/TA` (exported from Classic) | `MonthlyCalendarViewNova` header block | **Built** |
| Calendar grid (day cells: Tamil date, tithi, festival icons, Muhurtham/Pournami/Amavasai/Chathurthi/Sashti/Pradosham highlight colors, Karinaal warning, Today badge) | Same `PanchangamMonthDayEntry[]` data and the exact same `highlightType` priority logic as Classic (muhurtham > pournami > amavasai > chathurthi > sashti > pradosham) | Fresh Nova-token color mapping (`NOVA_CAL_HILITE`) — reused verbatim: `festivalIcon`/`festivalImagePath`/`festivalTags`/`MoonPhaseMark`/`lunarSpecialTithiMeta` (all pure logic/data, no Classic-only tokens) | **Built** |
| Legend row (9 categories) | Same 9 categories as Classic | `NOVA_LEGEND` — reuses Nova semantic tokens where an exact match exists (Muhurtham→`--color-high`, Chathurthi→`--color-low`, Karinaal→`--color-alert-critical`, Pradosham→`--color-accent`, Festival→`--color-accent-strong`, Ekadashi→`--color-faint`); Pournami/Amavasai/Sashti needed 3 small new literal one-off colors since Nova has no existing semantic slot for a 3rd gold shade or a blue — matched to the mockup's own legend swatch values, not invented | **Built** |
| Sidebar (Events/Vratha/Muhurtham tabs, item list, vratha-sequence chips, "best for your chart" muhurtham chips) | Same `monthFestivals`/`vrathaGroups`/`sidebarItems` derivation as Classic (recomputed from the same `PanchangamMonthlyData` prop — Classic doesn't expose these as a reusable hook, so the `useMemo` blocks are duplicated verbatim, not reinvented) | Fresh Nova-token card/tab/chip styling | **Built** |
| Day-click preview drawer | Classic's `DayDetailDrawer` reused the non-Nova-safe `AuspiciousSlotGroup`/inline avoid-rows | `DayDetailDrawerNova` (in `dashboard-calendar-tab-nova.tsx`) — reuses `DrawerPanel`'s existing default **dark** theme (only Classic's drawer opts into `theme="light"`) plus the `NovaAuspiciousCard`/`NovaAvoidRow`/`NovaFestivalRow` pieces already built for `cal-panch`, so no new sub-components were needed | **Built** |

### 6.3 Phase 4 gap & mapping table (`family` — Family tab)

*Populated 2026-07-06. Read `dashboard-family-tab.tsx` (946 lines), `useFamilyData.ts`, `synastry-matrix.tsx`, `dashboard-synastry-panel.tsx`, `dashboard-edit-member-modal.tsx` in full first. Unlike `cal-panch`, several mockup sections turned out to already be computed server-side but never surfaced in Classic's UI at all (not just non-Nova-safe styling) — a deeper class of "re-skin, not stub" than earlier phases found.*

| Mockup section (family) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Page header (kicker/headline/date subcopy, vault switcher, refresh, Compatibility + Add member) | `familyAggregate.familyLabel`, exported `formatHeaderDate`/`getTamilMonthDate` (pure date math, no Panchangam fetch needed — reused from Phase 2/3), existing vault-switcher/refresh logic | `dashboard-family-tab-nova.tsx` header block | **Built** |
| Relation filter chips (All/Elders/Adults/Children/Needs care) | `ChartSummaryData.currentAge` (already computed server-side per member chart — no new age math invented) | Bucket thresholds chosen (child <18, elder ≥60, matching common Indian senior-citizen convention) since no such bucketing existed anywhere in the codebase — **user decision**, see Progress Log | **Built**, real filter (clicking a chip filters the Members grid) |
| Harmony hero — score + headline + narrative paragraph | `familyAggregate.familyScore` (already used) + **`familyAggregate.summary.{en,ta}`** — a full bilingual narrative computed by `_family_summary()` in `family_vault_service.py`, already wired to the API response, but **never rendered anywhere in Classic's UI** | Small per-label headline phrase map (`FAMILY_HERO_TEXT`, presentation-only) + `summary[lang]` reused verbatim as the paragraph | **Built** — re-skin of an already-computed, previously-unsurfaced feature |
| Best shared window banner | `familyAggregate.bestFamilyWindows[0]` (already used as a small chip in Classic) | Restyled as a banner | **Built** — mockup's "Plan together" button omitted, no backing action exists anywhere (same class of gap as Phase 1's "Add to my day") |
| Today's rhythm (sparkline + needs-care/brightest lists) | `familyAggregate.bestFamilyWindows`/`.avoidForFamilyDecisions` (time-of-day → x-axis position), `FamilyMemberTodayScore.chandrashtama`/`.score` (per-member "today" feed, fetched the same way Classic's own internal `familyToday` effect already does) | New sparkline SVG (decorative day-arc backdrop + two real data-driven segment bars) + needs-care derivation (score < 45, matching `family_vault_service.py`'s own `low_score_count` threshold, OR chandrashtama/major-sani tag — no new astrology) + brightest (top-2 by score) | **Built** |
| Members grid (avatar, relation, score dial, lagna+dasha line, one-line insight, best-window chip, needs-care tag, Open→) | `familyAggregate.members`, `memberCharts[].summary`/`.dasha`, and **`FamilyMemberTodayScore.highlightEn/highlightTa`** — a per-member one-line insight already computed server-side (e.g. "Steady — move step by step..."), also never surfaced in Classic | New `NovaFamilyMemberCard` grid; exported `ScoreRing`/`formatRelLabel` from Classic (additive `export`, zero logic change) | **Built** — "Open →" does an **inline expand** (renders the existing `MemberDetailExpanded` below the grid, same interaction Classic already has), not a link to the not-yet-built `family-member` screen — **user decision**, see Progress Log |
| Family bonds (pairwise mini-cards + "strongest bond" banner + "All pairs →") | `synastry_service.py`'s `compute_synastry_score()` — a **generic, non-marriage** Venus/Mars/Moon/Sun synastry algorithm, already used for owner-vs-member pairs via `get_synastry_for_member`, but with no existing route for two *arbitrary* member charts (needed for e.g. Lakshmi↔Bharathan, neither of whom is the vault owner) | **New, additive backend endpoint** `POST /api/v1/relationships/compare-synastry` (`DirectSynastryRequest`/`DirectSynastryData`/`DirectSynastryResponse` in `app/schemas/relationships.py`, `compare_synastry_direct()` in `synastry_service.py`) mirroring the existing Porutham `compare_charts_direct`/`POST /relationships/compare` pattern exactly — reuses `compute_synastry_score()` verbatim, no new astrology. Typed wrapper `compareSynastry()` added to `packages/shared/src/api/relationships.ts` per CLAUDE.md's API-contract policy. Frontend computes all C(n,2) pairs among owner+members, lazy-loaded via a "Load bonds →" button (same UX precedent as the existing `SynastryMatrix`'s "Load scores" button, not an eager fetch storm on mount) | **Built** — **user decision**: full all-pairs matrix, see Progress Log. The mockup's per-pair "daily nuance" sentence (e.g. "evening favours reconnecting") isn't backed by any transit-aware pairwise calculation, so replaced with a generic score-band tone line (`bondToneLine`), not invented daily astrology |
| Shared muhurtham (Griha pooja / exam / avoid-travel rows) | — | **Genuine stub** — no cross-member "shared events" feed exists anywhere (only per-person daily guidance/festivals); flagged with an explicit "coming soon" note, not invented | Deliberate stub |
| "Show all... life-areas, dasa & remedies side by side" expandable row | — | No multi-member comparison table exists; same class of gap as the Porutham cross-check stub from Phase 2 | **Omitted** (not built this pass — flag here rather than a fake expand-to-nothing row) |
| Footer note ("Family guidance... indicative only" + "Notify me when a member needs care" toggle) | — | No family-specific care-notification preference exists (only personal journal reminders, a different feature) | Toggle **omitted**, matching the Phase 1 precedent of dropping non-functional CTAs rather than shipping a decorative no-op control |
| Compatibility sub-view (existing Synastry Matrix/Panel) | `synastry-matrix.tsx`, `dashboard-synastry-panel.tsx` — confirmed via grep: **100% `var(--color-*)`-driven, zero Classic-only hex** | Reused verbatim, toggled by the header's "Compatibility" button | **Built**, free re-skin |
| Site footer | Already fixed globally (see Progress Log, footer gap-fix entry) | N/A | Done |

### 6.4 Phase 5 gap & mapping table (`family-member` — Family member detail)

*Populated 2026-07-06. Read the mockup in full (lines 966-1173 of `Vinaadi Prototype.dc.html`). The original §3 guess ("maps to `dashboard-jadhagam-report-panel.tsx`, `dashboard-vargas-panel.tsx`") turned out wrong on inspection — `JadhagamReportPanel` is a different, much heavier lifetime-report feature (age-wise focus timeline, practical guidance, remedies) that this mockup screen doesn't show at all. The real content is much closer to what `MemberDetailExpanded` (Phase 4) and Today Nova's Deep Dive already assemble. Everything below reuses data already fetched into `memberCharts[]` for the Family tab (including `chart.yogas`/`chart.doshams` and `dasha.timeline`'s full mahadasha sequence) — no new backend endpoints, no new astrology.*

| Mockup section (family-member) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Top bar + context strip (breadcrumb back to Family, prev/next member arrows) | Shared Nova chrome; `familyAggregate.members` array order for prev/next | New in-tab view-swap (`detailView` state in `DashboardFamilyTabNova`) — no new route; consistent with how `cal-panch`/`cal-monthly` are sub-views of one tab, not separate URLs | **Built** |
| Member header (avatar, name, relation badge, birth identity line, Today status, score dial) | `member.displayName/individualScore`, `memberChart.chart.birthProfile` (birthDateLocal/birthTimeLocal/birthPlace), `memberChart.summary` (lagnaRasi/moonRasi/janmaNakshatra), `todayItem.highlightEn/Ta` (Phase 4's already-surfaced insight) | New header; mockup's "Review" link has no backing feature (only Edit/Remove exist) — **omitted**, same precedent as Phase 1's "Add to my day"/Phase 4's "Plan together" | **Built** |
| Timing strip (Best window / Rahu Kalam / Nakshatram / Chandrashtamam) | Member-specific: `dailyGuidance.bestWindows[0]`, `transit.isChandrashtama`. Shared: `panchangam.kalam.rahuKalam`, `panchangam.nakshatra.name` (newly threaded `panchangam` prop, same object Today/Calendar tabs already use) | New 4-card strip | **Built** — Rahu Kalam/Nakshatram deliberately shared across members (location-based Panchangam, not per-member), consistent with how Phase 4's rhythm card already treats family-wide timing |
| Charts (Rasi D1 + Navamsa D9 side by side) | `RasiChart`/`NavamsaChart` (`dashboard-charts.tsx`) | Reused verbatim (`showExplain={false}`, dropping the click-to-explain accordion the mockup doesn't show) — but see the CSS gap-fix note below | **Built** |
| Guidance card (narrative + "Why this prediction" Dasa/Panchangam/Transit + remedy) | `dailyGuidance.text`/`.remedy` (member); Dasa line from `dasha.current` lords; Panchangam line from shared `panchangam.tithi`/`.yoga`; Transit line from `sani.confirmationSentence` (same ready-made sentence Classic's `DashboardDailySnapshot` already surfaces) or a Chandrashtama caution | New card — the 3-line breakdown is presentation-only text assembled from already-computed fields, no new scoring/astrology | **Built** |
| Dasa card (Dasa/Bhukti/Antaram + % elapsed + upcoming mahadasha list + "show full timeline") | `memberChart.dasha` (current triplet + `.timeline`, the same object `MemberDetailExpanded` already receives) | New compact card; elapsed % computed from `mahadasha.startDate/endDate` vs `selectedDate`; upcoming list = `dasha.timeline` filtered to `level === "maha"` after the current mahadasha, default 3 rows, "show full timeline" expands the rest | **Built** |
| Planets table (5 shown + "All N & degrees") | `memberChart.chart.planets` — same fields Today Nova's Deep Dive planet table already renders (rasiName/houseFromLagna/degreeInRasi/isRetrograde/strengthScore) | Plain `<table>` (matching Today Nova's own precedent — the scaffolded `NovaTable` primitive turned out to have a generic constraint incompatible with typed rows like `ChartPlanet`, so used the same raw-table approach Today Nova already uses rather than fighting it); default-collapsed to first 5 rows, expand adds a Degree column and shows all — link label uses the real planet count, not a hardcoded "10" (mockup's exact 5-of-10 curation isn't derivable from data, so this is an honest simplification, not a copy of the mockup's specific picks) | **Built** |
| Yogas & Doshams (3 shown + "N active" + "show all") | `memberChart.chart.yogas`/`.doshams` (confirmed via grep to be the same fields `ChartExplanationPanel`'s "yogas" section already reads) | New compact one-line-per-row list (not the full `YogaDoshamPanel`, which is a much heavier per-item-expand component reading Classic-only tokens) — `isPresent` yogas + `isPresent` doshams counted as "active"; `YOGA_DISPLAY` map exported (additive) from `dashboard-yoga-dosham-panel.tsx` for yoga display names, doshams use their own existing `.label` field | **Built** |
| Relationships ("In the family" — pairwise score vs each other participant + best shared window) | `compareSynastry()` (Phase 4's endpoint/client wrapper), `familyAggregate.bestFamilyWindows[0]` | New card: the *viewed* member's own row vs every other participant (not the full C(n,2) matrix — that's Phase 4's family-wide Bonds card), lazy-loaded on a "Load →" button (same precedent as Phase 4's bonds card); each other party labeled with their stored `relationshipToOwner` regardless of whose page is open — **documented default**: the system has no member-to-member relation field, only relation-to-owner, and inverting/guessing bidirectional labels (e.g. Kaviya's page showing "Senthil · Husband") would require gendered inference this initiative has repeatedly declined to invent (see Phase 4's own synastry work) | **Built** — score shown on the real /100 scale the backend returns, not rescaled to the mockup's example "/10" (which reads as placeholder mockup data, not a specific product decision) |
| Footer | Already fixed globally | N/A | Done |

**Navigation-pattern decision (user, 2026-07-06):** keep both patterns rather than replace one with the other — Phase 4's inline "Open →" expand (`MemberDetailExpanded` rendered below the grid) stays for a quick glance; a new, more deliberate "View full profile →" button (placed just above the expanded panel, not inside the shared Classic component) switches the tab into the dedicated `family-member` screen built this phase. Back returns to the grid with the same member still expanded inline.

**CSS gap-fix found and corrected while cross-checking `RasiChart`/`NavamsaChart` for this screen:** Phase 4's progress log claimed these were "100% `var(--color-*)`-driven" — that was wrong. Grepping the actual file found ~18 Classic-only literal-hex custom properties (`--panel-tan`, `--chart-bg`, `--chart-cell-default/selected`, `--chart-d1-*`/`--chart-d9-*`, `--planet-lagna/saturn/nodes/other`, etc.), meaning Phase 4's inline chart-grid reuse has been rendering with Classic's tan/cream colors on Nova's dark background since it shipped, unnoticed. Fixed with the same mechanical redirect pattern as every prior gap-fix, added to the existing `[data-ui="nova"] .cd-shell {}` block in `dashboard-nova.css` (not a new block) — mapped to Nova's existing high/mid/low/accent semantic tokens, plus one reused one-off blue (`#7fa3c9`, the same value as `NOVA_CAL_HILITE.sashti` from Phase 3, not a second new blue). This fixes both Phase 4's already-shipped inline expand and this phase's new screen at once.

### 6.5 Phase 6 gap & mapping table (`explore` — Explore hub)

*Populated 2026-07-06. Read the mockup in full (lines 1174-1370 of `Vinaadi Prototype.dc.html`), then `dashboard-explore-tab.tsx` (189 lines) end to end. The two turned out to be almost entirely different concepts, not a re-skin: Classic's `DashboardExploreTab` is a plain nav hub — cards that jump to other dashboard tabs (Transits, Life Areas, Plan, Family, Journal, Tools). The mockup's `explore` screen is a searchable knowledge library (Natchathiram/Dosham/Yogam/Pariharam/Temples/Panchangam) plus a personalised "start from your chart" row and short-read articles — a different feature, matching what §3's original mapping guess already flagged for the sibling `explore-moolam`/`explore-sevvai` screens ("new shared detail shell... this mockup screen doesn't show at all" — same class of finding as Phase 5's).*

| Mockup section (explore) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Page header + search | — | New search input, real client-side substring filter over the Library/Learn cards actually rendered on the page (title/description, both languages) — not a network search across marketing content, since no such index exists; the 3 example query chips (`Guru Peyarchi 2026`/`Sevvai dosham`/`Rahu Kalam`) are reused verbatim from the mockup as one-click search shortcuts, on the assumption that a chip yielding no matches gracefully hands off to the Ask strip below (exactly the interaction the mockup itself frames: "Can't find what you're looking for? Ask Vinaadi") | **Built** |
| Start from your chart — "Your birth star" | `personalChartSummary.janmaNakshatra`/`.janmaPada` + `nakshatraCard` (`NakshatraCardData` — nameEn/Ta, profile, rulingPlanet; same object Today Nova's Deep Dive already renders) | Re-skinned card, real data — no new fetch, `nakshatraCard` was already threaded workspace-wide since Phase 1 | **Built** — CTA reads "Full star profile page — coming soon" rather than linking anywhere: the mockup's own `data-nav="explore-moolam"` target is Phase 7, not yet built (working style: build screen by screen, don't invent the destination early) |
| Start from your chart — "Your rasi" | `personalChartSummary.moonRasi` (display-ready string, already used identically in `dashboard-hero.tsx`/`dashboard-personal-tab.tsx`) | **Omitted.** Grepped the whole `web/` tree for a rasi analogue of `NakshatraCardData` (a per-rasi profile/description type) and for any rasi→ruling-planet lookup table — neither exists anywhere (marketing site has no `/rasi/*` content route either, only `indraiya-rasipalan`, a *today's* rasi-palan tool, not a rasi encyclopedia). The mockup's card is 100% interpretive prose ("Jupiter-ruled moon sign — faith, distance, the long view..."); writing that myself would be inventing astrological content, not re-skinning, so this card is left out entirely rather than shipped half-empty or with fabricated text | **Omitted — genuine content gap, not built** |
| Start from your chart — "Active in your chart" (Sevvai Dosham in the mockup's own example) | `personalChart.doshams` (`ChartDoshamInsight[]`, same field Phase 5 used for family members) — first `isPresent && !isCancelled` entry, generalised rather than hardcoded to Sevvai | Card reuses `dosham.label`/`.descriptionEn/Ta` (already chart-specific, engine-authored) verbatim, plus `strengthBand()` (newly exported, additive, from `dashboard-yoga-dosham-panel.tsx`) for the severity badge — its STRONG/PARTIAL/WEAK → Strong/Moderate/Mild wording already matches the mockup's own "mild" label exactly, so reused rather than re-invented | **Built**, real data, conditional (hidden entirely if no active dosham) — same "coming soon" CTA treatment as the nakshatra card, since `explore-sevvai` is Phase 8, not yet built |
| The library (Natchathiram/Dosham/Yogam/Pariharam/Temples/Panchangam, 6 tiles) | — | In the mockup itself, only 3 of these 6 tiles have a real `data-nav` target (Natchathiram→`explore-moolam`, Dosham→`explore-sevvai`, Panchangam→`cal-panch`); Yogam/Pariharam/Temples self-loop to `explore` (dead in the mockup's own source). Confirmed via `ls` that live, complete marketing content already exists for **all 6** topics (`/natchathiram` [27 pages], `/dosham`, `/yogam`, `/pariharam`, `/temples` index+detail pages), so every tile got a real destination instead of copying the mockup's dead loops: 5 open the matching marketing hub in a new tab (`target="_blank"`, same precedent as Classic's existing `/learn/what-is-chandrashtama` link in `dashboard-personal-tab.tsx`), Panchangam navigates in-app to the `calendar` tab via the existing `onNavigate`/`goToExploreDestination` mechanism (real, better than an external link since it's live personalised data) | **Built**, all 6 tiles functional — entry counts ("27 entries", "12 entries", "30+") from the mockup were **not** reproduced (no live count endpoint for marketing content catalogues exists; inventing one for a cosmetic number risks silently going stale, so tiles use a plain "Browse →"/"Open in Calendar →" instead) |
| Learn (3 short-read article teasers) | — | The mockup's 3 specific topics (Rahu Kalam explained / Guru Peyarchi transit / Dasa timeline) don't exist as articles anywhere (`ls web/app/learn` has 5 real slugs, none matching). Substituted the mockup's 3 example topics with all 5 real, already-shipped `/learn/*` articles (What is Thirukanitham / How to read a Jadhagam / What is Chandrashtama / What is Porutham / Why birth time matters) rather than writing new astrology-explainer prose for topics with no backing page — same class of honest substitution as Phase 5's real-planet-count-not-mockup's-10 | **Built** — reading-time labels ("4 min read" etc.) dropped, no real reading-time data exists and estimating one would be inventing a number, not reusing data |
| Ask strip | Same Ask Vinaadi FAB every Nova screen already opens (`onOpenAskVinaadi`, controlled `open` state lifted into `dashboard-workspace.tsx` since Phase 1) | Reused verbatim — no new chat surface | **Built** |

**Navigation destinations still missing (flagged, not invented):** `explore-moolam` (Phase 7 — personalised Nakshatram profile) and `explore-sevvai` (Phase 8 — personalised Dosham profile) are the two real gaps this screen surfaces; both already have a placeholder-free "coming soon" label rather than a dead link, and both are already sequenced next in §3's Design Inventory table.

### 6.6 Phase 7 gap & mapping table (`explore-moolam` — personalised Nakshatram profile detail)

*Populated 2026-07-06. Read the mockup in full (lines 1372-1543 of `Vinaadi Prototype.dc.html`), then cross-checked against `dashboard-personal-overview.tsx`'s Nakshatra card, Today Nova's Deep Dive Nakshatra card (`dashboard-today-tab-nova.tsx`), and `dashboard-yoga-dosham-panel.tsx`. Also skimmed `explore-sevvai` (Phase 8, lines 1544-1693) to test §3's original guess that the two share "a new shared detail-page shell."*

**Shared-shell judgment call:** the two screens share a real, non-superficial compositional skeleton — Breadcrumb → Hero (kicker+status badge, big serif title+Tamil name, prose) → horizontal Attribute band (N label/value facts) → two-column body (1.5fr prose-and-context cards left, 1fr personalised-and-pariharam cards right) — not just "both are dark cards." But the concrete pieces differ enough (moolam's hero has a symbol-icon box and prev/next star chips sevvai's doesn't; attribute band field-count/labels differ; sevvai's family card is a list-of-badges vs moolam's prose+dasha-note; moolam has an Ask-entry chip sevvai's right rail omits) that a single parameterised `DetailShell` component today would need to guess sevvai's exact needs from a mockup read alone, before Phase 8 is actually built — the classic premature-abstraction trap with only 2 concrete instances. **Decision: build `explore-moolam` as a standalone screen this phase**, extracting only genuinely generic low-level pieces as they naturally arise (a small breadcrumb row, the existing `nova-grid-2`/`nova-grid-3` layout utility pattern) rather than one leaky mega-component — revisit consolidating into a real shared shell once Phase 8 exists and the actual overlap is empirically known, not guessed.

**Real data found during research, not previously surfaced:** (1) `GET /api/v1/content/nakshatra/{n}` (`app/api/content.py`) already accepts **any** nakshatra 1-27, not just the caller's own — already called this way in `usePersonalData.ts`/`useFamilyData.ts` — so the mockup's prev/next star browsing is fully buildable, not a stub. (2) `moonRasiFromNakshatra`/`rasiName` (exported from `dashboard-calendar-tab.tsx` since Phase 2) is the exact classical pada→D1-rasi formula needed for the Attribute band's "Rasi" fact and the padas table's "Navamsa" column — pure deterministic math (each rasi = 9 padas), not new astrology. (3) `personalDailyGuidance.nakshatraPerspective` (already computed server-side by `build_nakshatra_perspective()`, already wired into the API response) is the exact "Today's lens" line the mockup shows, but was only ever rendered in Classic's `dashboard-personal-tab.tsx` and the retired `dashboard-today-tab-v2.tsx` — never in Nova. (4) Ganam/Yoni per-nakshatra are real classical lookup tables already living in `app/calculations/porutham.py` (`GANA_BY_NAKSHATRA`/`YONI_BY_NAKSHATRA`, previously private as `_GANA`/`_YONI`, used only for Porutham match-scoring) — confirmed exact-match against the mockup's own example (Moolam → Rakshasa Gana, Dog Yoni).

**Backend change (additive, no calc-engine logic touched):** renamed `_GANA`/`_YONI` to public `GANA_BY_NAKSHATRA`/`YONI_BY_NAKSHATRA` in `porutham.py` (grepped first — used only within that file, safe rename). Added `ganam`/`yoni` (`NakshatraBiText`) fields to `NakshatraCard` (`app/services/nakshatra_content.py`), auto-populated in `model_post_init` from the same tables plus two new small display-label dicts (`_GANA_LABELS`/`_YONI_LABELS`, standard Tamil/English terms for the 3 gana and 14 yoni categories — not per-nakshatra content, just labelling universal, already-computed codes). Mirrored the two new fields onto the shared `NakshatraCardData` TS type (`packages/shared/src/types/index.ts`) — additive, so `usePersonalData.ts`/`useFamilyData.ts`'s existing calls are unaffected. Grepped `mobile/` — no consumer of this type exists there, nothing to update. This is the same class of small additive backend enrichment as Phase 1's `dateResult` field, done directly rather than paused-on, since the values already existed as classical reference data (zero risk of fabricating astrology) and only widen an existing field set.

| Mockup section (explore-moolam) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Breadcrumb + prev/next star nav | `GET /api/v1/content/nakshatra/{n}` (generic, any 1-27 — confirmed via `app/api/content.py`) | New `DashboardExploreNakshatramNova` component (`dashboard-explore-nakshatram-nova.tsx` — named generically, not after the mockup's "Moolam" example, after a review comment flagged the original `-moolam-` filename as reading like it might be hardcoded to one star; it isn't — `viewedNumber` state cycles 1-27 (wraps) and every field renders from whichever card is fetched); refetches via the same endpoint/`apiFetchJson` pattern `usePersonalData.ts` already uses, cached with `useQuery` (`staleTime: STALE.static`, matching that hook's own treatment of this endpoint since the content never changes) | **Built** |
| Hero (Nakshatram N of 27, "Your birth star · pada P" badge, name EN/TA, symbol, prose) | `NakshatraCardData` (number/nameEn/Ta/symbolEn/Ta/profile) for the *viewed* star; `personalChartSummary.janmaNakshatra`/`.janmaPada` to detect "is this the user's own star" | New hero block; "Your birth star" badge only rendered when `viewedNumber === own janma nakshatra number` — browsing to any other star shows the identical hero with no such badge, not a fabricated one | **Built** |
| Attribute band (Lord / Rasi / Deity / Ganam / Yoni) | `nakshatraCard.rulingPlanet`/`.deityEn/Ta` (existing); `moonRasiFromNakshatra(nameEn, 1)` + `rasiName()` (existing, exported) for Rasi; **new** `nakshatraCard.ganam`/`.yoni` (this phase's additive backend fields) | Inline in the new component | **Built** — mockup's "Dhanusu 0°–13°20′" degree-range sub-text under Rasi deliberately simplified to just the rasi name: computing a fully correct degree range for all 27 nakshatras requires handling the ~9 that straddle two rasis (not evenly divisible by 30°), and getting that wrong would be a real domain-accuracy bug, not a cosmetic one — an honest simplification, not a data gap |
| Nature prose + strength/caution tags | `nakshatraCard.profile`/`.strengths`/`.cautions` (existing, same fields Today Nova's Deep Dive and Phase 6's birth-star card already render) | Reused verbatim as tag chips | **Built** — mockup shows 2 prose paragraphs; `NakshatraCardData.profile` is a single `BiText`, so this renders as one paragraph (the data doesn't have a second) — same honest-simplification class as Phase 5's planet-count link text |
| The four padas table (Pada / Syllable / Navamsa / Flavour, "YOU" highlight) | `moonRasiFromNakshatra(nameEn, pada)` + `rasiName()` for the Navamsa column (existing, real classical math); `personalChartSummary.janmaPada` for the "YOU" highlight | New 4-row table, **Pada + Navamsa columns only** | **Built, 2 of 4 mockup columns** — Syllable (baby-naming letters, e.g. Moolam = Ye/Yo/Ba/Bi) and Flavour (108 per-pada interpretive sentences) genuinely don't exist anywhere in the codebase (grepped for a naming-syllable table, found none) or the `/natchathiram` marketing pages (checked `mula/page.tsx` — long-form SEO prose, no per-pada breakdown) — fabricating either would be inventing astrological content, so both columns are omitted rather than filled with invented text |
| "In your chart" personalised card (pada + navamsa + Today's lens tied to score) | `personalChartSummary.janmaPada`; `personalDailyGuidance.nakshatraPerspective` + `.score` (existing API field, already computed, never rendered in Nova — see research note above) | New card, only rendered when `viewedNumber === own janma nakshatra number` (hidden while browsing other stars, since it's inherently about "your" chart) | **Built** — re-skin of an already-computed, previously-Nova-unsurfaced feature, same class of discovery as Phase 4's family `summary` narrative |
| "Pariharam for X-born" card (temple, mantra day, ritual acts) | — | **Not built as mockup'd.** Grepped for a structured per-planet remedy table (temple name / day / mantra) — none exists; the `/pariharam` marketing pages are long-form prose, not a lookup keyed by planet. Fabricating temple names or specific rituals per nakshatra would be inventing religious/astrological content. Originally shown as a link-out card to `/pariharam` (`target="_blank"`) — **revised 2026-07-06** after browser QA: the link-out was the card's only real content, and a breadcrumb/card that silently jumps to the marketing site read as broken, not helpful. Card is now omitted entirely (see Progress Log) | **Omitted — no link-out substitute** |
| "In your family" card (who shares this star, upcoming same-planet dasha) | `memberCharts[].nakshatraCard.number` (star match, already fetched per member since before this phase), `memberCharts[].dasha.timeline` filtered to `level === "maha" && lord === rulingPlanet && startDate` in the future (same mahadasha-timeline shape Phase 5 already reads) | New card: owner's own row (highlighted if it matches the viewed star) + any members who share it + any member with a real upcoming mahadasha of the viewed star's ruling planet — all derived from data already fetched for the Family tab, newly threaded into the Explore tab (`memberCharts` prop, additive, same plumbing precedent as Phase 5's `panchangam` prop into the Family tab) | **Built** |
| Ask entry chip | Same Ask Vinaadi FAB every Nova screen opens | Reused verbatim | **Built** |
| Footer | Already fixed globally | N/A | Done |

**CTA wiring:** Phase 6's birth-star card ("Full star profile page — coming soon") in `dashboard-explore-tab-nova.tsx` now opens this screen at the user's own nakshatra number instead of showing static text; the library's Natchathiram tile stays pointed at the external `/natchathiram` marketing hub (unchanged — that's the full 27-star encyclopedia, a different, larger surface than this single personalised detail view).

### 6.7 Phase 8 gap & mapping table (`explore-sevvai` — personalised Dosham profile detail)

*Populated 2026-07-06. Re-grepped `data-screen=` first (file still 14 blocks, `explore-sevvai` unchanged at lines 1544-1693) and read it in full. Cross-checked against `dashboard-explore-tab-nova.tsx`'s "Active in your chart" dosham card, Phase 5's Yogas & Doshams compact list (`dashboard-family-member-nova.tsx`), and — critically — `dashboard-yoga-dosham-panel.tsx` end to end (1074 lines) before assuming anything needed a link-out treatment.*

**A structural difference from Nakshatram, found before building anything:** there is no dosham "encyclopedia" endpoint independent of a chart the way `GET /api/v1/content/nakshatra/{n}` is — `ChartDoshamInsight` (`app/schemas/charts.py`) is inherently chart-relative (`isPresent`/`conditionsMet`/`strength` only exist for a specific birth chart). So "browsing" this screen cycles through the 8 dosham types the engine always computes for the signed-in user's own chart (`personalChart.doshams` — confirmed via `app/calculations/yogas.py:224-292`: Sevvai, Rahu-Ketu, Pitru, Kalathra, Putra Sarpa, Marana Karaka Sthana, Badhaka, Kalasarpa, always returned in this fixed order regardless of presence), not any-of-N independent of the viewer. There is consequently no "browsing someone else's" vs "your own" split the way Nakshatram has — every dosham viewed here already is about the viewer's own chart.

**Much more real content existed than the working assumption predicted, verified across 4 structurally different dosham types (Sevvai — house-based; Pitru — conjunction/9th-house-based; Badhaka — lagna-dependent, no fixed house set; Kalasarpa — arc-based, not house-based at all), not just the mockup's Sevvai example:**
1. `ChartDoshamInsight` already carries `conditionsMet`/`cancellationFactors`/`explanationWhatEn·Ta`/`explanationWhyEn·Ta`/`explanationHowEn·Ta` (`app/schemas/charts.py:89-109`) — full per-condition engine-authored prose (`_build_dosham_explanations`/`_marker_explain` in `app/calculations/_yoga_helpers.py`), already generic across every dosham type, already exposed via the API, but only ever consumed by Classic's `YogaDoshamPanel` accordion — never by anything Nova.
2. `dashboard-yoga-dosham-panel.tsx` already has real per-dosham content maps used by Classic's (unexported) `DoshamCard`: `DOSHAM_WHAT`, `DOSHAM_OUTCOMES`, `DOSHAM_HOW_TO`, and — the big find — `DOSHAM_REMEDIES`, real structured-enough remedy prose for 7 of the 8 types, including the *exact* temple (Vaitheeswaran Koil) and day (Tuesday) the mockup names for Sevvai. This means the mockup's Pariharam card is mostly **real, not a stub** — the opposite of Phase 7's nakshatra-pariharam finding. Shown as the flowing prose it already is, not forced into the mockup's exact 3-icon-row layout (which would over-imply a structure — temple / homam / mantra as three independently-sourced facts — the underlying text doesn't actually have).
3. A real bug found in the same file while reading it: `YOGA_DISPLAY`'s Tamil entries for all 7 dosham keys were placeholder English text copy-pasted into the `.ta` slot (e.g. `SEVVAI_DOSHAM: { ta: "Sevvai Dosham", en: "Sevvai Dosham" }`), silently shipping non-Tamil text in Tamil mode on Classic's own `YogaDoshamPanel` and Phase 5's family-member Yogas & Doshams list. Fixed with the real Tamil terms the backend itself already uses verbatim in `_yoga_helpers.py`'s `what_ta_map`/`description_ta` strings (not invented), and added the one missing `MARANA_KARAKA_STHANA` entry. Frontend-only fix, benefits Classic too, not scoped to Nova.
4. Real per-dosham marketing guides exist for 5 of the 8 types (`ls web/app/dosham`: `sevvai-dosham`, `naga-sarpa-dosham` [covers Rahu-Ketu], `kala-sarpa-dosham`, `pithru-dosham`, `kalathra-dosham`) — used for a "Read the full guide →" link, falling back to the general `/dosham` hub for the 3 without a dedicated page (Putra Sarpa, Badhaka, Marana Karaka Sthana), rather than guessing a URL.
5. Grepped `app/services/marriage_service.py` to check the mockup's "Related — how this factors into Porutham matching" link honestly rather than assuming it applies to every dosham type: confirmed only `SEVVAI_DOSHAM` and `RAHU_KETU_DOSHAM` labels actually feed the compatibility-challenge logic there. The Related card's Porutham link is shown only for those two; the other 6 dosham types get just the general "Browse all doshams" link.

**What genuinely does *not* generalise (the mockup's own attribute-band facts were Sevvai-specific, not universal):** the mockup's "Trigger houses: 1 · 2 · 4 · 7 · 8 · 12" and "Planet: Sevvai · Mars" facts don't hold across dosham types — Rahu-Ketu has two different house sets depending on category (marriage houses 1/2/7/8 vs sarpa houses 5/9, `_yoga_helpers.py`), Pitru/Kalasarpa aren't house-based at all, and Kalathra/Badhaka are about a *lord's* placement (7th lord in 6/8/12; the badhaka lord varies by lagna type), not a fixed house list counted from Lagna. The exact house number that triggered a dosham also isn't returned by the API in any case (`house_hits` in `_yoga_dosham.py` is used internally to decide `is_present`/`strength`, never serialized). Replaced with two facts that do hold for all 8: a `category`-derived "Mainly touches" label (small presentation-only map over the engine's existing, already-computed `category` enum — same class of addition as Phase 7's Ganam/Yoni labels) and a Dasha-activation fact (`dashaActivated`, already a real per-dosham field).

**Shared-shell empirical check (per the working style — Phase 7 deferred this until a second concrete instance existed; it now does):** the breadcrumb+prev/next row, hero shell (kicker/badge/title+secondary-name/prose), attribute-band grid, two-column card layout, card container style, and Ask-entry chip turned out to be pixel-identical between the two screens — `NovaKicker` and the card-style object were in fact already copy-pasted verbatim between the two files before this phase. Extracted those genuinely generic, presentation-only pieces (zero business logic) into a new `dashboard-explore-detail-nova.tsx` (`NovaKicker`, `novaDetailCardStyle`, `NovaDetailBreadcrumb`, `NovaDetailHero`, `NovaAttributeBand`, `NovaAskEntryChip`) and refactored Phase 7's `dashboard-explore-nakshatram-nova.tsx` to use them too — not just this phase's new file — proving the extraction actually removes duplication rather than just asserting it. The higher-level content cards (What-it-means/When-it-cancels/In-your-chart/Pariharam/In-your-family) stay bespoke per screen: their data shapes (a chart-independent encyclopedia entry vs. a chart-relative computed result) diverge enough that forcing them into one shared component would still be the premature-abstraction trap §6.6 flagged, just one level deeper. Also fixed a small pre-existing bug found in the process: `.nova-grid-detail` (Phase 7's two-column wrapper class) was referenced but never defined in `dashboard-nova.css`, and the div also carried a literal inline `gridTemplateColumns`, meaning the intended mobile collapse silently never worked. Added the real `.nova-grid-detail` rule (mirroring `.nova-grid-2/3`'s existing pattern) and removed the redundant inline override from both screens.

| Mockup section (explore-sevvai) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Breadcrumb + prev/next dosham nav | `personalChart.doshams` (fixed 8-entry array, always present regardless of `isPresent`) | `DashboardExploreDoshamNova` (`dashboard-explore-dosham-nova.tsx`) — cycles by array index (wrap-around), not a per-type content fetch (no such endpoint exists) | **Built** |
| Hero (Dosham library kicker, "Present in your chart · mild" badge, name EN/TA, prose) | `dosham.name` → `displayName()` (now with real Tamil, see finding #3); `dosham.explanationWhatEn/Ta`/`descriptionEn/Ta` via `getWhat()` (both additively exported from `dashboard-yoga-dosham-panel.tsx`) | New hero; badge reads "Not present in your chart" (still reassuring, not alarming) when browsing an absent dosham type, rather than hiding the badge | **Built** |
| Attribute band (Planet / Trigger houses / Mainly touches / In your chart) | `dosham.category` (existing field) | Replaced the two non-generalising facts (Planet, Trigger houses — see finding above) with `category` → "Mainly touches" (new small label map) and a Dasha-activation fact; kept "In your chart" (status) and added Severity | **Built, 4 of 4 mockup slots filled with generalisable facts, 2 of the mockup's specific facts substituted** |
| "What it actually means" + reassurance tip | `getWhat()` (existing); `dosham.explanationHowEn/Ta` (existing, generic guidance-signal boilerplate already returned by every `detect_*_dosham()` call) | Reused the engine's own "use this as a guidance signal, not a fixed outcome" text in place of the mockup's Sevvai-specific "roughly one chart in three" prevalence stat — no such prevalence-rate data exists anywhere, so it was not invented | **Built** |
| "When it cancels or softens" | `dosham.conditionsMet`/`.cancellationFactors` + `markerLabel()` (additively exported) | Bullet lists — reuses `DoshamCard`'s existing trigger/cancellation-bullet split (excluding annotation-only markers) rather than re-deriving it | **Built** |
| "In your chart" panel | `buildWhyText()` + `getDoshamPowerContext()` (both additively exported) | New card, only rendered when `isPresent` (mirrors Nakshatram's own "only when it applies to you" precedent); "See it in your chart →" now a real button (`onNavigateToday` → the Today tab), matching the mockup's own `data-nav="today"` target exactly | **Built** |
| Pariharam card | `DOSHAM_REMEDIES[dosham.name]` (existing, additively exported — see finding #2) | Real prose for 7 of 8 types. **Revised 2026-07-06**: dropped the trailing "Read the full guide →" marketing link after browser QA flagged it as an unwanted site-exit; Marana Karaka Sthana (no remedy entry) now gets no Pariharam card at all rather than a link-only fallback — matching the precedent Classic's own `DoshamCard` already follows (`remedies && isPresent`) | **Built, real prose for 7 of 8 — no outbound link** |
| "Check your family" card | `memberCharts[].chart.doshams` filtered to the matching `dosham.name` (same field already used per-member since Phase 5) | New card: owner's own status row + each member who has this same dosham type computed, each with a real status badge (Active/Mitigated/Absent) | **Built** |
| "Related" card | `app/services/marriage_service.py` (grepped, see finding #5) | Porutham-matching note shown only for Sevvai/Rahu-Ketu (verified relevant). **Revised 2026-07-06**: both this and the general "Browse all doshams" link opened the marketing site — the Porutham note is now plain informational text (no link), and "Browse all doshams" was dropped entirely (redundant with the breadcrumb's own prev/next dosham browsing, which is real personalised data rather than generic marketing prose); the card itself is omitted for the 6 non-Porutham-relevant dosham types rather than rendered empty | **Built, informational text — no outbound links** |
| Ask entry chip | Same Ask Vinaadi FAB every Nova screen opens | Reused verbatim via the new shared `NovaAskEntryChip` | **Built** |
| Footer | Already fixed globally | N/A | Done |

**CTA wiring:** Phase 6's "Active in your chart" dosham card in `dashboard-explore-tab-nova.tsx` (its CTA read "Full profile page — coming soon") is now a real button opening this screen at the active dosham's index; the mockup's Sevvai-specific label text is generalised (`activeDosham.label`/`.descriptionEn/Ta` were already generic fields, unchanged).

**Verified generalisation, not just a Sevvai re-skin:** spot-checked the built screen's logic (not just data lookups) against Sevvai (MARRIAGE, house-based), Pitru (PITRU, conjunction/9th-house-based), Badhaka (OBSTACLES, lagna-dependent with no fixed house set) and Kalasarpa (KALA_SARPA, arc-based) — all four render correctly through the same component with no per-type branching beyond the small presentation maps (`DOSHAM_CATEGORY_LABEL`, `DOSHAM_MARKETING_HREF`, `POROUTHAM_RELEVANT`) documented above, matching the same file/logic-generalisation bar Phase 7's Ganam/Yoni fix was held to.

### 6.8 Phase 9 gap & mapping table (`life-areas` — Life Areas tab)

*Populated 2026-07-06. Re-grepped `data-screen=` first (file still 14 blocks, `life-areas` unchanged at lines 2018-2251) and read it in full, then read `dashboard-life-areas-tab.tsx` (333 lines) and `life-area-card.tsx` (209 lines) end to end, plus skimmed `dashboard-prediction-panel.tsx`/`dashboard-remedies-panel.tsx`/`dashboard-jadhagam-report-panel.tsx`/`dashboard-event-windows.tsx` for Nova-token safety before deciding scope.*

**Not a "detail drill-down" — no shared-shell question applies.** Unlike `explore-moolam`/`explore-sevvai` (Phases 7-8, single-entity detail pages reached by browsing from Explore), `life-areas` is a full top-level tab screen with its own 5-way sub-nav (Overview/Predictions/Yogas & Doshams/Remedies/Full report) and a member switcher — the same shape as the Today/Family/Explore Nova tabs, not the `dashboard-explore-detail-nova.tsx` shell. No comparison to Phase 7/8's shared pieces was forced.

**§4.3 correction (per the working style — this grouping was flagged as an unverified first-pass guess):** `dashboard-life-event-log.tsx` and `dashboard-life-events.tsx` were listed under "Explore umbrella: Life Areas" in §4.3, but grepping their actual imports shows both are used exclusively by `dashboard-plan-tab.tsx` — they belong to **Plan** (next phase), not Life Areas. Not read for this phase; will be read properly when Plan is built. §4.3's table has not been edited in place (it's already flagged as unverified at the top of that section) but this correction is the authoritative one going forward.

**Much closer to a re-skin than a rebuild — the mockup's static export turned out to be a live capture of Classic's own "Overview" sub-tab, same tell as Phase 2's `cal-panch` finding:** Classic's `DashboardLifeAreasTab` already has the exact hero copy ("Where you stand, area by area." / the natal+dasha+transit methodology line), the same member-pill switcher, and the identical 5-tab sub-nav (Overview/Predictions/Yogas & Doshams/Remedies/Full report) the mockup shows as chrome. `LifeAreaCard` already renders every single per-card field the mockup shows: trend badge (UP/DOWN/STABLE, displayed as "FLAT" for STABLE — cosmetic label choice, not a bug), score+/100 with a colored bar, "Your focus" goal badge (`area.isGoalFocus`), Karaka+planet, primary-house-strength/karaka-status/dasha-activation/transit-support chips, supporting/blocking factors (or a `causalChain` "because...therefore" sentence for low-confidence areas — Reasoning Phase 5 output, already shipping), a "Next 30 days" outlook box, an optional caution box, an optional remedy box, and a "Details" button that opens `DrawerPanel`. All 12 of the engine's life areas (`app/services/life_areas_service.py:1401-1413` — CAREER/MONEY/HEALTH/RELATIONSHIPS/EDUCATION/SPIRITUAL/FAMILY_HARMONY/CHILDREN/PROPERTY/FOREIGN/LITIGATION/SPIRITUALITY, always computed in this fixed order) match the mockup's 12 example cards exactly, including the easy-to-miss detail that "Spiritual growth" and "Spirituality" are two genuinely distinct backend keys (SPIRITUAL vs SPIRITUALITY), not a mockup duplication mistake.

**Real, already-computed data found for the mockup's two genuinely new ideas:**
1. **Tier grouping** ("Needs attention"/"Steady"/"Supportive" instead of one flat grid) — no such bucketing exists anywhere, but `getScoreBand()`'s existing tone thresholds (`@vinaadi/shared/utils/score`: high ≥65, mid 45-64, low <45 — already used for every other score-color decision app-wide) map almost exactly onto the mockup's three groups. The one mismatch: the mockup's own example puts Health (score 45, which is "mid" tone) under "Needs attention" because its prose flags an active Ardhashtama Sani caution. Rule used: an area is "Needs attention" if `tone === "low"` **or** `area.caution` is non-null (a real, already-computed field), "Supportive" if `tone === "high"` (and no caution override), else "Steady" — same class of real-field-driven bucketing as Phase 4's `score < 45 OR chandrashtama` family "needs care" rule, not an invented threshold.
2. **Goal focus strip** (goal-name chips + "Edit goals in Plan →") — `LifeAreaData.isGoalFocus` (already used for the per-card badge) says *which areas* are goal-driven but not the *goal names* themselves ("Start Business", "Job Change"). The real goal list (`GoalData[]`, with `goalType`) is already fetched workspace-wide by `usePlanData()` (`dashboard-workspace.tsx:464`) for the Plan tab — **not** re-fetched, just threaded as a new additive `goals` prop into the Nova Life Areas component (same "already-fetched, just not surfaced here" plumbing precedent as Phase 4/7/8's cross-tab props). Goal-type→display-label mapping (`GOAL_OPTIONS`, i18n keys like `goal_job_change`/`goal_business`) already exists in `dashboard-plan-tab.tsx` — exported additively (zero behavior change) rather than re-authoring goal names. **Checked, not assumed, which tab-switch callback to reuse**: `goToExploreDestination` looked like the obvious precedent (used for Today Nova's `onGoToFamily`/`onGoToJournal`/`onGoToCalendar`), but tracing its actual mechanism (`dashboard-workspace.tsx:298-306` + the `exploreReturnTab === activeTab` banner at line ~1198) shows it specifically marks "the user arrived here from the Explore hub, show a Back-to-Explore banner" — reusing it from Life Areas (which the user did not reach via Explore) would incorrectly paint a misleading "← Back to Explore" banner on the Plan/Transits tab. Used the plainer `goToTab` instead (clears any stale explore-return state, just switches tabs) — two new callbacks (`onGoToPlan`, `onGoToTransits`), no new nav mechanism invented, but a deliberately different existing one than the closest-looking precedent.

**Overview is the only sub-tab in scope this phase — the other 4 are deferred, not rebuilt, matching the Phase 2 Monthly-toggle precedent:** the mockup's static export only expands the "Overview" pill; Predictions/Yogas & Doshams/Remedies/Full report are shown only as unexpanded nav pills, with no captured content to build against. Checked each of the 4 underlying panels for Nova-token safety before deciding: `dashboard-prediction-panel.tsx` and `dashboard-remedies-panel.tsx` both read multiple Classic-only literal-hex custom properties (`--panel-*`, `--cl-*`, `--chart-*`, `--planet-*` — confirmed via grep, no `[data-ui="nova"]` override), `dashboard-jadhagam-report-panel.tsx` likewise (15 Classic-only token hits), and `YogaDoshamPanel` was already confirmed Classic-token-heavy during Phase 5/8. Only `dashboard-event-windows.tsx` (used inside Predictions) is already 100% Nova-safe. Rebuilding all four would be a second Today-tab-sized effort with no mockup to build against — deferred exactly like Phase 2 deferred `cal-monthly` initially: reused Classic-styled (light/cream) verbatim inside the Nova tab so every sub-tab stays fully functional, flagged here for a future pass rather than silently left half-done.

| Mockup section (life-areas) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Header (kicker "Life areas · Age N", headline, methodology subcopy, member pills, 5-tab sub-nav) | `DashboardLifeAreasTab`'s existing hero copy, `isAreaRelevantForAge`/age-phase logic (additively exported), member-pill switcher, `SUB_TABS` array (all reused verbatim, content-identical to the mockup) | `dashboard-life-areas-tab-nova.tsx` header block, Nova-token styling only | **Built** |
| Goal focus strip ("Your focus" + goal chips + highlighted-areas sentence + "Edit goals in Plan →") | `lifeAreas.areas[].isGoalFocus` (existing); **new** `goals` prop (`plan.goals`, already fetched workspace-wide, threaded in) + additively-exported `GOAL_OPTIONS`/i18n label keys from `dashboard-plan-tab.tsx` | New strip; only rendered when at least one goal is active (no decorative empty state); "Edit goals in Plan →" wired to a new `onGoToPlan` callback (`goToTab`, see finding above) | **Built** |
| Needs attention / Steady / Supportive groups (4-up card grids) | `lifeAreas.areas` (existing); new tier derivation (`getScoreBand(area.score).tone` + `area.caution` override, see finding above) | New `.nova-grid-4` responsive utility (4→2→1 columns, mirrors the existing `.nova-grid-2/3` pattern in `dashboard-nova.css`); group headers reuse the mockup's own tone colors (amber/gold/sage, matching Nova's existing high/mid/low semantic tokens) | **Built** |
| Life area card (title, "Your focus" badge, trend, score+bar, Karaka+house line, prose, "Next 30 days" tip, remedy footer, "Details →") | `LifeAreaCard` (Classic) — grepped every `var(--...)` use in the file: 100% `var(--color-*)`/`var(--space-*)`-driven, and the two Classic-named tokens it reads (`--chart-d1-lagna-bg`, `--panel-warm-tint`, `--planet-lagna`) were **already** redirected to Nova-safe values by earlier phases' `dashboard-nova.css` gap-fix block (Phase 1/4/5) — confirmed Nova's own `--color-score-high/mid/low` (`#8fbf9a`/`#d4af5f`/`#e08a72`) are the *exact* hex the mockup itself uses for score text, since both trace back to the same design source | Reused **verbatim**, not re-skinned — no new Nova-specific card component needed, same class of free reuse as Phase 2's `DayTimeline`. One real pre-existing bug found and fixed while cross-checking (not invented): the remedy box's body text used a literal hardcoded `#7a3412` (no CSS variable at all), which would render as low-contrast rust-brown on Nova's dark card background; changed to `var(--color-text)` (Classic's own already-standard body-text token, confirmed to resolve to the same dark ink Classic already uses elsewhere — visually a no-op there, and correctly legible under Nova). "Details →" opens the existing `DrawerPanel` (already Nova-safe by default per Phase 3's finding) | **Built** |
| "Why these scores" explainer + "See the transits behind them →" | Same methodology line `DashboardLifeAreasTab`'s hero already states | New card, links to a new `onGoToTransits` callback (`goToTab`) | **Built** |
| Sub-tabs: Predictions / Yogas & Doshams / Remedies / Full report | `PredictionDetailPanel` + `EventWindowsPanel`, `YogaDoshamPanel`, `RemediesPanel`, `JadhagamReportPanel` (all existing, all functional) | **Re-skinned 2026-07-06** (see new §6.8.1 below) — `dashboard-life-areas-predictions-nova.tsx`, `dashboard-life-areas-yogas-doshams-nova.tsx`, `dashboard-life-areas-remedies-nova.tsx`, `dashboard-life-areas-report-nova.tsx` | **Built** |
| Footer | Already fixed globally | N/A | Done |

### 6.8.1 Life Areas' 4 deferred sub-tab panels, re-skinned (2026-07-06)

*Follow-up pass, done after all 14 mockup screens were built and `/dashboard/v2` was retired (§4.6). None of these 4 panels has a mockup to build against (the mockup's static export never expands them past "Overview," per §6.8's own finding) — this is extrapolation work per §3.1's policy: reuse Nova's established tokens/primitives rather than inventing a new visual language or leaving them in Classic. Read all 4 Classic files end to end and grepped every `var(--...)` reference before writing any code, per the working style.*

**Predictions (`dashboard-prediction-panel.tsx`) — a near-miss, not a clean pass.** Most tokens were already Nova-redirected (`--chart-d9-active-bg`, `--panel-warm-tint`, `--chart-d1-lagna-bg`, the `SCORE_HIGH/MID/LOW` constants), but every confidence/band chip's **border** color read `--cl-sage-border`/`--cl-rust-35`/`--cl-brand-edge` — three near-miss siblings of the already-redirected `--cl-sage-edge`/`--cl-rust-edge`/`--cl-rust-30` trio (Phase 4's gap-fix), never added to that block. Grepped app-wide before redirecting (per CLAUDE.md's instruction, since a repeat of the 2026-07-06 browser-QA mistake was explicitly flagged as a risk): both are used by several Classic-only dasha-panel/calendar/synastry/plan components and 2 marketing pages (`tamil-calendar`, `jadhagam-generator`) that never set `[data-ui="nova"]`, so redirecting them at the existing `[data-ui="nova"] .cd-shell {}` scope (not bare `:root`) is safe. Fixed centrally in `dashboard-nova.css` — this also fixes two already-shipped, unnoticed border-color bugs: `dashboard-synastry-panel.tsx`'s Compatibility sub-view (reused verbatim under Nova since Phase 4, but its own claim of being "100% `var(--color-*)`-driven" turns out to have been as incomplete as Phase 4's identical claim about `RasiChart`/`NavamsaChart`, corrected in Phase 5) and `lib/reasoning.ts`'s `bandTone()` (used by `LifeAreaCard`'s causalChain path, already shipped). With that fix, `confidenceTone()`/`bandTone()` became fully Nova-safe, so `dashboard-life-areas-predictions-nova.tsx` (`NovaPredictionsPanel`) reuses them directly via 5 additive `export` keywords (`confidenceTone`, `confidenceLabel`, `supportTone`, `supportLabel`, `predictionIsDeferred`) rather than re-deriving tone logic — only the JSX/layout is rebuilt (same accordion structure: one featured/expanded card, the rest collapsed rows, same married-7th-house reframing and later-phase ordering).

**Yogas & Doshams (`dashboard-yoga-dosham-panel.tsx`) — a real rebuild, not a near-miss.** Unlike Predictions, this one's (unexported) `YogaCard`/`DoshamCard` read `--panel-earth-dark`/`--panel-tan`/`--panel-cream`/`--chart-cell-default` plus several **literal inline `rgba(...)` values with no `var()` at all** — the same class of Classic-only styling as Phase 2/3/11's from-scratch rebuilds, confirmed already Classic-token-heavy during Phase 5/8/9's research. `dashboard-life-areas-yogas-doshams-nova.tsx` (`NovaYogaDoshamPanel`) rebuilds the JSX/styling fresh with Nova tokens, reusing every piece of pure data/logic via the additive exports already made across Phases 5/8/9 (`YOGA_DISPLAY`, `displayName`, `markerLabel`, `getWhat`, `buildWhyText`, `strengthBand`, `doshamSeverityScore`, `getDoshamPowerContext`) plus 4 more made this pass (`getYogaPowerContext`, `YOGA_OUTCOMES`/`YOGA_HOW_TO`/`YOGA_REMEDIES` — the yoga-side siblings of the already-exported `DOSHAM_OUTCOMES`/`DOSHAM_HOW_TO`/`DOSHAM_REMEDIES`).

**Remedies (`dashboard-remedies-panel.tsx`) — a real rebuild, same class as Yogas & Doshams.** Its own `W` token map reads the exact 5 custom properties (`--panel-earth-dark`/`--panel-earth`/`--panel-tan`/`--panel-tan-light`/`--panel-hover`) the 2026-07-06 browser-QA round reverted (not redirected) after they broke selected-pill text app-wide — the same finding Phase 11 (Journal) already made about its own token map — plus several Classic-only `--cl-*` rgba tints (`--cl-neutral-tint/-ring`, `--cl-brand-tint/-ring`, `--cl-sage-tint/-ring`) with no Nova override. Its `PLANET_COLORS` map also reads raw `--planet-sun/-moon/-mars/-mercury/-jupiter/-venus/-saturn-soft/-rahu/-ketu` — grepped `dashboard-nova.css` and confirmed none of these are redirected (only `--planet-lagna`/`-saturn`/`-nodes`/`-other` are). Rather than invent a new mapping, `dashboard-life-areas-remedies-nova.tsx` (`NovaRemediesPanel`) reuses the exact per-planet color precedent §6.10's Transits view already established (Sun/Jupiter → `--color-accent-strong`, Moon/Venus → `--color-accent-secondary`, Mars → `--color-low`, Mercury → `--color-high`, Saturn → the existing one-off `--planet-other` blue, Rahu/Ketu → plain text) for consistency across Nova rather than a second independent mapping. `CollapsibleSection` is reused verbatim (confirmed Nova-safe — its classes read `var(--color-text)`/`var(--color-muted)`, and the one place `globals.css` hardcoded literal Classic hex for it at `.cd-shell` scope was already gap-fixed in Phase 1). The shared `Button` primitive from `dashboard-ui.tsx` was deliberately **not** reused: its inline `style` object reads `--panel-brand`/`--panel-earth`/`--panel-tan`/`--panel-brand-border` directly — none of those are Nova-redirected either (confirmed via grep), so `Button` would render Classic's terracotta background on a Nova page despite the class-level gap-fix already in `dashboard-nova.css` (an inline style always wins over a class rule for the same property, regardless of specificity). No currently-shipped Nova file imports `Button` — this is a latent, not-yet-triggered gap, flagged here rather than fixed, since fixing it is out of this pass's scope (nothing built so far needed it; this file's own inline Nova button follows the same precedent every other Nova file already uses).

**Full report (`dashboard-jadhagam-report-panel.tsx`) — the one panel that was mostly already safe.** Read all 405 lines end to end: `Section`/`Row`/`StrengthBar`/`NatureBadge`/`FocusBadge` all read only core `--color-*`/`--radius-*`/`--space-*` tokens or already-redirected Classic-named ones (`--panel-cream`, `--planet-nodes`, `--planet-other`, `--chart-d9-active-dark`, `--chart-d1-lagna-bg`) — confirmed via grep against `dashboard-nova.css`, ~95% Nova-safe already. Two real issues, not a wholesale rebuild: (1) one literal hardcoded `#7a3412` (no CSS variable at all) in the Remedies section's caution text — the exact same bug class Phase 9 found and fixed in `life-area-card.tsx`'s remedy box. Fixed directly in the Classic file (`var(--color-text)`, confirmed to resolve to Classic's own near-black `#1A1612` — a visual no-op there, correctly legible under Nova) — benefits Classic too, same precedent as every prior phase's "found while reading for Nova-compat reasons" fixes. (2) The embedded Yogas & Doshams section reuses `YogaDoshamPanel`, which (unlike the rest of this file) is Classic-token-heavy. Given how much of the file was already safe, duplicating all ~400 lines for one swapped-out sub-component would be worse than the alternative used here: added one optional `renderYogaDoshamPanel` prop to `JadhagamReportPanel` (additive — Classic's own two call sites, `dashboard-life-areas-tab.tsx` and nothing else, omit the prop and get identical behavior). `dashboard-life-areas-report-nova.tsx` (`NovaJadhagamReportPanel`) is a thin wrapper that reuses `JadhagamReportPanel` verbatim, substituting only that one section's renderer with the fresh `NovaYogaDoshamPanel` this same pass already built. **Documented judgment call**: this is different in kind from the other 3 panels' "always build a fresh file" pattern — that pattern exists because those files were *entirely* Classic-token-unsafe; this one mostly wasn't, so the more surgical fix (one additive prop + a thin wrapper) beats duplicating a near-safe 400-line file.

Verified: `pnpm --filter jothidam-ai-web exec tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 8 touched/new files (`dashboard-life-areas-predictions-nova.tsx`, `dashboard-life-areas-yogas-doshams-nova.tsx`, `dashboard-life-areas-remedies-nova.tsx`, `dashboard-life-areas-report-nova.tsx`, `dashboard-life-areas-tab-nova.tsx`, `dashboard-prediction-panel.tsx`, `dashboard-yoga-dosham-panel.tsx`, `dashboard-jadhagam-report-panel.tsx`). No backend changes. Did not drive a real authenticated browser session — same handoff as every prior phase; user sign-off still needed.

### 6.9 Phase 10 gap & mapping table (`plan` — Plan tab)

*Populated 2026-07-06. Re-grepped `data-screen=` first (file unchanged at 14 blocks, `plan` still lines 2251-2421) and read it in full, then read `dashboard-plan-tab.tsx` (682 lines), `usePlanData.ts`, `dashboard-decision-panel.tsx`, and `dashboard-event-windows.tsx` end to end before assuming anything about the mapping (per the working style — §4.3's own grouping guess had already been corrected once, by Phase 9, to move `dashboard-life-event-log.tsx`/`dashboard-life-events.tsx` here; that correction held up on this real read).*

**Not a "detail drill-down" — no shared-shell question applies**, same reasoning as Phase 9: `plan` is a full top-level tab screen with its own 5-way sub-nav (Goals/Life Events/What-If/Best Dates & Muhurta/Decisions), not a single-entity page reached by browsing — the `dashboard-explore-detail-nova.tsx` shell doesn't fit. Only its genuinely generic `novaDetailCardStyle` card container is reused (same precedent as Phase 9), nothing structural.

**Only the "Goals" pill is expanded in the mockup's static export** — Life Events/What-If/Best Dates & Muhurta/Decisions are shown only as unexpanded nav pills, exactly the same one-sub-tab-captured pattern as Phase 9's Life Areas Overview. Unlike Phase 9 (where the other 4 sub-tabs were already separate, reusable components), Plan's Classic implementation had the Life Events/What-If/Muhurta/Decisions bodies inlined directly in `DashboardPlanTab`'s own render (~250 lines of stateful JSX, not standalone components) — matching the same reuse bar as Phase 9 required extracting them first. Did so as a mechanical, behavior-preserving refactor: `PlanEventsPanel`, `PlanWhatIfPanel`, `PlanMuhurtaPanel` (keeps its own local `activityType`/`activityMonth`/`activityTimingResult`/`muhurtaPresetDate` state, self-contained), and `PlanDecisionsPanel` are now exported from `dashboard-plan-tab.tsx`; Classic's own render calls these instead of inlining the JSX (verified identical output — `tsc`/`eslint` clean before and after, zero prop/behavior change), and Nova reuses the exact same four components for its own 4 deferred sub-tabs. `verdictColor`/`verdictKey`/`strengthColor`/`strengthKey` (pure functions, no closure) were hoisted to module scope and exported so `PlanWhatIfPanel` could use them without duplication. `GOAL_EVENT_MAP` and `normalizeGoalType` were additively exported for the same reason Phase 9 reused `GOAL_OPTIONS`.

**Backend event-windows finding that shaped the "Nearest supportive window" card**: `find_event_windows()` (`app/calculations/event_windows.py:109,186,265`) returns windows **sorted by score descending, not chronologically** — confirmed by reading the three `find_*_windows()` functions directly. `EventWindowsPanel` (reused as-is elsewhere, e.g. Phase 9's Predictions sub-tab) just renders whatever order the backend returns, which is fine for a flat list but wrong for "what's the *nearest* one" — so the Nova Goals section re-fetches the same `/api/v1/charts/{id}/event-windows` endpoint itself (one `useQuery` per fixed event type — `MARRIAGE`/`CAREER`/`FINANCE`, `enabled` gated on whether the user has a goal mapped to that event, not a variable number of hook calls) and sorts client-side by `startDate` ascending to find the first window whose `endDate` hasn't passed. No new backend endpoint, no new calculation — same data, different client-side sort.

**Honest limitation found, not invented around**: the mockup's "Nearest supportive window" headline names a specific dasa lord ("Mercury dasa"). The event-window reason codes only explicitly name a planet for 4 of the ~10 possible codes (`mercury_dasha_active`/`sun_dasha_active`/`venus_dasha_active`/`jupiter_dasha_active`) — the "Nth-house-lord" codes (`7th_lord_dasha_active`/`10th_lord_dasha_active`/`2nd_lord_dasha_active`/`11th_lord_dasha_active`) are relative to the chart's own lagna, and the actual resolved planet is computed server-side (`_seventh_lord()`/`_tenth_lord()` etc. in `event_windows.py`) but **never serialized** in the API response — only the reason-code string is. Rather than guess which planet fired the generic case, the headline only names a lord when one of the 4 explicit-planet codes is present (reused via `tPlanetLord()`, the same i18n lookup `dashboard-life-areas-tab-nova.tsx` already uses for dasha-lord names); otherwise it shows just the month/year with no fabricated planet name.

**"Remind me →" (per-window reminder link) is a genuine stub, confirmed by grep, not built**: searched the whole `web/`/`app/` tree for any per-event or per-date reminder mechanism. The only existing "reminder" feature (`dashboard-personal-overview.tsx`'s "Save reminder" button, `app/api/notification_preferences.py`) toggles a single recurring daily morning-notification preference — it has no concept of "remind me about this specific multi-year-out date range." Fabricating a fire-and-forget button with no backing action would be the same class of gap as Phase 1's "Add to my day"/Phase 4's "Plan together" — omitted, not stubbed with a dead click.

| Mockup section (plan) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Top toggle ("🎯 Goals & Windows" / "🪐 Transits & Dasha") | **Revised 2026-07-06 (§6.10)**: originally wired to `goToTab("transits")`, navigating to the sibling Transits tab. Once the `transits` mockup screen itself was read in full, its own top nav has no "Transits" pill at all — it highlights "Plan" as active and its header reads "Plan · Transits & Dasha" with a `data-nav="plan"` link back. Re-wired to a lifted `view`/`onViewChange` prop pair that switches this same tab's body in place — see §6.10 | New 2-segment control in the Nova header, both segments real buttons | **Built**, revised |
| Section pills (Goals/Life Events/What-If/Best Dates & Muhurta/Decisions) | Classic's own `PLAN_SUB_TABS` (function-scoped, not exported — recreated as a small local array rather than refactoring Classic's component signature just to share 5 tiny labels) | New Nova-token pill row, same 5 keys | **Built** |
| My goals (chips + add-goal control + "highlighted in Life Areas" note) | `goals`/`GOAL_OPTIONS`/`normalizeGoalType` (existing) | New card; add-goal control stays a bounded `<select>` (goal type is a fixed enum), not the mockup's free-text-styled placeholder input — an honest simplification, not a functional gap, same class as Phase 7/9's label-reuse precedent. "Highlighted in Life Areas" note now a real button (`onGoToLifeAreas`, new `goToTab("life-areas")` callback) | **Built** |
| Nearest supportive window | `/api/v1/charts/{id}/event-windows` (existing endpoint, re-fetched and re-sorted client-side — see finding above) | New hero card for the first goal-mapped event type; dasa-lord naming only when an explicit-planet reason code fired (see honest-limitation note); "prepare, build skills, save" advice line generalised into a small per-event-type (`CAREER`/`MARRIAGE`/`FINANCE`) presentation-only phrase map, not fabricated per-date astrology | **Built** — mockup's exact "~6 years" phrasing replaced with a real computed years/months-until figure |
| Supportive windows list (score, date range, dasa+transit reason bullets, methodology note) | `EventWindowItem`/`REASON_LABELS`/`humaniseReason` (additively exported from `dashboard-event-windows.tsx`, reused instead of re-deriving) | New Nova-token list card, one per goal-mapped event type (same grouping as Classic's existing `seenEvents` loop), sorted chronologically ascending rather than by score (a planning tool should show "what's coming up," not a leaderboard — a deliberate, documented judgment call) | **Built** — "Remind me →" omitted, genuine stub (see finding above) |
| Goals with no mapped event type (education/health/spiritual/other) | Same fallback as Classic (`!mappedEvent` branch) | Reused Classic's exact guide text ("use What-if/Muhurta tabs"), Nova-token card | **Built** |
| Life event log (icon+title+date+description+bhukti tag, expand for mahadasha/antardasha, "+ Log event" form) | Same `/api/v1/charts/{id}/life-event-log` GET/POST Classic's `DashboardLifeEventLog` already calls | `NovaLifeEventLogCard` (`dashboard-plan-life-event-log-nova.tsx`) — fresh Nova-token rebuild, not a gap-fix: Classic's file reads the same `W` token palette (`--panel-earth-dark`/`--panel-hover`/`--chart-cell-default`/etc.) the 2026-07-06 browser QA round found unsafe to redirect at shared `.cd-shell` scope (conflicting roles across 50+ components) — same class of decision as Phase 2/3's `cal-panch`/`cal-monthly` sub-widget rebuilds. `EVENT_TYPES`/`EVENT_ICON`/`eventLabel`/`LifeEventLogItem`/`EventCorrelation` additively exported from the Classic file and reused verbatim (same data, same API calls) | **Built** — mockup's inline "· Venus bhukti" tag on the collapsed row reproduced directly (Classic only showed this after expanding); mahadasha/antardasha chips still gated behind the expand, matching Classic |
| Sub-tabs: Life Events / What-If / Best Dates & Muhurta / Decisions | `PlanEventsPanel`/`PlanWhatIfPanel`/`PlanMuhurtaPanel`/`PlanDecisionsPanel` (all existing Classic logic, extracted this phase — see above) | **Re-skinned 2026-07-06** (see new §6.9.1 below) — Life Events reused verbatim (already Nova-safe); What-If/Best Dates & Muhurta/Decisions got fresh Nova-token rebuilds | **Built** |
| Footer | Already fixed globally | N/A | Done |

### 6.9.1 Plan's 4 deferred sub-tab panels, re-skinned (2026-07-06)

*Follow-up pass, done immediately after Life Areas' equivalent 4 panels (§6.8.1), same session. None of these 4 has a mockup to build against (same finding as §6.9's own note) — extrapolation per §3.1's policy. Read all 4 Classic panel files end to end plus the heavier sub-components 3 of them embed (`dashboard-life-events.tsx` 184 lines, `dashboard-decision-panel.tsx` 414 lines, `dashboard-muhurta-picker.tsx` 241 lines, `dashboard-muhurtham-naal.tsx` 350 lines) and grepped every `var(--...)` reference in each before writing any code — this pass's scope turned out considerably larger than Life Areas' equivalent, since 3 of the 4 sub-tabs each wrap one or two additional heavyweight Classic components, not just one self-contained panel.*

**Life Events — a real "free reuse," not assumed.** `PlanEventsPanel` (the thin wrapper in `dashboard-plan-tab.tsx`) uses only `Surface` (already Nova-safe) and `DashboardLifeEvents`. Grepped `dashboard-life-events.tsx` for `var(--...)` and found **zero** matches — the whole component styles via CSS classes (`.life-event-*`/`.life-events-*` in `globals.css`), and reading those classes end to end shows they were written dark-theme-first already: `var(--color-text, #e5e7eb)`/`var(--color-muted, #94a3b8)` fallbacks, translucent `rgba(255,255,255,0.0x)` card overlays, and bright saturated event-type accent colors (`#60a5fa`/`#f472b6`/`#a3e635`/`#fb923c`/`#34d399`) chosen for a dark background, not Classic's warm-cream palette. No `.cd-shell` override needed, no code changed — this is a genuine zero-touch reuse, same class as Phase 2's `DayTimeline` or Life Areas' `LifeAreaCard` (§6.8.1), just discovered by grepping rather than assumed from the file's age or its siblings' token-safety.

**What-If (`PlanWhatIfPanel` in `dashboard-plan-tab.tsx`) — a rebuild.** Its own `W` token map reads the same reverted 5-token set (`--panel-earth-dark`/`-earth`/`-tan`/`-tan-light`/`-hover`) as every other deferred panel across this session's two re-skin passes, plus several more Classic-only tokens (`--color-earth-mid`, `--cl-sage-20`, `--color-warm-cream`, `--cl-brand-ring-md`, `--cl-brand-25`) with no Nova override. `dashboard-plan-whatif-nova.tsx` (`NovaPlanWhatIfPanel`) rebuilds the JSX/styling fresh, reusing `bandTone`/`bandPhrase` (Nova-safe after §6.8.1's central CSS fix this same session) and the Classic file's own `verdictKey`/`strengthKey` (pure i18n-key lookups, no color, safe to reuse as-is) — but not `verdictColor`/`strengthColor`, since those resolve to Classic-only `--panel-brand`/`--planet-saturn` for two of three bands; replaced with a local Nova-token equivalent using the same high/mid/low semantic triplet every panel in both passes uses. State stays exactly as lifted in `dashboard-workspace.tsx` (a controlled component, unchanged prop shape).

**Best Dates & Muhurta (`PlanMuhurtaPanel`) — the largest rebuild of the pass, three files deep.** `PlanMuhurtaPanel`'s own "Step 1" quick month-scan JSX reads the same reverted token set. It also wraps two heavier Classic components, both confirmed unsafe by the same check: `DashboardMuhurtaPicker` (Step 2's hour-level search) and `DashboardMuhurthamNaal` (the published 2027 almanac wedding dates), each with the identical reverted `W` map plus, in `DashboardMuhurthamNaal`'s case, two literal hardcoded hex fallbacks (`#EEF5E8` good-bg, `#F9EDEA` avoid-bg) with no CSS variable at all. Built 3 new files: `dashboard-plan-muhurta-picker-nova.tsx` (`NovaMuhurtaPicker`) and `dashboard-plan-muhurtham-naal-nova.tsx` (`NovaMuhurthamNaal`) as direct rebuilds (same data/API calls, same filters, same scroll-into-view prefill sync), and `dashboard-plan-muhurta-nova.tsx` (`NovaPlanMuhurtaPanel`) as the wrapper — same overview card, Step 1 scan (activity/month picker, `find_best_dates` API call, click-to-prefill), and the two Nova sub-components in place of their Classic originals.

**Decisions (`PlanDecisionsPanel` wrapping `DecisionPanel` in `dashboard-decision-panel.tsx`) — a rebuild with one literal-hex finding.** Same reverted `W` token set, plus a literal hardcoded `#F9ECE7` (cream) reused repeatedly for risk/caution boxes — no CSS variable at all, a different instance of the same bug class §6.8.1's Full-report pass fixed (`#7a3412`). `dashboard-plan-decisions-nova.tsx` (`NovaPlanDecisionsPanel`) merges the thin `PlanDecisionsPanel` wrapper (the "when to use which tool" intro card) and `DecisionPanel`'s own form/result JSX into one file, since the wrapper had no content of its own worth splitting out — reuses the 7 `SCENARIO_GROUPS` (pure data) and the `/api/v1/decisions/brief` call unchanged; only the color mapping (`verdictColor`) and all inline styling are rebuilt with Nova's high/mid/low triplet.

Verified: `pnpm --filter jothidam-ai-web exec tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 6 new/touched files (`dashboard-plan-decisions-nova.tsx`, `dashboard-plan-whatif-nova.tsx`, `dashboard-plan-muhurta-picker-nova.tsx`, `dashboard-plan-muhurtham-naal-nova.tsx`, `dashboard-plan-muhurta-nova.tsx`, `dashboard-plan-tab-nova.tsx`); `dashboard-plan-tab.tsx` itself was not touched this pass (only imported from — `verdictKey`/`strengthKey`/`PlanEventsPanel` were already additively exported since Phase 10). No backend changes. Did not drive a real authenticated browser session — same handoff as every prior phase; user sign-off still needed.

### 6.10 `transits` folded into Plan's "Transits & Dasha" toggle — not a separate screen

*Populated 2026-07-06. Re-grepped `data-screen=` first (file unchanged at 14 blocks, `transits` still lines 2421-2641) and read it in full before writing any code, per the working style. §3.2's original guess (`transits` maps to the existing `dashboard-transits-tab.tsx`, a standalone Explore-umbrella tab) turned out structurally wrong once the mockup was actually read: the `transits` screen's own top nav bar has **no "Transits" pill at all** — it lists Today/Calendar/Life Areas/Plan/Family/Explore/Tools/Journal with **"Plan" highlighted as the active tab**, and its header reads `Plan · Transits & Dasha` with a `data-nav="plan"` link back. The `plan` screen's own header already has the exact 2-segment "🎯 Goals & Windows / 🪐 Transits & Dasha" toggle Phase 10 built (§6.9) — with the right segment's `data-nav="transits"` — and, critically, the `plan` screen shows its own 5 section pills (Goals/Life Events/What-If/Best Dates & Muhurta/Decisions) while the `transits` screen shows **none** of them. Read together, this is unambiguous: the mockup treats "Transits & Dasha" as a second top-level view *inside* the Plan tab, not a sibling destination — Phase 10's toggle was built with the right shape but the wrong behavior (it navigated away via `goToTab("transits")` instead of switching a view in place).

**User correction, 2026-07-06**: mid-sequencing (life-areas → plan → transits → journal), the user flagged this directly — "remove transit from nav links because as per new design transit locates inside plan subtab" — matching exactly what reading the mockup in full confirmed. Scope resolved without needing to pause and ask: **Nova only**, following the initiative's own unbroken precedent (Nova additive, Classic never touched unless a Nova change requires it). Classic's separate `dashboard-transits-tab.tsx` tab, its Explore-hub "Transits & Dasha" card (`dashboard-explore-tab.tsx`), and the `activeTab === "transits"` render branch in `dashboard-workspace.tsx` are all **untouched** — Classic users lose nothing. Only Nova's own reachability changed: the "Transits" pill is now filtered out of the shared `cd-tabnav` specifically when `uiVariant === "nova"` (`dashboard-hero.tsx`'s `tabs` useMemo now takes a `uiVariant` prop), and nothing in any Nova component calls `goToTab("transits")` anymore.

**Mechanism**: a `planView` state (`"goals" | "transits"`) is lifted into `dashboard-workspace.tsx` (next to the existing `lifeAreasViewId`/`transitViewId` cross-tab view-selector state) rather than kept as local state inside `DashboardPlanTabNova`, specifically so Life Areas Nova's existing "See the transits behind them →" link (`onGoToTransits`, §6.8) can jump straight into the Transits view from outside the Plan tab — it now does `setPlanView("transits")` then `goToTab("plan")`, instead of navigating to a tab that no longer has a nav entry point under Nova. `DashboardPlanTabNova` takes `view`/`onViewChange` and renders either its existing Goals content (sub-nav pills + the 4 deferred sub-tabs, unchanged from Phase 10) or the new `NovaTransitsView` component, never both.

**Not a gap-fix, a fresh Nova rebuild** — same class of decision as Phase 2/3's `cal-panch`/`cal-monthly` sub-widget rebuilds and this same phase's own `NovaLifeEventLogCard`: `dashboard-transits-tab.tsx`'s `W`/`PLANET_COLORS` token palettes (`--panel-earth-dark`, `--planet-sun`, `--chart-cell-default`, `--transit-card-bg`, etc.) and `dashboard-dasha.tsx`'s `DashaTimeline`/`DASHA_COLORS` (`--panel-brand`, `--planet-saturn`, `--chart-d9-active`, etc.) were all confirmed via grep to be defined at bare `:root` in `globals.css` with zero `[data-ui="nova"]` override — the same class of token the 2026-07-06 browser QA round found unsafe to redirect at shared scope. Built `web/components/dashboard-plan-transits-nova.tsx` (`NovaTransitsView`) fresh instead: same data and derivation logic as Classic (`hasTransitSnapshot`/`flaggedCount`/`activeSaniCycles`/`dashaSupportScore`, the same `dashaElapsedPct`/`dashaStatus` math Phase 5's family-member Dasa card already established), new Nova-token visuals. Per-planet colors were not invented — every one of the mockup's own planet-card colors traces to an existing Nova token exactly (Sun/Jupiter `#e7c87e` → `--color-accent-strong`, Moon/Venus `#c9a9dd` → `--color-accent-secondary`, Mars `#e08a72` → `--color-low`, Mercury `#8fbf9a` → `--color-high`, Saturn `#7fa3c9` → the existing one-off `--planet-other` blue from Phase 3/5, Rahu/Ketu's near-cream → `--color-text`).

| Mockup section (transits) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Header + stat pills (Transits/Flagged/Sani cycles/Dasa support) | Same derivations as `dashboard-transits-tab.tsx` (`hasTransitSnapshot`/`flaggedCount`/`activeSaniCycles`/`dashaSupportScore`) | Inline in `NovaTransitsView`, Nova-token pills | **Built** |
| Person selector (switch to a family member's own transits/dasha) | `family.memberCharts`, same `resolveMemberChart`/`transitViewId` mechanism the pre-existing standalone tab already used (kept, now shared with the Nova view rather than retired) | Nova-token pill row | **Built** |
| Planetary positions today (rasi, house-from-Moon, natal strength bar + breakdown chips, Retro/Combust/Gandanta/Sandhi flags, interpretation line) | `personalTransit.transits`, `personalChart.planets[].strengthScore/.strengthBreakdown` (same fields, same data) | Fresh Nova-token grid — see per-planet color mapping above | **Built** |
| Sani cycle status (from Moon / from Lagna) | `personalSani.moonBasedCycle`/`.lagnaBasedCycle`/`.confirmationSentence` (same fields) | Fresh Nova-token 2-card grid | **Built** |
| Dasa · Bhukti · Antaram (current triplet, elapsed %, action/caution advice, bhukti/antaram nesting, upcoming mahadasha list, expandable full life-story) | `personalDasha`/`personalDashaMaha`/`personalDashaAntar`/`dashaStory` (same fields); elapsed-% math and upcoming-mahadasha-list pattern mirror Phase 5's family-member Dasa card rather than reinventing Classic's proportional lifetime bar (`DashaTimeline`) — a deliberate substitution, not a scope cut, since `DashaTimeline`'s own `DASHA_COLORS` map is Classic-token-unsafe and Phase 5's simpler current+upcoming-list pattern is already an established, signed-off Nova precedent for the same data | `NovaProgressBar` (existing Nova primitive) for elapsed %; new upcoming-mahadasha list and expandable life-story section, Nova-token styled | **Built** — mockup's exact proportional full-lifetime segmented bar not reproduced pixel-for-pixel; the same information (current period, elapsed %, what's next, the full life-story on request) is present |
| Journal patterns teaser (entries progress, "Start journalling →") | `journalCorrelations` (same field) | Nova-token card; "Start journalling →" wired to a new `onGoToJournal` callback (`goToTab("journal")`) | **Built** |
| Varshaphala (annual chart) — collapsed teaser | `onLoadVarshaphala`/`varshaphalaData` (same callback/data) | Fresh Nova-token collapsed card matching the mockup's own capture exactly (it never shows the expanded state either) — expanding reveals the existing `VarshaphalaPanel` reused verbatim inside a Nova-token wrapper, Classic-styled internally (same "deferred, Classic-styled, functional" precedent as Plan's own 4 deferred sub-tabs) | **Built**, expanded content deliberately deferred |
| Footer | Already fixed globally | N/A | Done |

### 6.11 Phase 11 gap & mapping table (`journal` — Journal tab)

*Populated 2026-07-06. Re-grepped `data-screen=` first (file unchanged at 14 blocks, `journal` still lines 2641-2777, the last block — confirms the file hasn't grown since the last sighting) and read it in full, then read `dashboard-journal-tab.tsx` (699 lines), `useJournalData.ts`, and `dashboard-shadow-prompts.tsx` end to end before assuming anything about the mapping, per the explicit instruction that §4.3's grouping guesses have already been wrong twice.*

**Not a "detail drill-down" — no shared-shell question applies**, same reasoning as Phases 9/10: `journal` is a full top-level tab screen with its own 3-way sub-nav (Write/Entries/Reflections, `JOURNAL_SUB_TABS` in Classic), not a single-entity page reached by browsing. Only `novaDetailCardStyle` (the genuinely generic card container) is reused, nothing structural from `dashboard-explore-detail-nova.tsx`.

**Only the "Write" pill is expanded in the mockup's static export** — same one-sub-tab-captured pattern as every prior "Explore umbrella" phase (Life Areas' Overview, Plan's Goals). Entries/Reflections show only as unexpanded nav pills.

**Unlike Phase 9/10, the deferred sub-tabs could *not* just be reused Classic-styled verbatim — this needed a full rebuild of all three sub-tabs, not just Write.** Checked `dashboard-journal-tab.tsx`'s own `W` token map for Nova-safety before deciding scope (the same check every phase runs before reusing a component): its `ink`/`inkMid`/`border`/`borderLt`/`surfaceMd` all read `--panel-earth-dark`/`--panel-earth`/`--panel-tan`/`--panel-tan-light`/`--panel-hover` — the exact 5 custom properties the 2026-07-06 browser QA round (Progress Log, `cal-monthly`/selected-pill entry) found **reverted, not redirected**, specifically because redirecting them broke selected-pill text elsewhere. Meanwhile `W.surface`/`W.card` (`--panel-cream`/`--chart-cell-default`) *are* redirected to Nova's dark surface tokens (for unrelated reasons, from earlier gap-fixes). The result is a genuinely broken combination, not just an unstyled-but-legible "Classic island" like Phase 9/10's deferred panels: entry text (`color: var(--panel-earth)`, Classic's dark-brown `#3D352B`) would render on top of an already-Nova-dark card background (`var(--chart-cell-default)` → `var(--color-surface)`, `#2c2338`) — dark-brown-on-near-black, the same low-contrast "invisible text" defect class as the Life Areas selected-pill bug, not a merely-unpolished reuse. Verified this concretely (not just from the variable names): `#3D352B` on `#2c2338` fails contrast, unlike Phase 9/10's deferred panels which keep *both* their background and text on the unconverted Classic side and so stay internally consistent (light card, dark text) even though they look out of place. Given that, deferring Entries/Reflections by reusing Classic's JSX verbatim would ship a real regression, not an acceptable "not yet re-skinned" gap — so all three sub-tabs are rebuilt fresh with Nova tokens this phase, extrapolating Nova's established list/card/chip patterns per §3.1's policy for content the mockup itself never expands.

**`dashboard-shadow-prompts.tsx` (backs Reflections in BALANCED/TRADITIONAL mode) has the same token-safety problem, plus an unrelated real bug found while reading it end to end as instructed**: every Tamil string in the file is mangled to literal `?????` characters (confirmed via `git log -p` — introduced in an old encoding mishap, commit `e8b7ebd9`, still live today in Classic). This is a live, pre-existing bug affecting Classic's own Reflections tab today in Tamil mode, not something introduced by this phase. Fixed directly (same class of decision as Phase 8's `YOGA_DISPLAY` Tamil-placeholder fix — found while reading the file for Nova-compat reasons, benefits Classic too, not scoped to Nova): replaced the corrupted ternaries with `t()` calls, reusing the two real existing keys already in `i18n.ts` (`shadow_prompts_title`/`shadow_prompts_cta`, added for a past P3-A pass but never wired into this component) and adding 3 new keys for the remaining strings that had no key yet (`shadow_prompts_desc`, `shadow_prompts_reflect_on`, `shadow_prompts_empty`) — plain UI-chrome translation, not astrological content. Frontend-only, `dashboard-shadow-prompts.tsx` itself is otherwise untouched (still Classic-styled, still reused as-is by Classic's own Reflections sub-tab) — only its text is corrected.

**Real, already-computed data found for the mockup's dasha-context line** ("Moon dasa · Jupiter antaram" next to "New entry · [date]"): `ChartSummaryData.currentMahadasha`/`.currentAntardasha` (`personal.chartSummary`, already fetched workspace-wide, unrelated to the Today tab's own member-switcher — Journal has never had one, matching Classic's `chartId={personal.chartId}`, so `personal.chartSummary` is used directly rather than the member-switch-aware `personalChartSummary` variable other Nova tabs share). These are raw planet-lord codes (`_chart_summary.py`: `current_mahadasha=timeline.current_mahadasha.lord`), so rendered through `tPlanetLord()` — the same function every other Nova screen already uses for lord names — rather than Classic's own `dashboard-personal-tab.tsx:270-273`, which concatenates the raw code directly with no translation (a small pre-existing Classic bug, out of scope to fix here, not repeated in the new code). Used the app's own real 2-tier convention (`dasha_word`="Dasa", `bhukti_word`="Bhukti" for the antardasha level) rather than the mockup's own "antaram" label for that same tier — the codebase already reserves "antaram" for the 3rd tier (pratyantardasha, see `dashboard-family-member-nova.tsx`/`dashboard-plan-transits-nova.tsx`'s 3-part dasa/bhukti/antaram strings) and using it for the 2nd tier here would contradict that established, already-shipped terminology — not a fabrication, a consistency choice.

**"↻ New prompts" (refresh icon next to "Need a starting point?") is a genuine stub, confirmed by reading `build_journal_prompts()` directly, not built**: the backend's prompt templates (`_PROMPT_TEMPLATES`/`_DEFAULT_PROMPTS`, `app/services/journal_service.py`) are fixed 3-item tuples keyed only by `(life_area, score_bucket)` — fully deterministic, no randomization or larger pool to reshuffle from. Re-calling the same endpoint with the same chart/date/life-area/score would return the *identical* 3 prompts every time, so a "shuffle" control would be a visible no-op — same class of gap as Phase 1's "Add to my day"/Phase 10's "Remind me →". The mockup's own real lever for a different prompt set already exists and is kept: changing the "Life area" selector genuinely changes `effective_area` and returns a different real 3-prompt set (Classic's existing `useEffect` refetch behavior, reused as-is). The "Use →" click-to-insert-into-textarea interaction (real, already in Classic) is kept.

**"Pattern insights" card reuses the exact same `journalCorrelations` (`JournalCorrelationData`) rendering already built for the Transits view's "Journal patterns" card (§6.10)** — `hasSufficientData` ? real correlation sentences : progress bar toward `minimumEntriesRequired`. The mockup's own inline link ("...line up with *transits and dasa periods*") is wired to a new `onGoToTransits` prop using the exact same `setPlanView("transits"); goToTab("plan")` mechanism Life Areas' equivalent link already uses (§6.8) — confirmed by reading `dashboard-workspace.tsx`'s existing wiring rather than guessing, so it lands on the right view instead of a bare tab switch.

**"Upcoming events" card is a 1:1 re-skin of an already-fully-built Classic feature** — `dashboard-journal-tab.tsx`'s "context events" section (`ContextData`/`ContextEvent`, `handleAddContextEvent`/`handleRemoveContextEvent`, the `CONTEXT_EVENT_TYPES`/`CTX_TYPE_KEY` select) already does exactly what the mockup shows (register an event type/date/note, see the active list, remove one) — same data, same API calls (`/api/v1/context`), reimplemented with Nova tokens rather than gap-fixed (per the token-safety finding above), with `CONTEXT_EVENT_TYPES`/`CTX_TYPE_KEY` additively exported and reused rather than redeclared.

**"Why journal here?" nudge card** — static prose, no backing data of any kind needed; reproduced as inline bilingual text (Nova-only net-new copy, matching the established convention of inline `lang === "ta" ? ... : ...` ternaries for screen-specific strings not present in Classic, e.g. `dashboard-plan-transits-nova.tsx`).

**Streak chip** — reuses `useStreak()` directly (same per-component hook-call precedent as `dashboard-today-tab-nova.tsx`, not lifted to workspace) and the identical pill styling Today Nova already established (`✦ {days}-day check-in streak`, gated on `days > 1`). The mockup's appended "— write your first note today" is a real, derived condition (no entry in `journalEntries` with `entryDate === todayIso()`), not fabricated — shown only when true; the plain streak text (no suffix) covers the "already wrote today" case, which the mockup itself (captured at 0 entries) never shows.

| Mockup section (journal) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Header (kicker/headline/subcopy, Write/Entries/Reflections segmented pill w/ Entries count, streak chip) | `journal_tab_desc` (existing), `journalTotal`, `JOURNAL_SUB_TABS`-equivalent 3-way state (existing concept, same 3 keys), `useStreak()` (existing, same precedent as Today Nova) | `dashboard-journal-tab-nova.tsx` header block, Nova-token styling; streak "write your first note today" suffix is a new, real, derived (not fabricated) condition | **Built** |
| New entry card (date/life-area fields, dasha-context line, prompts, textarea, Save, privacy note) | `LIFE_AREAS`/`AREA_KEY` (additively exported), `handleSave` logic (reimplemented, same API call), `/api/v1/journal/prompts` (existing, reused as-is), `personal.chartSummary.currentMahadasha/.currentAntardasha` (existing, newly surfaced in Nova via `tPlanetLord`) | Fresh Nova-token rebuild (not a gap-fix, see token-safety finding); "↻ New prompts" omitted (genuine stub, see finding above); privacy note is new inline copy | **Built** |
| Pattern insights card | `journalCorrelations` (existing, same rendering as §6.10's Transits "Journal patterns" card) | Reused rendering logic, Nova-token card; new `onGoToTransits` link using the exact Life-Areas-established `setPlanView`+`goToTab` mechanism | **Built** |
| Upcoming events card | `contextData`/`handleAddContextEvent`/`handleRemoveContextEvent`, `CONTEXT_EVENT_TYPES`/`CTX_TYPE_KEY` (additively exported) | Fresh Nova-token rebuild of an already-fully-functional Classic feature, same data/API calls | **Built** |
| "Why journal here?" nudge card | — | New static bilingual prose card, no backing data needed | **Built** |
| Entries sub-tab (list, export, edit, archive) | `journalEntries`/`journalTotal`, `handleArchive`/`startEdit`/`handleSaveEdit`/`exportJournal` (reimplemented, same API calls), `Chip` (already Nova-safe, reused verbatim) | Fresh Nova-token rebuild (not deferred — see token-safety finding, deferring would have shipped illegible text) | **Built**, not in the mockup's own captured state but required for a working tab |
| Reflections sub-tab (Shadow Work prompts) | `/api/v1/journal/prompts?promptType=SHADOW` (existing endpoint, same call `dashboard-shadow-prompts.tsx` already makes) | Fresh Nova-token `NovaShadowPromptsCard` (same token-safety reasoning); BEGINNER-mode notice reused from Classic's exact fallback copy | **Built**, not in the mockup's own captured state but required for a working tab |
| Footer | Already fixed globally | N/A | Done |

**CTA wiring check (per the working style — confirm rather than assume):** Plan's Transits view (`dashboard-plan-transits-nova.tsx`, §6.10) already has a "Journal patterns" teaser card with a "Start journalling →" link wired to `onGoToJournal`, built before this phase's Nova Journal tab existed. Traced its wiring end to end: `onGoToJournal={() => goToTab("journal")}` (`dashboard-workspace.tsx:1907`) simply flips `activeTab` to `"journal"`; the `activeTab === "journal"` render block (now split into `uiVariant` branches by this phase) picks up `uiVariant === "nova"` automatically and renders the new tab. No change was needed there — it now lands correctly on the Nova screen instead of the Classic one, confirming the link was future-proofed correctly when it was first built.

### 6.12 Tools tab (`tools` / `tools-porutham`) — first Nova pass, 2026-07-06

*Populated 2026-07-06. This was the one tab that had never had a `uiVariant` branch at all — confirmed by grep that `dashboard-workspace.tsx`'s `activeTab === "tools"` block rendered identical "cd-tools-v3" markup regardless of the Look toggle. Read the `tools` mockup (lines 1694-1836) and `tools-porutham` mockup (lines 1837-1986) in full, then the real render branch, before writing anything.*

**§3's original mapping guess was wrong, same class of correction as Phase 5's `JadhagamReportPanel` miss:** `tool-card.tsx`/`tools-grid.tsx` are dead code — grepped the whole `web/` tree and found zero importers outside `tools-grid.tsx` itself (which imports `tool-card.tsx`, but nothing imports `tools-grid.tsx`). Classic's real Tools tab is ~200 lines of inline "cd-tools-v3" JSX in `dashboard-workspace.tsx`, built later and never reconciled with these two orphaned files. Likewise, `friendship-result-card.tsx` (§3's guess for `tools-porutham`) turned out to belong to `web/app/tools/friendship-compatibility/FriendshipTool.tsx` — a `PublicNav`/`PublicFooter` **marketing** page, out of scope per §0 — not the dashboard's Porutham panel at all.

**Classic's real tool list (4 tools: Porutham, Generate Chart, Annual Wrapped, Retrospective) is deliberately narrower than the mockup's 6 cards — verified each gap individually rather than assuming the mockup should just be copied:**
- **Jadhagam Generator** = Classic's existing "Generate Chart" tool, direct match (renamed to match the mockup's more evocative label).
- **Birth-time Rectification** — Classic's own code has a comment directly above `TOOL_LIST`: *"Find Birth Time (rectification) removed — results were unreliable"*. This is an explicit, already-made product decision to remove the feature, not an oversight — **not resurrected**, matching CLAUDE.md's guidance not to reintroduce something a past decision deliberately took out.
- **Friendship Compatibility** — grepped `porutham-panel.tsx` and found a real `compatCtx` selector (`GENERAL`/`MARRIAGE`/`FRIENDSHIP`/`BUSINESS`/`FAMILY`) already built into the same Porutham tool, described in Classic's own tool copy ("marriage, friendship, business, or family contexts"). Not a separate card — the mockup's distinct tile and Classic's own single-tool-with-context-selector are two different, equally valid IAs for the same real feature; kept Classic's (avoids a second, redundant entry point into the same code path).
- **Muhurta Finder** — real functionality exists (Plan tab's "Best Dates & Muhurta" sub-tab, `find_best_dates` API), just not exposed from Tools. Added as a cross-nav card (`goToTab("plan")`) rather than either inventing a duplicate calculator or silently omitting a real, working feature the mockup calls out. **Documented simplification**: Plan's own sub-tab selection (`subTab` in `dashboard-plan-tab-nova.tsx`) is local `useState`, not lifted to `dashboard-workspace.tsx` (unlike `planView`), so this lands on Plan's default "Goals" view, one click short of the Muhurta pill itself — lifting that state solely to save one click was judged not worth the scope this pass; flagged here, not silently accepted as "close enough."
- **Panchangam Planner** — real functionality is the Calendar tab itself (any date/place). Cross-nav card (`goToTab("calendar")`), landing exactly on the right screen (no extra click, unlike Muhurta above).
- **Indraiya Rasipalan** — confirmed via `Glob`/`grep` that `web/app/tools/indraiya-rasipalan/page.tsx` is a real, live marketing-site tool. Linked as an external card (`target="_blank"`), same precedent as Phase 6's Explore library tiles.
- **"Recent results"** — grepped the whole `web`/`app` tree for any tool-run-history/comparison-log concept; none exists (Porutham's own `/api/v1/public/compare` result is explicitly "Preview only... not saved to your account"). Genuine stub — kept the section visible with an honest "coming soon" note (same precedent as Phase 4's "Shared muhurtham" card) rather than a fabricated history list.
- **"Share" button** (mockup's breadcrumb, `tools-porutham`) — checked `ShareCardType` (`DAILY_VIBE`/`DASHA_ERA`/`NAKSHATRA` only, all single-saved-chart) — no compatibility/two-chart share card type exists, and Porutham's own preview charts aren't persisted at all. Omitted (PDF download, which is real, was kept).

**Two real pre-existing Tamil-corruption bugs found and fixed while reading `porutham-panel.tsx`/`chart-generate-inline-panel.tsx` end to end (same class as Phase 11's `dashboard-shadow-prompts.tsx` find, same root commit `e8b7ebd`):** three validation/error-message strings (`porutham-panel.tsx`'s "fill all fields"/"PDF download failed", `chart-generate-inline-panel.tsx`'s "fill all required fields") were mangled to literal `??????` sequences — the original correct Tamil text was recovered from `git log -p` (a prior, unrelated commit had fixed the same strings correctly before something re-corrupted them) and restored verbatim, not retranslated. Grepped all of `web/components` afterward for the same 10-`?`-run pattern — clean, no more instances. Frontend-only, benefits Classic too.

**Closed the long-flagged "`dashboard-charts.tsx` chart-grid cosmetic gap from Phase 5" while here** (named in this doc's own top status line as one of two remaining items, alongside Deep Dive completeness) — needed anyway to safely reuse `RasiChart`/`JathagamKattam` in both this phase's Porutham result view and item 1's newly-added Chart Context surface. Root cause confirmed exactly as the status line described: `ExplainPanel`/the grid's occupant chips/`JathagamKattam`'s title all read `--panel-earth`/`--panel-earth-dark`/`--panel-tan`/`--panel-tan-light`/`--panel-cream` directly — the same 5-token set the 2026-07-06 browser-QA round proved unsafe to redirect at shared `.cd-shell` scope (redirecting broke "selected pill" text app-wide). Rather than leave the gap or repeat that mistake, changed `dashboard-charts.tsx` itself to read **new, component-scoped names with a same-value fallback** — `var(--chartgrid-ink, var(--panel-earth))` etc. — so Classic is a complete no-op (the fallback resolves to the exact old literal) and only `dashboard-charts.tsx`'s own usage can ever be affected by a `--chartgrid-*` redirect, no matter what any other component does with the shared `--panel-*` names. Added the 5 new names (`--chartgrid-ink/-ink-strong/-border/-border-light/-surface`) to the existing `[data-ui="nova"] .cd-shell {}` block in `dashboard-nova.css`. This is the properly-scoped fix the Phase 5 progress log entry itself asked for ("new dashboard-charts.tsx-only variable names that don't collide with any other component"), done here rather than deferred further since the context (exact token list, exact regression history) was already fully loaded.

**A new reusable pattern for the 3 deferred tool bodies not shown in the mockup's own captured state (chart generation / annual wrapped / retrospective, 679/364/290 lines each):** all three read the identical reverted `--panel-earth`/`-tan`/`-tan-light`/`-hover`/`-cream` set (confirmed via grep) *plus* `--chart-cell-default`/`--chart-d9-active`/`--chart-amber`/`--planet-saturn`/`--dignity-neecha`/`--ring-brand` — critically, their own **backgrounds** (`--chart-cell-default`, `--panel-cream`) are *already* redirected to Nova's dark surface by the existing gap-fix block, while their **text** (`--panel-earth`) is not — the exact broken "dark card, dark text" combination Journal/Prasna/MorningGuidance/Remedies already found this session, not the safer "whole island stays light" case Phase 9/10's deferred sub-tabs enjoyed. Rebuilding 1,333 combined lines for 3 tools absent from both mockup screens was judged not worth this pass's scope (matching the Phase 9/10 cost-benefit precedent for genuinely large, uncaptured panels) — but reusing them verbatim would ship real regressions, not just unstyled-but-legible islands. **Solution:** a small `ClassicToolIsland` wrapper (`dashboard-tools-tab-nova.tsx`) that locally re-pins every one of those tokens (plus the 5 new `--chartgrid-*` names above, and `--color-faint`, all of which would otherwise still inherit their Nova value from the ambient `.cd-shell` scope even inside the wrapper) back to their exact Classic literal hex values via an inline `style` override — the same CSS-variable-scoping trick `dashboard-personal-tab.tsx` (line ~508) already uses in the opposite direction to render Classic content correctly on Nova's variable set. The result is a deliberately Classic-styled "paper card" floating on the Nova page — internally consistent and fully legible, not a silent regression — used for `ChartGenerateInlinePanel`/`DashboardAnnualWrapped`/`RetrospectivePanel`. `CompatibilityIntelligencePanel` (Porutham's nested MARRIAGE-context upsell report) reads a much larger, disjoint set of `--cl-*` tint tokens on top of all of the above — **omitted from the Nova Porutham view entirely this pass** (not force-wrapped, not rebuilt) rather than extend `ClassicToolIsland`'s override list indefinitely for a secondary, vault-member-only upsell path the mockup doesn't show either.

| Mockup section (tools) | Existing component/hook | Net-new work | Status |
|---|---|---|---|
| Page header + hero Porutham tool | Classic's real `TOOL_LIST[0]` (Porutham) | `dashboard-tools-tab-nova.tsx` header + hero card | **Built** |
| Tools grid (6 mockup cards) | 4 real Classic tools + 2 real cross-tab features, see findings above | Nova-token 6-card grid (`.nova-grid-3`); Rectification omitted (Classic already removed it), Friendship folded into Porutham's context selector | **Built, 6 of 6 mockup cards resolved to a real destination** |
| Recent results | — | Genuine stub, honest "coming soon" note | **Built**, not fabricated |
| Ask strip | Same Ask Vinaadi FAB every Nova screen opens | Reused via `onOpenAskVinaadi` | **Built** |
| Breadcrumb + PDF/Share/Run-another (`tools-porutham`) | `handleDownloadPdf` (existing, reused) | PDF kept; Share omitted (no compatibility share-card type exists); "Run another" = the existing back-to-hub breadcrumb | **Built, PDF only** |
| Verdict hero (gauge, names, narrative) | `DirectPoruthamData` (existing, same fields Classic renders) | `NovaScoreDial`-style gauge sized to `porutham.maxScore`, Nova-token hero card | **Built** |
| 10-porutham table w/ Governs column + CRITICAL tag | `porutham.kutas[]` (existing) | New `KUTA_GOVERNS` presentation-only label map (standard classical-astrology reference terms, matched to the mockup's own column text verbatim — same class of addition as Phase 7's Ganam/Yoni labels); Rajju/Vethai rows detected by name-substring match and shown with a CRITICAL tag | **Built** |
| Cross-checks (Sevvai dosham / Dasa sandhi / Papasamyam) | Sevvai: real, `chartA.doshams`/`chartB.doshams` (`ChartDoshamInsight[]`, already fetched by `/api/v1/public/compare`) — Dasa sandhi/Papasamyam: **already flagged in §6's own Phase-1 gap table as a confirmed stub** ("no existing calculation found anywhere in the codebase") | Sevvai balanced/one-sided/absent derived from both charts' real `SEVVAI_DOSHAM` presence, no new astrology; Dasa sandhi/Papasamyam shown as real "not yet available" rows, not fabricated checkmarks | **Built, 1 of 3 real, 2 confirmed-stub rows honest** |
| "If you go ahead" (muhurta CTA + Ask Vinaadi) | Plan tab's Best Dates & Muhurta (real); Ask Vinaadi FAB (real) | Cross-nav button + Ask button, same wiring as the grid's Muhurta card | **Built** |
| Disclaimer | Same "preview only, not saved" text Classic already shows | Nova-token card | **Built** |
| Side-by-side D1 charts | `RasiChart` (existing, `showExplain={false}`) — not shown in the mockup's own captured `tools-porutham` state | Kept anyway as real confirmation of the underlying chart calculation (Classic has it; dropping a working feature the mockup simply didn't capture would be a regression, not a simplification) | **Built** |
| Footer | Already fixed globally | N/A | Done |

Verified: `tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 6 new/touched files (`dashboard-tools-tab-nova.tsx`, `dashboard-tools-porutham-nova.tsx`, `dashboard-workspace.tsx`, `dashboard-charts.tsx`, `porutham-panel.tsx`, `chart-generate-inline-panel.tsx`). No backend changes. Confirmed `/dashboard` still compiles on the already-running dev servers (307 auth redirect, no 500). Did not drive a real authenticated browser session — same handoff as every prior phase.

---

## 7. Open Questions / Decisions Needed

All items below are **resolved** as of 2026-07-05 except the two flagged still-open:

- [x] **Design access** — resolved via manual download to a gitignored local folder (§2).
- [x] **Which screens are mocked up?** — all 10 main dashboard screens (§3), not a subset as first assumed.
- [x] **Brand direction** — Nova is additive, not a `DESIGN_CONSTITUTION.md` revision (§2 Blockers).
- [x] **`/dashboard/v2` disposition** — retired, superseded by Nova's Today screen (§4.6).
- [x] **Today tab "Ask + decide" section mapping** — resolved 2026-07-05: user chose to add a small additive backend field (`dateResult` on `/api/v1/activity-timing`) rather than downgrade the UI to a single-activity picker or stub the grid out (§6, Progress Log).
- [x] **`.cd-shell` incremental-rollout risk during screen-by-screen rollout** — resolved at the CSS-mechanics level for the outer shell. **Widened 2026-07-05**: found while building the Deep Dive section that `Surface`/`Chip`/`Metric`/`.card`/`.table`/buttons/etc. in `globals.css` are governed by a *second*, separate set of `.cd-shell .X { background: #hex }` rules that hardcode literal Classic colors (not `var()` references) — Nova's variable redefinitions never reached them. Fixed with a matching `[data-ui="nova"] .cd-shell .X { ... var(--color-*) }` override block in `dashboard-nova.css`. Still worth a visual spot-check once a mid-rollout screen mix exists (some screens Nova, some still Classic) to confirm nothing leaks through in practice.
- [x] **Deep Dive completeness** — resolved 2026-07-06 (see Progress Log). All 6 previously-missing pieces (chart validation chip, Chart Context/Guidance/Gochar two-col, Dasa-Bhukti-Antaram strip, `DashboardActivityTimingCard`, `MorningGuidanceCard`, Prasna/Horary trigger) folded into `dashboard-today-tab-nova.tsx`'s Deep Dive section.

---

## 8. Progress Log

- **2026-07-05** — Created this tracking document. Completed a first-pass Functionality Inventory of the dashboard surface (routes, left-rail tabs, ~70 `dashboard-*`/supporting components grouped by tab, 10 data hooks, CSS/token file locations) by reading the repo directly (no design input yet). Attempted to read the Claude Design project via `DesignSync.get_project` — failed, needs `/design-login` which isn't available in this environment; needs the user to either use "Send to Claude Code Web" from the Claude Design canvas, or paste/export the mockup into the repo. Flagged a real conflict: the shipped `DESIGN_CONSTITUTION.md` (Playfair Display / Inter / JetBrains Mono, "Warm Cosmic Minimalism") disagrees with the new direction's stated fonts (Fraunces / Inter / IBM Plex Mono) and aesthetic (Tamil palm-leaf/kolam) — needs a user decision before Step 2 (Design System Extraction) can start for real. Also noted an already in-flight, unshipped `/dashboard/v2` Today-tab redesign preview awaiting sign-off, which likely needs to be reconciled with (not built alongside) this initiative. No code changed yet — audit only.
- **2026-07-05 (later)** — User chose "Send to Claude Code Web" to unblock design access; re-checked `DesignSync` — still not authorized as of this entry, no new design files landed in the repo yet. User then clarified an important scoping point: the Claude Design project only has revamped mockups for **a subset** of dashboard pages, not all of them. Added §3.1 coverage policy: build explicitly-mocked screens to spec; for the rest, extrapolate the same tokens/primitives/visual language for consistency rather than reinventing per-page or leaving them in the old style. This makes token/primitive extraction (Process §5 step 2) a hard prerequisite for touching any undesigned page. Still need: (a) design access to actually land, (b) the specific list of which screens/tabs are covered by mockups today. No code changed — audit/policy only.
- **2026-07-05 (Phase 0 build)** — Design access unblocked: user manually downloaded the Claude Design exports to `D:\sanstro\Astrology App Dashboard Redesign\` (added to `.gitignore` immediately — never commit). Read `Vinaadi Prototype.dc.html` (primary mockup, all 10 screens — coverage turned out complete, not partial as previously assumed) and `Design Tokens.dc.html` in full; `Dashboard Redesign.dc.html` confirmed as historical reference (8 screens verbatim-identical, 2 extra superseded Today variants). User confirmed two decisions: (1) Nova is an additive "Look: Classic/Nova" toggle, not a replacement of Light/Dark/System — Nova is always-dark and disables that sub-toggle while active; (2) the in-flight `/dashboard/v2` Today preview is retired, superseded by Nova's Today screen. Went through Plan Mode (approved plan at the time saved in `C:\Users\senth\.claude\plans\rosy-wishing-pelican.md`) before writing any code, per the working style. Built Phase 0 (foundation): `web/hooks/useUiVariant.ts` (mirrors `useTheme.ts`); FOUC-prevention restore for `data-ui` in `web/app/layout.tsx`; **found during implementation that `.cd-shell` is much bigger than a simple override** — it's ~400 lines across `dashboard.css`+`globals.css`, including a large `!important`-heavy layer of literal `[style*="#hex"]` attribute-selector hacks patching old raw inline styles. Refined the plan's approach: Nova's own `--color-*`/`--radius-*` variable redefinitions under `[data-ui="nova"] .cd-shell` win automatically via CSS specificity against the plain `.cd-shell {}` definitions (no edits needed there) — reusing the entire existing token-driven chrome (topbar, tabnav, chips, buttons) for free. Only the literal inline-style hack block (`globals.css` ~3298-3486) needed an actual edit, since attribute-selector-on-raw-style-text is orthogonal to CSS variables — rescoped every selector there to `html:not([data-ui="nova"])`. Created `web/app/dashboard/dashboard-nova.css` (Nova token values + nav-shell reuse: forcing `.cd-tabnav` visible and `.cd-left-rail` hidden at all viewport widths, confirmed to work via specificity without `!important` since `DashboardLeftRail` is a flex sibling not a wrapper) and imported it in `web/app/dashboard/page.tsx`. Added Cormorant Garamond + Source Serif 4 via `next/font/google`, scoped to `web/app/dashboard/layout.tsx` (not root layout) as `--font-nova-display`/`--font-nova-prose`; confirmed Noto Sans Tamil already loads app-wide, reused as-is. Mapped `--font-display` to Cormorant Garamond but left `--font-body` on the system-ui stack (not Source Serif 4) — the source tokens reserve "Prose" (Source Serif 4) for body-copy/insight text specifically, not all UI chrome; `--font-nova-prose` is applied selectively per-component starting in Phase 1. Scaffolded `web/components/dashboard-ui-nova.tsx` (`NovaScoreDial`, `NovaProgressBar`, `NovaTable`) as a separate file from `dashboard-ui.tsx` to avoid variant-branching the widely-used Classic primitive library. Added the "Look" toggle UI to `dashboard-settings-session-tab.tsx` (new card before "Appearance"; existing Light/Dark/System row now visually disabled with an explanatory note when Nova is active). Updated this doc's §2/§3/§4.3/§4.6/§6/§7 to reflect all of the above. **Next:** dev-server verification of Phase 0 mechanics, then start Phase 1 (Today tab) screen build.
- **2026-07-05 (Phase 1 build)** — Built the Today tab (`data-screen="today"`, all 8 mockup sections). Before writing code, resolved the one flagged open question with the user (§6/§7): the mockup's "Is today right for…?" grid needs a live per-activity status for *today*, but the only backend (`/api/v1/activity-timing`) returns just the top-5 best dates in a month for one activity — it silently computes every day internally but only returns the top 5, so today's own result was already being thrown away. User chose to add a small additive field over downgrading the UI or stubbing it out. Backend: added `date_result`/`dateResult` to `ActivityTimingData` (`app/schemas/daily_guidance.py`), an optional `asOf` query param (`app/api/daily_guidance.py`), and capture-during-the-existing-loop logic in `get_activity_timing` (`app/services/daily_guidance_service.py`) — no new calculation, no astrology logic touched, fully backwards-compatible (omitting `asOf` reproduces prior behavior exactly). Mirrored the type into `packages/shared/src/types/index.ts`. Mapped the mockup's 4 activity cards (Travel/Signing/Buy gold/New job) onto the *existing* allowed activity keys (`travel`/`property`/`money`/`job_change`) rather than inventing new astrological categories.
  Built 4 new frontend files: `dashboard-today-ribbon-nova.tsx` (day timeline, segmented-bar visual built from real Rahu Kalam/Nalla Neram data, range and segment count computed dynamically per day — not hardcoded), `dashboard-today-decide-nova.tsx` (Ask Vinaadi teaser — real quota + real mode-based suggested questions, opens the existing FAB rather than duplicating its chat — plus the 4-activity Decide grid), `dashboard-today-glance-nova.tsx` (Coming up/Week ahead/Family/Dasa chapter/Life areas — all existing data, re-laid-out), and `dashboard-today-tab-nova.tsx` (orchestrator: greeting hero + score dial, Panchangam band, one-focus+remedy, and a collapsed Deep Dive section reusing existing panels verbatim). Exported `downloadJadhagamPdf` from `dashboard-personal-tab.tsx` (was an inline function) so Nova's Deep Dive PDF button reuses it instead of re-implementing.
  **Found and fixed a real Phase 0 coverage gap while wiring the Deep Dive section**: `dashboard-nova.css`'s header comment claimed `.cd-shell`'s `--color-*` variable redefinitions re-theme "cards, tables..." for free, but `globals.css` actually governs `Surface`/`Chip`/`Metric`/`.card`/`.table`/buttons/snapshot-box/alert-banner via a *second*, separate set of `.cd-shell .X { background: #hex }` rules that hardcode literal Classic colors directly rather than reading the variables — meaning any Nova screen reusing these shared primitives (not just this one) would have silently rendered Classic's cream/tan colors. Added a matching `[data-ui="nova"] .cd-shell .X { ...var(--color-*) }` override block to `dashboard-nova.css` (mirrors the same selector list at one higher specificity level) — fixes this for the whole initiative going forward, not just today's build. Also added `.nova-grid-2`/`.nova-grid-3`/`.nova-grid-anticipation` responsive utility classes (the one place Nova needs a `@media` breakpoint, which can't be expressed in the inline-style convention `dashboard-ui-nova.tsx` otherwise uses).
  Wired it into `dashboard-workspace.tsx`: lifted `useUiVariant()` up from `dashboard-settings-session-tab.tsx` into the workspace itself (previously two independent hook instances — toggling in Settings wouldn't have swapped the Today tab tree without a page reload, since nothing threaded the variant into the component that decides which tab tree to render) and now passes `uiVariant`/`onUiVariantChange` down as props. Added a new `activeTab === "personal" && uiVariant === "nova"` render branch (checked before the existing `todayVariant === "v2"` branch — Nova supersedes v2 per §4.6), extended the household-strip gating to also skip when Nova is active (it has its own Family glance card), and lifted the Ask-Vinaadi FAB's `open` state from `dashboard-ask-vinaadi-widget.tsx`'s internal `useState` up into the workspace (now a controlled `open`/`onOpenChange` pair) so the new Nova teaser card can open the same chat panel instead of duplicating it.
  **Scoped Deep Dive deliberately narrower than Classic's full detail** (flagged in §7, not silently dropped): covers planet table, chart explanation, vargas, shadbala, the three alternate dashas (Yogini/Ashtottari/Kalachakra), classical timing (Chara Dasha/Solar Return), nakshatra card, and PDF download — all reused verbatim from `dashboard-personal-tab.tsx`. NOT yet carried into Nova: the two-col Chart Context/Guidance-detail/Gochar surfaces, the Dasa-Bhukti-Antaram strip, `DashboardActivityTimingCard` (single-activity month browser, distinct from the new Decide grid), `MorningGuidanceCard`, the Prasna/Horary trigger, and the chart-validation confidence chip. Also omitted (no backing feature exists, confirmed by grep): the mockup's "Add to my day" button and the remedy's "✓ Done" button; and the "Chandrashtama · in 6 days" forward-countdown chip in the Coming Up card (only "is it active today" exists, not a days-until-next-occurrence forecast).
  Verified: `tsc --noEmit` clean project-wide (strict mode), `eslint` clean on all new/touched files, edited Python backend modules import cleanly, and the already-running dev backend's live OpenAPI schema confirms `dateResult`/`asOf` are live. Both dev servers (web:3000, backend:8000) were already running from a prior session — did not need to start them. **Next: visual sign-off from the user in a real signed-in browser session** (toggle Look → Nova in Settings, check the Today tab), then either follow-up fixes or move to Phase 2 (screen 2, `cal-panch`).
- **2026-07-05 (Phase 2 build)** — User confirmed Phase 1 sign-off, proceeded straight to Phase 2. Read the `cal-panch` mockup in full (lines 320-539 of `Vinaadi Prototype.dc.html`) and then `dashboard-calendar-tab.tsx` end to end (1943 lines — both the daily Panchangam view inside `CalendarTab` and the `MonthlyCalendarView`/`DayDetailDrawer` it also contains).
  **Key finding that shaped the whole approach**: unlike the Today tab (which needed a from-scratch rebuild in Phase 1), Classic's daily Panchangam view turned out to already be content- and layout-identical to the mockup — same header text ("Transits & Events"), same 11-row Five Limbs list in the same order (down to a static, unexplained "5L" tag appearing on every row in *both* the shipped Classic code and the mockup, meaning the mockup was very likely captured from a live render of this exact page), same Hora table, same Chandrashtamam/Nakshatra/Events sections. More importantly, several of its sub-components (`DayTimeline`'s SVG day-arc, `LunarTithiBadge`, the `.cd-detail-spec-row` CSS class backing Five Limbs) were already written using only `var(--color-*)` custom properties that Nova's `[data-ui="nova"] .cd-shell` block already redefines — meaning they re-theme automatically with zero changes, the same mechanism Phase 1 relied on for the base chrome.
  Other sub-components (`AuspiciousSlotGroup`, `GowriNamedSlotPanel`, `HoraRow`, festival tag badges) instead read Classic-only literal-hex custom properties defined in `globals.css` (`--panel-warm-tint`, `--cl-brand-edge`, `--chart-d9-active-bg`, `--cal-*`, `--cl-festival-*` — confirmed via grep that none of these have a `[data-ui="nova"]` override anywhere) — reusing those verbatim would have rendered Classic's light cream/sage colors on top of Nova's dark plum background, a real visual bug of the same class Phase 1 found and fixed for `Surface`/`Chip`/`Metric`. Rather than extend the gap-fix list in `dashboard-nova.css` to cover a dozen more one-off tokens whose exact Classic semantics would need reverse-engineering, rebuilt those specific pieces fresh with Nova's own token set (`--color-high-bg`/`--color-low-bg`/`--color-accent-muted`/etc.), while still importing and reusing the underlying *data and pure logic* unchanged (`gowriPeriodLabel`/`gowriCategoryLabel`/`gowriPurposeLabel` from `lib/gowri.ts`, `timeWindowsOverlap`, `festivalTags`, `festivalIcon`).
  Made 18 additive `export` keyword changes to `dashboard-calendar-tab.tsx` (zero behavior change, same class of edit as Phase 1's `export downloadJadhagamPdf`) to expose the pure derivation helpers (`activeLimb`, `moonRasiFromNakshatra`, `chandrashtamaAffectedNatalRasi`, `rasiName`, `formatHeaderDate`, `getTamilMonthDate`, `formatChandrashtamaWindowSummary`, `parseHmToMinutes`, `timeWindowsOverlap`, `festivalTags`, `festivalIcon`, `RASI_NAMES_EN/TA`) plus three components reused verbatim (`DayTimeline`, `LunarTithiBadge`) and two reused for the deferred Monthly toggle (`MonthlyCalendarView`, `DayDetailDrawer`, plus the `CalendarView` type) — no astrological calculation logic was touched or duplicated, only exposed.
  Built `web/components/dashboard-calendar-tab-nova.tsx` (new file, ~500 lines): page header, the two-column "Day at a glance" mega-card (day arc, auspicious/avoid cards, nakshatra/chandrashtamam/events/significance cards, the 14-slot Gowri Nalla Neram detail grid) and the right-column Five Limbs + Hora Table panel — see §6.1 for the full section-by-section mapping. Kept the location-override feature (📍 "change location" popover using the existing `PlaceCombobox`) for parity with Classic rather than dropping it, since it's a real working feature, not a mockup-only idea. The Monthly toggle is wired to the exact same `useMonthlyPanchangam` hook and reuses Classic's `MonthlyCalendarView`/`DayDetailDrawer` verbatim (Classic-styled, functional) — its own Nova re-skin is Phase 3's job (screen 3, `cal-monthly`), not this one's; nothing regresses in the meantime.
  Wired into `dashboard-workspace.tsx`: added a `DashboardCalendarTabNova` dynamic import and split the existing `activeTab === "calendar"` render block into `uiVariant === "nova"` / `uiVariant !== "nova"` branches (same pattern as the Today tab's Phase 1 branch), so the Look toggle now also swaps the Calendar tab.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings) on the new file and both touched files (`dashboard-calendar-tab.tsx`, `dashboard-workspace.tsx`). Did not drive a real signed-in browser session for this screen — no browser-automation tool is available in this Windows environment (no `chromium-cli`/xvfb, and the dashboard requires an authenticated session this agent doesn't have credentials for), so — same handoff as Phase 1 — **visual sign-off needs to happen in the user's own browser** (dev servers already running on :3000/:8000; toggle Look → Nova, open the Calendar tab). **Next:** user sign-off on `cal-panch`, then Phase 3 (`cal-monthly`).
- **2026-07-05 (Phase 3 pulled forward)** — User checked Phase 2 in the browser and sent a screenshot: the page header/toggle were correctly Nova-dark, but the **Monthly** sub-view (which the user had clicked into) showed the old Classic cream/light calendar grid. Confirmed via follow-up question this was expected/documented (Monthly was deliberately deferred, reusing Classic's `MonthlyCalendarView` verbatim per §6.1) and that the actual Phase 2 deliverable — the **Panchangam** (daily) sub-view — looked correct. User then chose to pull Phase 3 forward immediately rather than leave Monthly Classic-styled even temporarily, so built it in the same session.
  Read the `cal-monthly` mockup in full (lines 540-700 of `Vinaadi Prototype.dc.html`). Unlike `cal-panch`, found that essentially **none** of Classic's `MonthlyCalendarView` was safe to reuse verbatim: its `monthlyTheme` object and every highlight-color path resolve through `--cal-*`/`--panel-cream-light`/etc. custom properties, all confirmed via `globals.css` to be literal hardcoded hex with zero `[data-ui="nova"]` override — the opposite situation from `DayTimeline`/`.cd-detail-spec-row`, which Phase 2 found were already Nova-safe. So this phase is a genuine from-scratch rebuild of the presentation layer (matching Phase 1's Today-tab pattern), not a light re-skin.
  Made 8 more additive `export` keyword changes to `dashboard-calendar-tab.tsx` (same zero-behavior-change class as Phase 2's 19) to expose `tamilMonthOnly`, `MoonPhaseMark` (already fully `currentColor`-driven, theme-safe with zero changes), `MONTH_LABELS_EN/TA`, `WEEKDAY_LABELS_EN/TA`, `festivalImagePath`, and `VRATHA_FESTIVAL_PATTERN` — all pure data/logic, reused as-is.
  Built `web/components/dashboard-calendar-monthly-nova.tsx` (new file): month nav, the 7-column day-cell grid (recomputing the same `cells`/`monthFestivals`/`vrathaGroups`/`sidebarItems` derivations Classic's `useMemo` blocks compute, off the same `PanchangamMonthlyData` prop — duplicated since Classic doesn't expose them as a standalone hook, but pure JS derivation over already-fetched data, not astrological calculation), the 9-item legend, and the Events/Vratha/Muhurtham sidebar. Introduced one small `NOVA_CAL_HILITE` color map: reused exact Nova semantic tokens where they already existed (Muhurtham→`--color-high`, Chathurthi→`--color-low`, Karinaal→`--color-alert-critical`, Pradosham→`--color-accent` — this one turned out to be the *exact* same hex Nova already uses, `#d4af5f`, since the mockup's own Pradosham legend swatch matches it — and Festival→`--color-accent-strong`, another exact match, `#e7c87e`), and added 3 small new literal one-off colors only where Nova truly has no equivalent semantic slot (Pournami needs a 3rd distinct gold shade since Pradosham/Festival already claim Nova's two existing golds; Amavasai kept `--color-accent-secondary` reused rather than a new purple; Sashti needed a genuinely new blue) — matched to the mockup's own stated legend values, not invented from scratch.
  Also rebuilt the day-click preview drawer: added `DayDetailDrawerNova` directly in `dashboard-calendar-tab-nova.tsx` (co-located with the Nova sub-components it reuses — `NovaAuspiciousCard`, `NovaAvoidRow`, `NovaFestivalRow`, `LunarTithiBadge`). Found `DrawerPanel` (`drawer-panel.tsx`) already defaults to a dark theme app-wide (`theme="dark"` is the default; only Classic's `DayDetailDrawer` explicitly opts into `theme="light"` for its cream aesthetic) — so no new drawer chrome was needed, just a Nova-styled body reusing the same sub-components as the main `cal-panch` screen instead of Classic's non-Nova-safe `AuspiciousSlotGroup`/inline avoid-rows.
  Added a `.nova-cal-monthly-layout` responsive rule to `dashboard-nova.css` (collapses the grid+sidebar 2-column layout to 1-column under 960px — the one `@media` breakpoint this screen needs, same convention as the existing `.nova-grid-2/3` utilities from Phase 1).
  Wired `MonthlyCalendarViewNova` into `dashboard-calendar-tab-nova.tsx` in place of the Classic `MonthlyCalendarView` reuse, and `DayDetailDrawerNova` in place of `DayDetailDrawer` — both drop-in swaps (identical prop signatures), no changes needed in `dashboard-workspace.tsx` beyond what Phase 2 already wired.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings) on all touched/new files, confirmed the `dashboard-calendar-tab.tsx` diff is still purely additive `export` keywords (27 total across Phases 2+3, zero logic changed). Hit `/dashboard` to confirm the dev server compiles the changed route (307 auth redirect, no 500). Same as before, could not drive a real authenticated browser session myself. **Next:** user sign-off on both `cal-panch` and `cal-monthly` together, then continue to the remaining screens (`family`, `family-member`, `explore`, `explore-moolam`, `explore-sevvai`, `tools`, `tools-porutham`).
- **2026-07-05 (footer gap-fix, found before Phase 4)** — User flagged that the dashboard's site footer still renders with Classic's cream/tan colors under Nova, on every screen (not tab-specific). Root cause: same class of bug as the Phase 1 Surface/Chip/Metric gap and Phase 2/3's per-widget rebuilds — `<footer className="cd-footer">` in `dashboard-workspace.tsx` (rendered once at the shell level, inside `.cd-main-content`/`.cd-shell`, unconditional on `activeTab` or `uiVariant`) is styled entirely by `globals.css`'s `.cd-footer*` rules (lines ~4919-4986), which hardcode literal Classic hex (`#EDE5D4` bg, `#D4C8AE` border, `#1A1612`/`#7A6F5E`/`#A89D89` text) with zero `[data-ui="nova"]` override anywhere — confirmed via grep. Fixed the same way as the Phase 1 gap-fix: added a `[data-ui="nova"] .cd-shell .cd-footer*` override block to `dashboard-nova.css` (placed right after the existing `.dasha-bar-bg` gap-fix), remapping to Nova's own tokens (`--color-accent-strong` for the wordmark, `--color-muted`/`--color-faint` for tagline/links/disclaimer/copy, `--font-display` for the two serif elements) plus two literal one-off values (`rgba(212,175,95,.13)` border/divider, `rgba(0,0,0,.22)` background) matched to the mockup's own footer styling verbatim (`Vinaadi Prototype.dc.html` lines 301-315) since Nova has no existing semantic token at that specific low opacity. Content was **not** changed to match the mockup's simpler single-row layout (wordmark + copyright left, 5 legal links right) — the existing footer's tagline, tab-name links, and astrology disclaimer sentence are real content already shipping in Classic (the disclaimer in particular reads as a legal/compliance line), so per the "no functionality regression" principle this was treated as a color-only re-skin of the existing structure, not a content rewrite to match the mockup's copy. CSS-only change, no `.tsx` touched. **Next:** user re-check the footer in the browser, then proceed to Phase 4 (`family` screen) once confirmed.
- **2026-07-06 (Phase 4 build)** — User confirmed the footer fix, Phase 3 sign-off, and the scroll-bounce fix all looked correct. Read the `family` mockup in full (lines 701-965 of `Vinaadi Prototype.dc.html`), then `dashboard-family-tab.tsx` (946 lines), `useFamilyData.ts`, `synastry-matrix.tsx`, and `dashboard-synastry-panel.tsx` end to end before writing anything.
  **Three open questions surfaced during research** (design implied data/interaction that didn't cleanly exist yet) were put to the user before building, per working style:
  1. *Relation filter chips (Elders/Adults/Children)* — no such bucketing existed anywhere. **User chose:** compute real age (found `ChartSummaryData.currentAge` is already computed server-side per member chart — no new age math invented) with child <18 / elder ≥60 thresholds (a specific, documented judgment call, not a discovered convention).
  2. *Family bonds — all-pairs vs owner-only* — the mockup's 4 pairwise cards include 2 non-owner pairs (e.g. Lakshmi↔Bharathan); existing `SynastryMatrix`/`SynastryPanel` only ever compute owner-vs-member. **User chose:** build real all-pairs backend support rather than narrow the UI.
  3. *Member card "Open →"* — mockup implies navigating to the not-yet-built `family-member` screen (Phase 5). **User chose (recommended):** inline expand, reusing Classic's existing `MemberDetailExpanded` pattern (select → show detail below the grid) rather than a dead link or a stub route.
  **Backend (additive, no astrology touched):** found `synastry_service.py`'s `compute_synastry_score()` is already a fully generic (non-marriage) two-chart function — no gendered/Porutham-specific checks — and the codebase already had a precedent for a direct two-chart-ID endpoint bypassing the owner/vault assumption (`compare_charts_direct` / `POST /relationships/compare`, used by the Porutham tool). Mirrored that exact pattern for Synastry: added `DirectSynastryRequest`/`DirectSynastryData`/`DirectSynastryResponse` to `app/schemas/relationships.py`, `compare_synastry_direct()` to `app/services/synastry_service.py` (same ownership-check closure pattern as its Porutham sibling), and `POST /api/v1/relationships/compare-synastry` to `app/api/relationships.py`. Added the typed wrapper `compareSynastry()` + `DirectSynastryData` type to `packages/shared/src/api/relationships.ts` per CLAUDE.md's API-contract policy (this is a brand-new endpoint, so no existing bypass-grandfather applies). Verified via the running backend's live OpenAPI schema that `/api/v1/relationships/compare-synastry` is live.
  **Also found two "already computed, never surfaced" features while reading the backend for other reasons** (same class of discovery as Phase 1's Ask-Vinaadi widget): `FamilyAggregateData.summary` (a full bilingual narrative paragraph from `_family_summary()` in `family_vault_service.py`, e.g. *"The family day is supportive but mixed... support the member who needs a gentler pace"*) and `FamilyMemberTodayScore.highlightEn/highlightTa` (a per-member one-line insight, e.g. *"Steady — move step by step..."*) — both exactly match the mockup's Harmony-hero paragraph and per-member card insight text respectively. Neither needed any backend change, just wiring into the new Nova UI — Classic's own UI simply never rendered them.
  Made 3 additive `export` keyword changes to `dashboard-family-tab.tsx` (`ScoreRing`, `formatRelLabel`, `MemberDetailExpanded` — zero behavior change, same class of edit as prior phases) and confirmed via grep that `MemberDetailExpanded`'s inline styles read 8 Classic-only custom properties (`--panel-warm-tint`, `--chart-d9-active-bg`, `--chart-d1-lagna-bg`, `--cl-rust-edge`, `--cl-rust-30`, `--panel-cream`, `--panel-mid-earth`, `--cl-sage-edge`) defined at bare `:root` in `globals.css` with no Nova override — unlike the `--color-*`/`--radius-*` tokens (which Classic itself scopes to `.cd-shell`, letting Nova's existing `[data-ui="nova"] .cd-shell {}` block re-theme them for free), these are genuinely global tokens also used outside the dashboard (marketing pages), so they can't be redefined at bare `:root` under Nova without leaking there too. Fixed by adding all 8 (plus `--shadow-faint`, a minor warm-brown shadow that would look wrong on a dark background) to the *existing* `[data-ui="nova"] .cd-shell {}` block in `dashboard-nova.css` (not a new gap-fix block — these are variable redefinitions, the same mechanism as the block they live in, just scoped to `.cd-shell` so marketing pages never see the override), each pointed at Nova's own already-existing high/mid/low semantic triplet (e.g. `--chart-d9-active-bg: var(--color-high-bg)`) — no new literal colors invented, and `MemberDetailExpanded`/`ScoreRing`/`RasiChart`/`NavamsaChart`/`ChartExplanationPanel` (all confirmed via grep to be otherwise 100% `var(--color-*)`-driven) now render correctly under Nova with zero code changes to any of them.
  Built `web/components/dashboard-family-tab-nova.tsx` (new file): page header (kicker/headline/date subcopy reusing Phase 2/3's exported `formatHeaderDate`/`getTamilMonthDate` — pure date math, no Panchangam fetch needed — plus the vault switcher/refresh button, preserved from Classic since they're real reachable features, not decorative), the relation filter chips (a real filter — clicking a chip filters the Members grid, not just decorative counts), the Harmony hero (score dial via `NovaScoreDial` + the previously-unsurfaced `summary` narrative), `NovaFamilyRhythmCard` (day-arc sparkline with two real data-driven segment bars for best/avoid windows, plus needs-care/brightest lists derived from `FamilyMemberTodayScore` using the same score<45 threshold `family_vault_service.py` already uses for `low_score_count` — no new astrology), `NovaFamilyMemberCard` grid (reuses exported `ScoreRing` for score-band-colored dials, `formatRelLabel`, and the newly-surfaced `highlightEn/highlightTa` insight text), `NovaFamilyBondsCard` (lazy-loaded via a "Load bonds →" button — same UX precedent as `SynastryMatrix`'s own "Load scores" button, avoiding an eager fetch storm over C(n,2) pairs on mount), and a "Shared muhurtham" stub card with an explicit "coming soon" note (no cross-member shared-events feed exists anywhere — not invented). Omitted, matching established precedent of not shipping decorative no-ops: the mockup's "Plan together" button (no backing action, same class of gap as Phase 1's "Add to my day"), the "Show all... life-areas/dasa/remedies side by side" expandable row (no multi-member comparison table exists), and the footer note's "Notify me when a member needs care" toggle (no such preference exists — only unrelated personal journal reminders).
  Wired into `dashboard-workspace.tsx`: added a `DashboardFamilyTabNova` dynamic import and split the existing `activeTab === "family"` render block into `uiVariant === "nova"` / `uiVariant !== "nova"` branches (same pattern as every prior phase), passing the exact same prop shape Classic already receives (no new plumbing needed beyond the tab component itself).
  Verified: Python backend modules (`app.api.relationships`, `app.services.synastry_service`, `app.schemas.relationships`) import cleanly; `tsc --noEmit` clean across both `web/` and `packages/shared/`; `eslint` clean (0 errors/warnings, including two `react-hooks/exhaustive-deps` warnings caught and fixed by memoizing off the source objects instead of derived-every-render arrays) on all new/touched files. Confirmed on the running dev servers that `/api/v1/relationships/compare-synastry` is live in the backend's OpenAPI schema and that `/dashboard` still compiles (307 auth redirect, no 500). Did not drive a real authenticated browser session — same handoff as every prior phase. **Next:** user sign-off on the `family` screen in the browser, then Phase 5 (`family-member`).
- **2026-07-06 (Phase 5 build)** — Asked the user whether Phase 4 had been checked in the browser yet (per the handoff above); user hadn't checked yet and asked to proceed with Phase 5 regardless, taking care of the Phase 4 check separately. Read the `family-member` mockup in full (lines 966-1173 of `Vinaadi Prototype.dc.html`).
  **Navigation-pattern question put to the user before building** (flagged explicitly in this doc as needing a decision, not left to default judgment): the mockup's breadcrumb + prev/next arrows clearly imply a dedicated screen, but Phase 4 shipped an inline-expand as an interim stand-in. **User chose "keep both"** rather than the recommended full replacement — see §6.4 for the resulting interaction design (inline expand stays for a quick glance; a new "View full profile →" button opens the dedicated screen; both read from the same `MemberChart` data, no duplicated fetches).
  Re-read `dashboard-jadhagam-report-panel.tsx`/`dashboard-vargas-panel.tsx` (the plan's original §3 guess for this screen) and found neither matches — the mockup has no age-wise focus timeline, practical guidance list, or optional-remedies section (that's `JadhagamReportPanel`'s content, a different, heavier lifetime-report feature entirely), and no varga charts beyond D1/D9 (so `VargasPanel`/`ShadbalaPanel` aren't needed either). Cross-checked `MemberDetailExpanded` (Phase 4) and Today Nova's Deep Dive section instead and confirmed the real content — member header, timing strip, D1/D9 charts, guidance narrative, Dasa card, Planets table, Yogas & Doshams, family relationships — is a re-skin of pieces already built, not a rewrite, once traced to the right existing data (`chart.yogas`/`.doshams`, `dasha.timeline`'s full mahadasha sequence, `chart.birthProfile`) — see §6.4 for the full mapping.
  **Found a real pre-existing gap while cross-checking `RasiChart`/`NavamsaChart` for Nova-safety** (same check every prior phase has done for its reused sub-components): Phase 4's progress log asserted these were "100% `var(--color-*)`-driven," but grepping the actual file turned up ~18 Classic-only literal-hex custom properties never covered by any `[data-ui="nova"]` override (`--panel-tan`, `--chart-bg`, `--chart-cell-default/selected`, `--chart-d1-*`/`--chart-d9-*`, `--planet-lagna/saturn/nodes/other`). This means Phase 4's inline `MemberDetailExpanded` chart-grid reuse has been showing Classic's tan/cream chart colors on Nova's dark background since it shipped, unflagged. Fixed with the established mechanical-redirect pattern, added to the existing `[data-ui="nova"] .cd-shell {}` block in `dashboard-nova.css` (not a new block) — full mapping and rationale in §6.4. This fixes Phase 4's already-shipped bug and Phase 5's new screen at the same time.
  Exported `YOGA_DISPLAY` (additive, `dashboard-yoga-dosham-panel.tsx`) to get human-readable yoga names for the new compact list without duplicating the map or rebuilding the full (Classic-token, per-item-expand) `YogaDoshamPanel`.
  Built `web/components/dashboard-family-member-nova.tsx` (new file): breadcrumb + prev/next (cycles `familyAggregate.members` order), member header, a 4-card timing strip (member-specific Best-window/Chandrashtamam, shared-Panchangam Rahu-Kalam/Nakshatram), Charts row (`RasiChart`/`NavamsaChart` reused verbatim with `showExplain={false}`), a Guidance card (narrative + remedy from `dailyGuidance`, plus a new "Why this prediction" Dasa/Panchangam/Transit 3-line breakdown assembled from already-computed fields — reuses `sani.confirmationSentence`, the same ready-made sentence Classic's `DashboardDailySnapshot` already surfaces, rather than inventing new caution copy), a Dasa card (current triplet + elapsed % + upcoming-mahadasha list from `dasha.timeline`, default 3 rows with a "show full timeline" expand), a Planets table (plain `<table>`, not the scaffolded `NovaTable` primitive — that generic component turned out to have a `Record<string, ReactNode>` row constraint incompatible with typed data like `ChartPlanet`, caught by `tsc`; used the same raw-table approach Today Nova's own planet table already takes instead of fighting the generic), a Yogas & Doshams compact list (`chart.yogas`/`.doshams`, `isPresent` filtered, default 3 rows + "show all" expand), and a new `NovaMemberRelationshipsCard` ("In the family" — the *viewed* member's own pairwise score vs every other participant via `compareSynastry()`, lazy-loaded on a "Load →" button, distinct from Phase 4's family-wide all-pairs Bonds card).
  Documented default judgment calls (cosmetic/labeling ambiguity, not data gaps — kept moving per working style rather than asking): relationship labels in the new card always show the other party's stored `relationshipToOwner` regardless of whose page is open, since the system has no member-to-member relation field and inventing bidirectional inference would need gendered guessing this initiative has repeatedly declined to do; the Planets table's default-5 rows are simply the first 5 in data order (not an attempt to replicate the mockup's specific hand-picked 5), and the "All N & degrees" link uses the real planet count rather than the mockup's literal "10"; relationship scores are shown on the real 0-100 scale the backend returns, not rescaled to the mockup's example "/10" styling.
  Wired into `dashboard-family-tab-nova.tsx`: added a `detailView` boolean state, computed `prevMember`/`nextMember` from array position, computed `relationshipParticipants` (owner + every other member, each with their `relationshipToOwner` label), and an early-return that swaps the whole tab body to `DashboardFamilyMemberNova` when `detailView` is true — same "sub-view of one tab" mechanism `cal-panch`/`cal-monthly` already established, no new route. Added the "View full profile →" button as a sibling element directly above the existing `<MemberDetailExpanded>` call (not inside the shared Classic component — keeps `dashboard-family-tab.tsx` untouched). Threaded a new `panchangam` prop from `dashboard-workspace.tsx` (`personal.panchangam`, already fetched workspace-wide for Today/Calendar) down to the Nova family tab, additive and Nova-branch-only.
  Verified: `tsc --noEmit` clean project-wide (caught and fixed the `NovaTable` generic-constraint issue above, and a missed `tNakshatra()` translation on the timing strip's Nakshatram card, before this passed), `eslint` clean (0 errors/warnings) on all new/touched files. Unlike every prior phase, the web dev server (port 3000) was **not** running this session (only the backend on :8000 was) — could not hit `/dashboard` for even the usual 307-no-500 smoke check this time, and did not start it myself since that wasn't asked. This phase's verification rests on `tsc`/`eslint` alone; the user's own browser check is the first real look at this screen. **Next:** user sign-off on both the outstanding Phase 4 `family` screen check and this phase's `family-member` screen (including the "View full profile" hand-off and the relationships card's live data), then Phase 6 (`explore` hub).
- **2026-07-06 (browser QA round — one real regression, one pre-existing bug, one open data question)** — User started the web dev server themselves and drove a real browser session across `cal-monthly` and Life Areas (not yet a Nova-built screen). Two clear, reproducible-from-code issues reported, plus a third needing clarification:
  1. **`cal-monthly` — today/Pournami/Amavasai highlights not showing.** Root cause in `dashboard-calendar-monthly-nova.tsx`: `isSelected` fully *replaced* `cellBg`/`cellBorder` instead of layering on top of the day's own `highlightType` tint, and the "Today" pill was explicitly hidden whenever `isToday && isSelected` — meaning on the default view (today is the initially-selected date), all three signals were suppressed simultaneously. Fixed: `cellBg`/`cellBorder` now always reflect the highlight tint first (falling back to a selected/plain tint only when there's no `highlightType`), selection/today are shown as an added `boxShadow` ring instead of an override, and the "Today" badge no longer hides itself when selected.
  2. **Universal "selected tab text is invisible" bug — a real regression from this session's own Phase 5 work, not pre-existing.** Traced to Life Areas' member-switcher and sub-tab pills (`dashboard-life-areas-tab.tsx:190-192,215-217`), which set `background: "var(--panel-earth-dark)"` + `color: "var(--panel-hover)"` for their active state — a light-text-on-dark-bg pairing that's correct in Classic (`--panel-earth-dark` is Classic's near-black `#1A1612`). Earlier this session, Phase 5's chart-grid gap-fix redirected `--panel-earth-dark` to `--color-text-strong` (light cream) because `dashboard-charts.tsx`'s `JathagamKattam` uses the *same* variable name for dark *text* on a light background — a role that needed the opposite Nova treatment. Since `--panel-earth-dark`/`--panel-hover`/`--panel-tan`/`--panel-tan-light`/`--panel-earth` turned out (grepped across the full `web/components` tree, not just the one file being fixed at the time) to be generic tokens reused in conflicting roles across 50+ not-yet-Nova-built components — this is the actual reason the bug reads as "universal": every screen still on Classic markup that happens to use this exact background+text pairing for a "selected" pill inherited the same invisible-text bug the moment the redirect landed. Fixed by reverting those 4 redirects from `dashboard-nova.css` (kept the `--chart-*`/`--planet-*` ones, individually grep-verified to be either scoped only to `dashboard-charts.tsx` or used consistently as accent-text colors elsewhere, with no conflicting background role found). Net effect: the D1/D9 chart grid's outer border/cell-border/body text go back to showing Classic's tan/cream look under Nova — a real but minor, explicitly deferred cosmetic gap (unlike the reverted vars, this one is low-visibility and low-risk to leave for a future pass) until a properly scoped fix lands (new `dashboard-charts.tsx`-only variable names that don't collide with any other component, rather than reusing shared Classic tokens). **Lesson applied going forward:** before redirecting any Classic-only custom property at the shared `.cd-shell` scope, grep its usage across the *entire* `web/components`/`web/app` tree, not just the one file motivating the fix — a variable name being safe in one component says nothing about its other consumers.
  3. **"Root user name appears twice" in Life Areas' member-selector row — asked the user to localize it rather than guessing.** User confirmed: two separate pills, not a header/pill duplication. Read the full render path (`dashboard-life-areas-tab.tsx`'s dedicated owner pill vs. its `memberCharts.map(...)` loop) and its data wiring in `dashboard-workspace.tsx` (`memberCharts={family.memberCharts.map(...)}`) — found no code path that would insert the owner into `family.memberCharts` a second time; per Phase 4's own research, that array is already confirmed to exclude the owner. Most likely explanation: the user's own family vault has a member entry that duplicates the owner (e.g. a test-time addition), which is vault *data*, not a code defect — did not invent a filter to guess around it, since excluding any member who happens to share the owner's display name would be wrong for a legitimately-named family member. **Left for the user to check directly**: open Family → member list and look for a duplicate self-entry to remove via the existing "Remove member" action.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings) on both touched files (`dashboard-nova.css`, `dashboard-calendar-monthly-nova.tsx`). Did not re-verify in a live browser session myself (server is the user's to drive per their message). **Next:** user re-check `cal-monthly` highlights and the previously-invisible selected pills (Life Areas and anywhere else the pattern showed up) in the browser; separately check the Family vault member list for a duplicate self-entry; then proceed to Phase 6 (`explore` hub) once clear.
- **2026-07-06 (Phase 6 build)** — Before starting, asked the user the two outstanding questions from the prior entry's handoff: (1) did the `cal-monthly` highlight fix and the selected-pill/tab-text-visibility fix look right in the browser — **user confirmed yes, both correct**; (2) did they find/remove a duplicate "self" entry in the Family vault member list — **user checked, found no duplicate**, meaning the root cause of Life Areas' "name appears twice" report from the prior entry is still unexplained (ruled out as a code defect in that same entry; still an open, low-priority data/UX mystery, not a blocker). Proceeded to Phase 6 as instructed.
  While locating the mockup's screen boundaries (`grep data-screen=`), found the source file now has **14** blocks, not the 10 §3 documents — 4 more exist (`life-areas`, `plan`, `transits`, `journal`), mapping directly onto the already-inventoried Explore-umbrella tabs. Logged as new §3.2 rather than silently building against stale coverage assumptions; not read in full this session, out of scope for Phase 6 itself.
  Read the `explore` mockup in full (lines 1174-1370) and `dashboard-explore-tab.tsx` (189 lines) end to end. Found the two are almost entirely different concepts, not a re-skin: Classic's version is a plain nav-hub (cards that jump to other dashboard tabs); the mockup is a searchable knowledge library (Natchathiram/Dosham/Yogam/Pariharam/Temples/Panchangam) plus a personalised "start from your chart" row and short-read articles. Confirmed via `ls` that real, live marketing content already backs all 6 library topics (`/natchathiram` with all 27 nakshatra pages, `/dosham`, `/yogam`, `/pariharam`, `/temples`) and 5 real `/learn/*` articles — even though the mockup's own `data-nav` attributes mostly self-loop (dead links in the source mockup itself, only Natchathiram/Dosham/Panchangam point somewhere real). Chose to wire every library tile to real content rather than copy the mockup's own dead loops: 5 open the matching marketing hub in a new tab (same `target="_blank"` precedent as the existing `/learn/what-is-chandrashtama` link in `dashboard-personal-tab.tsx`), Panchangam navigates in-app to the `calendar` tab via the existing `onNavigate`/`goToExploreDestination` mechanism.
  Traced the "start from your chart" personalisation to real, already-fetched data: `personalChartSummary.janmaNakshatra`/`.janmaPada` + `nakshatraCard` (`NakshatraCardData`, already threaded workspace-wide since Phase 1) for the birth-star card, and `personalChart.doshams` (`ChartDoshamInsight[]`, same field Phase 5 used per family member) — generalised to the first `isPresent && !isCancelled` entry rather than hardcoded to the mockup's own Sevvai-Dosham example — for the "active in your chart" card. Exported `strengthBand` additively from `dashboard-yoga-dosham-panel.tsx` (zero behavior change, same class of edit as every prior phase's additive exports) to reuse its existing STRONG/PARTIAL/WEAK→Strong/Moderate/Mild wording for the severity badge, which already matches the mockup's own "mild" label verbatim.
  **One card deliberately omitted, not stubbed or faked:** the mockup's "Your rasi" card is 100% interpretive prose ("Jupiter-ruled moon sign — faith, distance, the long view..."). Grepped the whole `web/` tree for a rasi analogue of `NakshatraCardData` and for any rasi→ruling-planet lookup table — neither exists anywhere, and the marketing site has no `/rasi/*` encyclopedia route either (only `indraiya-rasipalan`, a *today's-rasi-palan* tool). Writing the interpretive line myself would be inventing astrological content rather than re-skinning existing content, so the card is left out entirely rather than shipped half-empty or fabricated — flagged in §6.5, not silently dropped.
  **Two navigation destinations flagged as missing, not invented:** the mockup's "Read your star →"/"Understand it →" CTAs and the Natchathiram/Dosham library tiles' `data-nav="explore-moolam"`/`"explore-sevvai"` targets are Phase 7/8 screens (personalised Nakshatram/Dosham detail pages) that don't exist yet — per the working style (screen by screen, don't invent ahead), the two "start from your chart" cards instead show real data with a "Full profile page — coming soon" label rather than a dead link or an invented detail page.
  **Learn section**: the mockup's 3 example articles (Rahu Kalam explained / Guru Peyarchi transit / Dasa timeline) don't exist as pages anywhere (`ls web/app/learn` has 5 real slugs, none matching) — substituted with all 5 real, already-shipped `/learn/*` articles rather than writing new astrology-explainer prose for topics with no backing page. Reading-time labels ("4 min read") were dropped since no real reading-time data exists.
  **Search**: implemented as a real client-side substring filter over the Library/Learn cards actually rendered (title/description, both languages), not a network search across marketing content (no such index exists). The mockup's 3 example query chips (`Guru Peyarchi 2026`/`Sevvai dosham`/`Rahu Kalam`) were kept verbatim as one-click search shortcuts — a chip yielding zero matches gracefully falls through to the Ask strip's "Can't find what you're looking for? Ask Vinaadi" prompt, which is exactly the interaction the mockup's own copy already frames, not an accidental dead end.
  Built `web/components/dashboard-explore-tab-nova.tsx` (new file): header + search, conditional "start from your chart" row (0-2 cards depending on real chart data), a 6-tile library grid, a 5-article Learn grid, and the Ask strip — see §6.5 for the full mapping. Wired into `dashboard-workspace.tsx`: added a `DashboardExploreTabNova` dynamic import and split the existing `activeTab === "explore"` render block into `uiVariant === "nova"` / `uiVariant !== "nova"` branches (same pattern as every prior phase), passing `personalChartSummary`/`personalChart`/`nakshatraCard` (all already in scope from the Today Nova wiring) plus the existing `goToExploreDestination`/Ask-Vinaadi-open callbacks — no new data fetching anywhere in this phase.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings) on all three touched/new files (`dashboard-explore-tab-nova.tsx`, `dashboard-workspace.tsx`, `dashboard-yoga-dosham-panel.tsx`). No dev server was running this session on either port — could not do the usual `/dashboard` 307-no-500 smoke check; did not start one myself since that wasn't asked (same precedent as Phase 5). This phase's verification rests on `tsc`/`eslint` alone. **Next:** user sign-off on the `explore` hub in the browser (search filter, the 6 library links opening real pages, the conditional chart-starter cards, the Ask strip), then Phase 7 (`explore-moolam` — personalised Nakshatram profile detail) or Phase 8 (`explore-sevvai` — personalised Dosham profile detail), whichever the user prefers to sequence next; also still open from earlier sessions: the unexplained Life-Areas "name appears twice" report (confirmed not a code defect, cause unknown), and reading the newly-found `life-areas`/`plan`/`transits`/`journal` mockup screens (§3.2) in full before their own Nova passes.
- **2026-07-06 (pre-Phase-7 sign-off + a real navigation bug)** — Before starting Phase 7, asked the user the two outstanding Phase 6 questions per the working style. Answers: (1) Explore hub sign-off — **user reported "can't find explore in nova mode"**, not a cosmetic note but a hard blocker; (2) the 4 newly-found mockup screens (§3.2) — **user chose to sequence them in soon**, updating the top status banner accordingly rather than leaving them indefinitely deferred.
  **Root-caused the Explore-unreachable report before touching Phase 7**, since Phase 7 builds a sub-view *inside* Explore — shipping it while the parent hub itself was unreachable would have made it unreachable too. Found: Nova's nav-shell CSS (`dashboard-nova.css`, Phase 0) unconditionally hides `.cd-left-rail` and forces `.cd-tabnav` visible at *every* viewport width (not just mobile, unlike Classic's `>=1024px` media-query swap) — meaning under Nova, `DashboardHero`'s `cd-tabnav` pill strip is the *only* navigation surface, desktop included. That strip's `TAB_DEFS` array (`dashboard-hero.tsx`) never included an `"explore"` entry at all — it lists `life-areas`/`plan`/`transits`/`journal` as individual pills but not the umbrella hub itself, unlike `dashboard-left-rail.tsx`'s `RAIL_ITEMS`, which has exactly 5 top-level items (`personal`/`calendar`/`family`/`tools`/`explore`) and treats those same four as children that only light up the `explore` icon, never appearing as their own rail entries. This asymmetry was always latent in Classic (mobile tabnav missing a real hub tab), but harmless there since Classic's own `DashboardExploreTab` was "just a nav hub to those same four tabs" (Phase 6's own finding) — nothing unique was behind it. Nova's Explore hub is a real, unique surface (search, library, personalised cards) with no other entry point, so the same latent gap became a hard dead-end the moment Nova made the tabnav the sole nav mechanism. Fixed by adding `{ id: "explore", labelEn: "Explore", labelTaKey: "tab_explore" }` to `TAB_DEFS` (`dashboard-hero.tsx`), positioned right before its umbrella children for readability; `tab_explore` already existed in `lib/i18n.ts` (oddly, `dashboard-left-rail.tsx`'s own rail entry doesn't use it and falls back to hardcoded English — a separate, pre-existing, out-of-scope Classic-only gap, not touched). Additive to both Classic and Nova's shared `cd-tabnav` component; `tsc`/`eslint` clean.
  Read the `explore-moolam` mockup in full (lines 1372-1543) and skimmed `explore-sevvai` (1544-1693, Phase 8) to test §3's "shared detail-page shell" guess — see §6.6 for the resulting judgment call (standalone screen this phase; real shared skeleton exists but only 2 concrete instances so far, revisit consolidation after Phase 8 is actually built rather than guessing its needs from a mockup read). Cross-checked `dashboard-personal-overview.tsx`'s Nakshatra card, Today Nova's Deep Dive Nakshatra card, and `dashboard-yoga-dosham-panel.tsx` per the working style before building.
  **Research turned up more real, already-computed data than expected** — see §6.6's "Real data found" note: `GET /api/v1/content/nakshatra/{n}` already accepts any 1-27 (not just the caller's own), making the mockup's prev/next star browsing fully real rather than a stub; `moonRasiFromNakshatra`/`rasiName` (Phase 2 exports) is the exact classical formula for the Attribute band's Rasi fact and the padas table's Navamsa column; `personalDailyGuidance.nakshatraPerspective` (already computed by `build_nakshatra_perspective()`, already in the API response) is the mockup's "Today's lens" line, previously rendered only in Classic and the retired v2 preview, never in Nova.
  **Made one small additive backend change directly rather than pausing to ask**, since the data was 100% classical reference data with zero fabrication risk (same class of confidence as Phase 4's family-summary/highlight discoveries, not the same class as Phase 1's product-shape decision that warranted a pause): Ganam and Yoni per-nakshatra were real lookup tables already living in `app/calculations/porutham.py` as private `_GANA`/`_YONI` dicts, used only for Porutham match-scoring. Renamed them to public `GANA_BY_NAKSHATRA`/`YONI_BY_NAKSHATRA` (grepped first — used only within that file, safe rename, zero calculation-engine logic touched), added `ganam`/`yoni` (`NakshatraBiText`) fields to `NakshatraCard` (`app/services/nakshatra_content.py`) auto-populated in `model_post_init` from those tables plus two new small label dicts (`_GANA_LABELS`/`_YONI_LABELS` — standard Tamil/English terms for 3 gana + 14 yoni categories, not per-star content), and mirrored the two fields onto the shared `NakshatraCardData` TS type (additive; grepped `mobile/` — no consumer exists there). Verified directly against Python (`get_nakshatra_card(19)` → Rakshasa Gana/Dog for Moolam, matching the mockup's own example exactly) and spot-checked 5 different nakshatra numbers to confirm the values genuinely vary per star rather than being copy-pasted.
  Built `web/components/dashboard-explore-moolam-nova.tsx`: breadcrumb + prev/next star nav (real, fetches any of the 27 via `useQuery`/`STALE.static`, same endpoint `usePersonalData.ts` already calls), hero, 5-fact attribute band (Lord/Rasi/Deity/Ganam/Yoni), nature prose + strength/caution chips, a 2-column padas table (Pada + Navamsa only — see §6.6 for why Syllable/Flavour are genuinely omitted, not fabricated), a personalised "In your chart" card (own pada + `nakshatraPerspective`, shown only when viewing your own star), a Pariharam card substituted with a real link-out to `/pariharam` (no structured per-planet remedy data exists anywhere to build the mockup's temple/mantra list without inventing it), an "In your family" card (real cross-reference against `memberCharts[].nakshatraCard`/`.dasha.timeline`, same mahadasha-shape Phase 5 already reads), and an Ask-entry chip. Threaded `memberCharts` and `personalDailyGuidance` into `DashboardExploreTabNova` (additive props, same plumbing precedent as Phase 5's `panchangam` prop into the Family tab) and wired Phase 6's "Full star profile page — coming soon" birth-star card into a real button that opens this screen.
  **User caught a real naming smell mid-build**: flagged that a file called `dashboard-explore-moolam-nova.tsx` looked hardcoded to one star. Checked — the logic itself was never hardcoded (grepped the file for "Moolam"/"Ketu"/"19", zero literal matches; everything renders from the fetched `card` object and the signed-in user's own `janmaNakshatra`/`janmaPada`), but the file/component name was inherited from the mockup's own screen id and did invite exactly that misreading. Renamed to `dashboard-explore-nakshatram-nova.tsx`/`DashboardExploreNakshatramNova` (same rename class as every prior phase's naming cleanup) and expanded the file's header comment to say explicitly that Moolam was only the mockup's example star. Also caught mid-rename: a `replace_all` for the state variable `moolamOpen` missed the case-different setter `setMoolamOpen` (capital M doesn't match a lowercase-first search string), leaving a real would-have-been runtime bug (calling an undefined setter) — fixed as a second pass once spotted.
  Verified: Python (`app.api.content`, `app.services.share_card_service`, `app.calculations.porutham`, `app.services.synastry_service`) imports cleanly; `tsc --noEmit` clean project-wide; `eslint` clean (0 errors/warnings) on all touched/new files (`dashboard-hero.tsx`, `dashboard-explore-nakshatram-nova.tsx`, `dashboard-explore-tab-nova.tsx`, `dashboard-workspace.tsx`). No dev server running this session — could not do the usual `/dashboard` smoke check; verification rests on `tsc`/`eslint`/direct Python checks. **Next:** user re-check that Explore is now reachable under Nova at all (the tabnav fix) before anything else, then sign off on the `explore-moolam` screen itself (prev/next star browsing, the attribute band's new Ganam/Yoni facts, the personalised "In your chart"/"In your family" cards, the Pariharam link-out) — then Phase 8 (`explore-sevvai`) or the newly-prioritised `life-areas`/`plan`/`transits`/`journal` screens, whichever the user prefers to sequence next.
- **2026-07-06 (Phase 8 build)** — Before starting, asked the user the two outstanding Phase 7 questions per the working style: (1) is Explore reachable under Nova now (the tabnav fix) — **user confirmed yes**; (2) does the `explore-moolam` screen work correctly (prev/next across all 27, Ganam/Yoni facts, personalised cards, Pariharam link-out) — **user confirmed yes, all correct**. Proceeded to Phase 8 as instructed.
  Re-grepped `data-screen=` (file unchanged at 14 blocks, `explore-sevvai` still lines 1544-1693) and read it in full, then read `dashboard-yoga-dosham-panel.tsx` end to end (1074 lines — not previously read in full for this initiative) before writing anything. See §6.7 for the full gap/mapping table; summary of what shaped the build:
  **Structural finding, decided the whole approach**: unlike Nakshatram, there is no dosham "encyclopedia" endpoint independent of a chart — `ChartDoshamInsight` is inherently chart-relative. "Browsing" therefore cycles through the 8 dosham types the engine always computes for the viewer's own chart (`personalChart.doshams`, confirmed always-8 via `app/calculations/yogas.py:224-292`), not any-of-N independent of the viewer. No backend changes were needed or made this phase (a more conservative footprint than Phase 7's Ganam/Yoni addition) — everything came from fields and content maps that already existed.
  **The Pariharam card turned out mostly real, not a stub** (opposite of Phase 7's nakshatra-Pariharam finding): `dashboard-yoga-dosham-panel.tsx` already has `DOSHAM_REMEDIES` — real remedy prose for 7 of 8 dosham types, already shipping in Classic's (unexported) `DoshamCard`, including the exact temple (Vaitheeswaran Koil) and day (Tuesday) the mockup names for Sevvai. Made 10 additive `export` keyword changes to that file (`markerLabel`, `displayName`, `DOSHAM_WHAT`, `getWhat`, `buildWhyText`, `DOSHAM_OUTCOMES`, `DOSHAM_REMEDIES`, `DOSHAM_HOW_TO`, `getDoshamPowerContext`, `doshamSeverityScore` — same zero-behavior-change class as every prior phase's additive exports) to reuse this content instead of re-deriving or duplicating it.
  **Found and fixed a real pre-existing bug in the same file**: `YOGA_DISPLAY`'s Tamil entries for all 7 dosham keys were placeholder English text sitting in the `.ta` slot (e.g. `SEVVAI_DOSHAM: { ta: "Sevvai Dosham", ... }`), meaning Classic's own `YogaDoshamPanel` and Phase 5's family-member Yogas & Doshams list have been silently showing non-Tamil text in Tamil mode. Fixed with the real Tamil terms the backend already uses verbatim (`_yoga_helpers.py`'s `what_ta_map`), and added the one missing `MARANA_KARAKA_STHANA` entry. Frontend-only, benefits Classic too.
  **Verified generalisation across 4 structurally different dosham types before trusting any of it** (not just the mockup's Sevvai example, per the explicit instruction): Sevvai (house-based), Pitru (conjunction/9th-house-based), Badhaka (lagna-dependent, no fixed house set) and Kalasarpa (arc-based, not house-based at all). Confirmed the mockup's own "Trigger houses"/"Planet" attribute-band facts genuinely don't generalise across these (grepped `app/calculations/_yoga_dosham.py`'s 6 `category=` call sites and the house-set constants in `_yoga_helpers.py`) — replaced with a `category`-derived "Mainly touches" label (new small presentation-only map, same class as Phase 7's Ganam/Yoni labels) and a Dasha-activation fact instead. Also grepped `app/services/marriage_service.py` to confirm the mockup's "Related — Porutham matching" link is only truthful for Sevvai/Rahu-Ketu, not all 8 types, before deciding when to show it.
  **Shared-shell judgment call, now with 2 concrete instances to check against (per §6.6's own deferral)**: the breadcrumb+prev/next row, hero shell, attribute band, two-column layout, card style, and Ask-entry chip turned out pixel-identical between Nakshatram and Dosham — `NovaKicker` and the card-style object were already copy-pasted verbatim between the two files. Extracted those into a new `web/components/dashboard-explore-detail-nova.tsx` (`NovaKicker`, `novaDetailCardStyle`, `NovaDetailBreadcrumb`, `NovaDetailHero`, `NovaAttributeBand`, `NovaAskEntryChip`) and refactored `dashboard-explore-nakshatram-nova.tsx` (Phase 7's file) to use them too, not just this phase's new file — proof the extraction actually removes duplication. The higher-level content cards stayed bespoke per screen (data shapes diverge too much — a chart-independent encyclopedia entry vs. a chart-relative computed result). Also fixed a small bug found in the process: `.nova-grid-detail` (Phase 7's two-column wrapper class) was referenced but never defined in `dashboard-nova.css`, and carried a literal inline `gridTemplateColumns` that silently defeated the mobile collapse the class name implied existed. Added the real CSS rule (mirroring `.nova-grid-2/3`) and removed the redundant inline override from both screens.
  Built `web/components/dashboard-explore-dosham-nova.tsx`: breadcrumb + prev/next dosham nav (cycles `personalChart.doshams` by array index, wrap-around — no fetch, data is already in scope), hero (status badge, name EN/TA via the now-fixed `displayName`, prose via `getWhat`), a 4-fact attribute band (Mainly touches / In your chart / Severity / Dasha), a "What it actually means" card (engine prose + the engine's own generic guidance-signal reassurance text in place of the mockup's unverifiable "one chart in three" prevalence stat), a "Why it triggers or softens" card (bullet lists via `markerLabel`, reusing `DoshamCard`'s existing trigger/cancellation split), a personalised "In your chart" card (only when `isPresent`, with a real "See it in your chart →" button wired to the Today tab — matching the mockup's own `data-nav="today"` target), a Pariharam card (real `DOSHAM_REMEDIES` prose for 7 types, link-out substitute for the 1 without an entry), a "Check your family" card (cross-references `memberCharts[].chart.doshams` by matching dosham name, same field shape Phase 5 already used), a "Related" card (Porutham link gated to the 2 verified-relevant types, general dosham-hub link always), and the shared Ask-entry chip. Wired Phase 6's "Active in your chart" dosham card's "Full profile page — coming soon" text into a real button that opens this screen at the active dosham's index.
  Verified: `tsc --noEmit` clean project-wide (both before and after all edits), `eslint` clean (0 errors/warnings, `--max-warnings=9999` full-project run) on every touched/new file (`dashboard-yoga-dosham-panel.tsx`, `dashboard-explore-detail-nova.tsx`, `dashboard-explore-nakshatram-nova.tsx`, `dashboard-explore-dosham-nova.tsx`, `dashboard-explore-tab-nova.tsx`, `dashboard-nova.css`). Both dev servers were already running this session (web:3000, backend:8000) — confirmed `/dashboard` still compiles (307 auth redirect, no 500), same smoke check as every prior phase. Did not drive a real authenticated browser session myself — same handoff as every prior phase. **Next:** user sign-off on the `explore-sevvai` screen in the browser (dosham browsing across all 8 types, the Mainly-touches/Dasha attribute facts, the Pariharam prose for a few different dosham types, the family cross-reference, the Related-card Porutham link only appearing for Sevvai/Rahu-Ketu) — then the newly-prioritised `life-areas`/`plan`/`transits`/`journal` screens (§3.2), which still haven't been read in full.
- **2026-07-06 (breadcrumb/marketing-link-out fix, before Phase 9)** — Before starting the newly-prioritised `life-areas`/`plan`/`transits`/`journal` screens, asked the user for Phase 8 browser sign-off per the working style. **User reported a real bug, not a sign-off**: "some links in [the] nakshathiram and pariharam and yogam [content] pages take[] the user to [the] marketing site['s] dosham [and] yogam pages — breadcrumb also doing the same." Traced this to `NovaDetailBreadcrumb` (`dashboard-explore-detail-nova.tsx`) — its middle "hub" crumb (e.g. "Natchathiram" in the Nakshatram screen's breadcrumb, "Dosham" in the Dosham screen's) was a real `<a href=... target="_blank">` pointing at the marketing site, which is surprising behavior for a breadcrumb (a crumb should navigate within the app to that hierarchy level, not launch an external tab). The two detail screens (Phase 7/8) also each had additional deliberate marketing link-outs from their own build (Pariharam "Browse the Pariharam library →"/"Read the full guide →", Related card's "Browse all doshams →" and Porutham-matching article link) — all `target="_blank"`, all working-as-designed per §6.6/§6.7, but the user's report read them as unwanted site-exits stacking on top of the breadcrumb bug, not as separate, acceptable features.
  Asked the user directly whether to (a) fix only the breadcrumb (keep the content-card links) or (b) remove every marketing link-out from both detail screens. **User chose (b), the more thorough fix** — keep users fully inside the dashboard from these two screens.
  Made these changes: (1) `NovaDetailBreadcrumb` (`dashboard-explore-detail-nova.tsx`) — dropped the `hubHref` prop entirely; the hub crumb now renders as plain `<span>` text, not a link. (2) `dashboard-explore-nakshatram-nova.tsx` — removed the Pariharam card entirely (its only content was the now-removed link; per §6.6 no structured per-nakshatra remedy prose exists to show instead, so a link-less version of this card would have been a dead-end sentence with nothing to act on — same "omit rather than ship half-empty" precedent as Phase 6's "Your rasi" card), and removed the now-unused `PARIHARAM_HUB_HREF`/`NATCHATHIRAM_HUB_HREF` constants. (3) `dashboard-explore-dosham-nova.tsx` — Pariharam card now only renders when `DOSHAM_REMEDIES` has a real entry (7 of 8 types; drops the trailing link, keeps the real remedy prose), and is omitted for Marana Karaka Sthana rather than showing a link-only fallback — this now matches the exact precedent Classic's own `DoshamCard` already follows (`remedies && dosham.isPresent`, found by re-checking `dashboard-yoga-dosham-panel.tsx`). The Related card's Porutham-matching link became plain informational text ("This dosham also factors into Porutham (marriage) compatibility matching") for Sevvai/Rahu-Ketu only, the "Browse all doshams" link was dropped entirely (genuinely redundant — the breadcrumb's own prev/next arrows already browse all 8 dosham types in-app, with real personalised data instead of generic marketing prose), and the whole Related card is omitted for the other 6 dosham types rather than rendered as an empty box. Removed the now-unused `DOSHAM_HUB_HREF`/`DOSHAM_MARKETING_HREF`/`POROUTHAM_ARTICLE_HREF` constants and the `marketingHref` computation. Updated both files' header comments and §6.6/§6.7's Pariharam/Related table rows to record the revision.
  **Not touched, deliberately**: Phase 6's Explore-hub library tiles (Natchathiram/Dosham/Yogam/Pariharam/Temples/Panchangam, `dashboard-explore-tab-nova.tsx`) — those are the hub's own explicit "browse the encyclopedia" feature, a different UX pattern (a tile whose entire job is to jump to marketing content, clearly labeled as such) from a breadcrumb or an in-card aside that silently exits the app, and the user's question/answer this round was scoped to "these two detail screens," not the hub.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings) on all three touched files (`dashboard-explore-detail-nova.tsx`, `dashboard-explore-nakshatram-nova.tsx`, `dashboard-explore-dosham-nova.tsx`). No dev server was running this session — could not do the usual `/dashboard` smoke check; verification rests on `tsc`/`eslint`. **Next:** user re-check both screens in the browser (breadcrumb no longer links out, Pariharam/Related cards read correctly without dead ends) — then Phase 9 (`life-areas`, the first of the four newly-prioritised screens per user's chosen sequencing order).
- **2026-07-06 (Phase 9 build)** — User confirmed the breadcrumb/marketing-link-out fix would be checked separately and chose to sequence the 4 newly-discovered screens in mockup order, starting with `life-areas`. Re-grepped `data-screen=` (file unchanged at 14 blocks, `life-areas` still lines 2018-2251) and read it in full, then read `dashboard-life-areas-tab.tsx` (333 lines) and `life-area-card.tsx` (209 lines) end to end, plus checked `dashboard-prediction-panel.tsx`/`dashboard-remedies-panel.tsx`/`dashboard-jadhagam-report-panel.tsx`/`dashboard-event-windows.tsx` for Nova-token safety before deciding scope. See §6.8 for the full gap/mapping table; summary of what shaped the build:
  **§4.3 correction, verified for real this time (per the explicit instruction not to trust the old first-pass guess)**: grepped actual imports of `dashboard-life-event-log.tsx`/`dashboard-life-events.tsx` — both are used exclusively by `dashboard-plan-tab.tsx`, not `dashboard-life-areas-tab.tsx`. They were mis-grouped under "Life Areas" in §4.3; correction logged in §6.8 rather than silently reading the wrong files. Not read this phase — will be read properly when Plan is built.
  **Biggest finding: this is much closer to a re-skin than a rebuild.** The mockup's static export is effectively a live capture of Classic's own "Overview" sub-tab — identical hero copy, identical member-pill switcher, identical 5-tab sub-nav (Overview/Predictions/Yogas & Doshams/Remedies/Full report), and `LifeAreaCard` already renders every single field the mockup shows (trend, score+bar, Karaka+house, supporting/blocking factors or a reasoning-engine `causalChain`, "Next 30 days" outlook, caution, remedy, "Your focus" badge, "Details →"). Grepped every `var(--...)` reference in `life-area-card.tsx` and found it's 100% Nova-token-driven already — the three Classic-named tokens it reads (`--chart-d1-lagna-bg`, `--panel-warm-tint`, `--planet-lagna`) were already redirected to Nova-safe values by earlier phases' gap-fix block, and Nova's own `--color-score-high/mid/low` turned out to be the *exact* hex the mockup itself uses for score text (both trace back to the same design source). Reused `LifeAreaCard` **verbatim** rather than building a parallel Nova card — same class of free reuse as Phase 2's `DayTimeline`. One real pre-existing bug found while cross-checking (not invented): the remedy box's text used a literal hardcoded `#7a3412` with no CSS variable at all, which would render as low-contrast rust-brown on Nova's dark card background. Fixed directly in `life-area-card.tsx` (a Classic-shared file) by switching to `var(--color-text)` — confirmed this resolves to the same dark ink Classic already uses for body text elsewhere (a visual no-op there), so the fix benefits Classic too rather than being a Nova-only patch.
  Also confirmed via grep that all 12 of the engine's life areas (`app/services/life_areas_service.py:1401-1413`, always computed in this fixed order) match the mockup's 12 example cards exactly — including that "Spiritual growth" (SPIRITUAL) and "Spirituality" (SPIRITUALITY) are two genuinely distinct backend keys, not a mockup duplication mistake.
  **The two genuinely new pieces, both built on real data, nothing invented:** (1) tier grouping (Needs attention/Steady/Supportive) — derived from `getScoreBand()`'s existing tone thresholds (high ≥65/mid 45-64/low <45, already used app-wide) plus the real `caution` field as an override (an area is "Needs attention" if its tone is low OR it carries a caution — matches the mockup's own Health example, which is mid-tone but caution-flagged for an active Ardhashtama Sani). (2) the goal-focus strip — threaded a new `goals` prop (`plan.goals`, already fetched workspace-wide by `usePlanData()` for the Plan tab, zero new fetch) into the Nova component, and additively exported `GOAL_OPTIONS` from `dashboard-plan-tab.tsx` to reuse its existing goal-type→display-label i18n mapping rather than re-authoring goal names.
  **Caught and corrected my own first instinct on the nav-callback wiring before shipping it**: the obvious-looking precedent for "Edit goals in Plan →"/"See the transits behind them →" was `goToExploreDestination` (used for Today Nova's `onGoToFamily`/`onGoToJournal`/`onGoToCalendar`), but tracing its actual mechanism (`dashboard-workspace.tsx:298-306` plus the `exploreReturnTab === activeTab` banner check around line 1198) showed it specifically marks "arrived here via the Explore hub, show a Back-to-Explore banner." Reusing it from Life Areas — which the user did not reach via Explore — would have painted a misleading "← Back to Explore" banner on the Plan/Transits tab after clicking either link. Used the plainer `goToTab` instead (clears any stale explore-return state, just switches tabs), added as two new callbacks (`onGoToPlan`, `onGoToTransits`).
  **Overview is the only sub-tab built this phase.** The mockup's static export never expands Predictions/Yogas & Doshams/Remedies/Full report — only their nav pills are visible. Checked all four underlying panels for Nova-token safety before deciding: `dashboard-prediction-panel.tsx`, `dashboard-remedies-panel.tsx`, and `dashboard-jadhagam-report-panel.tsx` all read multiple Classic-only literal-hex tokens (confirmed via grep, no `[data-ui="nova"]` override), and `YogaDoshamPanel` was already confirmed Classic-token-heavy during Phase 5/8. Deferred all four exactly like Phase 2 initially deferred `cal-monthly` — reused Classic-styled verbatim so every sub-tab stays fully functional, flagged in §6.8 for a future pass rather than silently left half-done or rebuilt as an unplanned scope expansion with no mockup to build against.
  Made 1 additive `export` keyword change (`isAreaRelevantForAge`, `dashboard-life-areas-tab.tsx`) and 1 additive export (`GOAL_OPTIONS`, `dashboard-plan-tab.tsx`) — both zero-behavior-change, same class as every prior phase. Built `web/components/dashboard-life-areas-tab-nova.tsx` (new file): header (kicker/headline/methodology subcopy, member pills, 5-tab sub-nav — all Nova-token styling only, content identical to Classic), the chart-signature banner (reused from Classic, previously Nova-unsurfaced), the goal-focus strip (conditional, only when a goal is active), the three tier groups rendered through a new `.nova-grid-4` responsive utility (`dashboard-nova.css`, 4→2→1 columns, mirrors the existing `.nova-grid-2/3` pattern), the "Why these scores" explainer card, and the four deferred sub-tab panels reused verbatim. Reused `novaDetailCardStyle` from `dashboard-explore-detail-nova.tsx` for the strip/banner/explainer card containers (a genuinely generic, presentation-only value — not the detail screens' breadcrumb/hero/attribute-band structure, which does not apply to this tab-level screen and was not forced onto it).
  Wired into `dashboard-workspace.tsx`: added a `DashboardLifeAreasTabNova` dynamic import and split the existing `activeTab === "life-areas"` render block into `uiVariant === "nova"` / `uiVariant !== "nova"` branches (same pattern as every prior phase), threading `goals={plan.goals}` (`usePlanData` was already called workspace-wide at line 464 for the Plan tab) and the two new `goToTab`-backed callbacks.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings, `--max-warnings=9999`) on all touched/new files (`dashboard-life-areas-tab-nova.tsx`, `dashboard-life-areas-tab.tsx`, `dashboard-plan-tab.tsx`, `dashboard-workspace.tsx`, `life-area-card.tsx`, `dashboard-nova.css`). Both dev servers were running this session — confirmed `/dashboard` still compiles (307 auth redirect, no 500). Did not drive a real authenticated browser session myself — same handoff as every prior phase. **Next:** user sign-off on the `life-areas` screen in the browser (tier grouping across a chart with areas in all three bands, the goal-focus strip with a real active goal, "Edit goals in Plan →"/"See the transits behind them →" switching tabs cleanly with no stray "Back to Explore" banner, the Details drawer, and that the 4 deferred sub-tabs still work even though they're Classic-styled) — then `plan` (next in the user's chosen mockup-order sequencing).
- **2026-07-06 (Phase 10 build)** — User confirmed Life Areas work is done, proceeded straight to `plan` (second of the four newly-discovered screens) without re-litigating sequencing. Re-grepped `data-screen=` first (file unchanged at 14 blocks, `plan` still lines 2251-2421) and read it in full, then read `dashboard-plan-tab.tsx` (682 lines) and `usePlanData.ts` end to end, plus `dashboard-decision-panel.tsx` and `dashboard-event-windows.tsx` for Nova-token safety before deciding scope. See §6.9 for the full gap/mapping table; summary of what shaped the build:
  **§4.3's Phase-9 correction held up on a real read**: `dashboard-life-event-log.tsx`/`dashboard-life-events.tsx` are indeed Plan-only (confirmed via the imports in `dashboard-plan-tab.tsx`), not mis-grouped a second time.
  **Structural finding that shaped the whole build**: the mockup's static export only expands the "Goals" pill — same one-sub-tab-captured pattern as Phase 9's Life Areas Overview — but unlike Phase 9 (where the other 4 sub-tabs were already separate, reusable components), Plan's Life Events/What-If/Best Dates & Muhurta/Decisions bodies were inlined directly in Classic's `DashboardPlanTab` render (~250 lines of stateful JSX, no standalone components). Matching Phase 9's reuse bar meant extracting them first: added `PlanEventsPanel`, `PlanWhatIfPanel`, `PlanMuhurtaPanel` (keeps its own local Best-Dates/Muhurta-preset state, fully self-contained), and `PlanDecisionsPanel` as exported components in `dashboard-plan-tab.tsx`, hoisted the pure `verdictColor`/`verdictKey`/`strengthColor`/`strengthKey` helpers to module scope alongside them, and additively exported `GOAL_EVENT_MAP`/`normalizeGoalType`. Classic's own render now calls these four components instead of inlining the JSX — a mechanical, behavior-preserving refactor verified by `tsc`/`eslint` staying clean before and after, not a rewrite. This is a bigger footprint than a typical additive-export pass, but the alternative (duplicating ~250 lines of stateful JSX into the Nova file, or double-rendering Classic's whole tab component with its own internal nav underneath Nova's) was worse on every axis.
  **Backend finding, not obvious from the frontend alone**: read `app/calculations/event_windows.py`'s three `find_*_windows()` functions directly and confirmed `find_event_windows()` returns windows **sorted by score descending, not chronologically** (`sorted(windows, key=lambda item: item.score, reverse=True)`, all three finders). `EventWindowsPanel` (reused as-is elsewhere) just renders that order, fine for a flat list but wrong for "what's the *nearest* one" — the mockup's own "Nearest supportive window" hero card needs chronological order. No backend change: the Nova Goals section re-fetches the same `/api/v1/charts/{id}/event-windows` endpoint itself (one `useQuery` per fixed event type — MARRIAGE/CAREER/FINANCE, `enabled` gated on whether a goal maps to it, not a variable hook-call count) and sorts client-side by `startDate` ascending.
  **Honest limitation, not invented around**: the mockup's hero headline names a specific dasa lord ("Mercury dasa"). Reason codes only explicitly name a planet for 4 of the ~10 possible codes (`mercury_dasha_active`/`sun_dasha_active`/`venus_dasha_active`/`jupiter_dasha_active`) — the "Nth-house-lord" codes are relative to the chart's own lagna, and the actual resolved planet (`_seventh_lord()`/`_tenth_lord()` etc.) is computed server-side but never serialized in the API response. The headline only names a lord when one of the 4 explicit-planet codes fired (via `tPlanetLord()`, the same i18n lookup Phase 9's tier blurb already uses for dasha-lord names); otherwise it shows just the month/year, not a guessed planet.
  **"Remind me →" confirmed a genuine stub, not built**: grepped the whole `web/`/`app/` tree for any per-event/per-date reminder mechanism. The only existing "reminder" feature (`dashboard-personal-overview.tsx`'s "Save reminder" button, backed by `app/api/notification_preferences.py`) toggles a single recurring daily morning-notification preference — no concept of "remind me about this specific multi-year-out date range" exists anywhere. Omitted, same precedent as Phase 1's "Add to my day"/Phase 4's "Plan together" rather than a dead-click button.
  Built `web/components/dashboard-plan-life-event-log-nova.tsx` (`NovaLifeEventLogCard`) — a fresh Nova-token rebuild of Classic's `DashboardLifeEventLog`, not a gap-fix: that file's own `W` token palette (`--panel-earth-dark`/`--panel-hover`/`--chart-cell-default`/etc.) is the same class of token the 2026-07-06 browser QA round found unsafe to redirect at shared `.cd-shell` scope (conflicting roles across 50+ components) — same decision class as Phase 2/3's `cal-panch`/`cal-monthly` sub-widget rebuilds. Additively exported `EVENT_TYPES`/`EVENT_ICON`/`eventLabel`/`LifeEventLogItem`/`EventCorrelation` from the Classic file and reused them verbatim (same API calls, same data) rather than re-deriving. The mockup's inline "· Venus bhukti" tag on the collapsed row (Classic only shows this after expanding) is reproduced directly since the data (`correlation.antarLord`) was already being fetched, just not surfaced at that layer.
  Built `web/components/dashboard-plan-tab-nova.tsx` (new file): header (kicker/headline/subcopy + a new 2-segment "🎯 Goals & Windows / 🪐 Transits & Dasha" toggle, the right segment wired to a new `onGoToTransits` callback), the 5-pill sub-nav (own local state, same 5 keys as Classic), and — for the Goals sub-tab — the "My goals" card (goal chips + a bounded `<select>` add-goal control, kept as the real enum-backed control rather than the mockup's free-text-styled placeholder — an honest simplification, not a functional gap; "highlighted in Life Areas" note wired to a new `onGoToLifeAreas` callback), the "Nearest supportive window" hero card (purple-accented, reusing Nova's existing `--color-accent-secondary`/`--color-accent-secondary-muted` tokens — the same purple Phase 3's legend already established, not a new color), one "Supportive windows" list card per goal-mapped event type (chronologically sorted, score/date-range/reason-bullets, the exact same methodology note text as Classic), a small guide card for goals with no mapped event type (reusing Classic's exact fallback copy), and the `NovaLifeEventLogCard`. The 4 deferred sub-tabs render the newly-extracted `PlanEventsPanel`/`PlanWhatIfPanel`/`PlanMuhurtaPanel`/`PlanDecisionsPanel` directly (Classic-styled, fully functional — same precedent as Phase 9's 4 deferred sub-tabs).
  Wired into `dashboard-workspace.tsx`: added a `DashboardPlanTabNova` dynamic import and split the existing `activeTab === "plan"` render block into `uiVariant === "nova"` / `uiVariant !== "nova"` branches (same pattern as every prior phase), passing the identical prop shape Classic already receives plus three new `goToTab`-backed callbacks (`onGoToTransits`, `onGoToLifeAreas`, `onGoToCalendar`).
  Verified: `tsc --noEmit` clean project-wide (both before and after the `dashboard-plan-tab.tsx` extraction refactor), `eslint` clean (0 errors/warnings, `--max-warnings=9999`) on all touched/new files (`dashboard-plan-tab.tsx`, `dashboard-plan-tab-nova.tsx`, `dashboard-plan-life-event-log-nova.tsx`, `dashboard-event-windows.tsx`, `dashboard-life-event-log.tsx`, `dashboard-workspace.tsx`). No backend changes this phase (confirmed no Python files touched). Both dev servers were already running this session — confirmed `/dashboard` still compiles (307 auth redirect, no 500), same smoke check as every prior phase. Did not drive a real authenticated browser session myself — same handoff as every prior phase. **Next:** user sign-off on the `plan` screen in the browser (the Goals/Transits toggle, goal chips add/remove, the Nearest-window hero card's dasa-lord naming and years-until phrasing across a chart with different goal combinations, the Supportive-windows list's chronological order, the Life Event Log's inline bhukti tag and log-event form, and that all 4 deferred sub-tabs still work even though they're Classic-styled) — then `transits` (next in the user's chosen mockup-order sequencing).
- **2026-07-06 (`transits` resolved as folded into Plan, not a separate screen)** — User confirmed Life Areas and Plan work were done and to move to `transits` next, then, before any code was written, corrected the sequencing plan directly: "remove transit from nav links because as per new design transit locates inside plan subtab." Re-grepped `data-screen=` first (file unchanged at 14 blocks, `transits` still lines 2421-2641) and read it in full per the working style — the mockup itself confirmed the correction independently: the `transits` screen's own top nav bar has **no "Transits" pill**, highlights **"Plan"** as the active tab, and its header reads `Plan · Transits & Dasha` with a `data-nav="plan"` link back; the sibling `plan` screen's header already has the exact "🎯 Goals & Windows / 🪐 Transits & Dasha" toggle Phase 10 built, with the right segment's own `data-nav="transits"`, and shows its 5 section pills only in "Goals & Windows" mode (none in `transits`). Read together: the mockup was never asking for a standalone `transits` tab at all — Phase 10's toggle had the right shape (a 2-segment control) but the wrong behavior (`onGoToTransits` navigated away via `goToTab("transits")` to a separate destination instead of switching a view in place).
  **Scope resolved without pausing to ask**: Nova only, per this initiative's own unbroken precedent that Nova is additive and Classic is never touched unless a Nova change requires it. Classic's pre-existing standalone `dashboard-transits-tab.tsx` tab, its Explore-hub "Transits & Dasha" nav card (`dashboard-explore-tab.tsx`), and the `activeTab === "transits"` render branch in `dashboard-workspace.tsx` are all **left completely untouched** — Classic users lose nothing. Read `dashboard-transits-tab.tsx` (582 lines) and `dashboard-dasha.tsx`'s `DashaTimeline`/`DASHA_COLORS` end to end before deciding whether either was reusable: both read Classic-only custom properties defined at bare `:root` in `globals.css` with zero `[data-ui="nova"]` override (`--panel-earth-dark`, `--planet-sun`, `--chart-cell-default`, `--transit-card-bg`, `--panel-brand`, `--planet-saturn`, etc.) — the same class of token the 2026-07-06 browser-QA round found unsafe to redirect at shared scope (conflicting roles across 50+ components) — so, matching the Phase 2/3/Life-Event-Log precedent, built fresh rather than gap-fixed.
  **Changes made**: (1) `dashboard-hero.tsx` — `DashboardHero` now takes an optional `uiVariant` prop; the shared `cd-tabnav`'s `tabs` filter drops the `"transits"` entry specifically when `uiVariant === "nova"` (Classic's own tabnav, and Classic's Explore-hub card which sets `activeTab` directly, are both unaffected). (2) New `web/components/dashboard-plan-transits-nova.tsx` (`NovaTransitsView`) — same data/derivations as Classic (`hasTransitSnapshot`/`flaggedCount`/`activeSaniCycles`/`dashaSupportScore`, and the same `dashaElapsedPct`/`dashaStatus` math Phase 5's family-member Dasa card already established for the Dasa/Bhukti/Antaram section, rather than reinventing Classic's `DashaTimeline` proportional lifetime bar), all-fresh Nova-token visuals — every per-planet color traces to an already-existing Nova token, not an invented one (Sun/Jupiter → `--color-accent-strong`, Moon/Venus → `--color-accent-secondary`, Mars → `--color-low`, Mercury → `--color-high`, Saturn → the existing one-off `--planet-other` blue from Phase 3/5, Rahu/Ketu → `--color-text`) — see §6.10 for the full mapping. (3) `dashboard-plan-tab-nova.tsx` — the header toggle's two segments are now real buttons calling a new lifted `view`/`onViewChange` prop pair instead of one static span + one `onGoToTransits` button; when `view === "transits"`, the tab renders `NovaTransitsView` instead of the sub-nav pills + Goals/deferred-sub-tab content (matching the mockup's own `transits` screen, which shows neither); the "See the dasa timeline →" link inside each Supportive-windows card now also calls `onViewChange("transits")` instead of navigating away; added a new `onGoToJournal` callback for the Journal-patterns card's "Start journalling →" link. (4) `dashboard-workspace.tsx` — lifted a new `planView` state (`"goals" | "transits"`, next to the existing `lifeAreasViewId`/`transitViewId` cross-tab selectors) specifically so Life Areas Nova's pre-existing "See the transits behind them →" link (`onGoToTransits`, §6.8) can jump straight into the Transits view from outside the Plan tab (`setPlanView("transits")` then `goToTab("plan")`, instead of a now-nonexistent nav target); threaded the same transit-specific data (`transitChart`/`transitDailyGuidance`/`transitTransit`/`transitSani`/`transitDasha`/`transitDashaMaha`/`transitDashaAntar`/`transitViewId`, already computed for the old standalone tab and left as-is) into the Plan Nova invocation instead.
  Verified: `tsc --noEmit` clean project-wide, `eslint` clean (0 errors/warnings, `--max-warnings=9999`) on all touched/new files (`dashboard-hero.tsx`, `dashboard-workspace.tsx`, `dashboard-plan-tab-nova.tsx`, `dashboard-plan-transits-nova.tsx`, `dashboard-life-areas-tab-nova.tsx`). No backend changes (confirmed no Python files touched). Both dev servers were already running this session — confirmed `/dashboard` still compiles (307 auth redirect, no 500), same smoke check as every prior phase. Did not drive a real authenticated browser session myself — same handoff as every prior phase. **Next:** user re-check in the browser that (a) the "Transits" pill no longer appears in Nova's tabnav while Classic's own Transits tab is unaffected, (b) Plan's toggle now switches the Transits & Dasha content in place (planetary positions, Sani cycles, Dasa/Bhukti/Antaram, journal patterns, Varshaphala teaser) without leaving the tab, and (c) Life Areas' "See the transits behind them →" link lands directly in that same view — then `journal` (the last of the 4 newly-discovered screens, §3.2).
- **2026-07-06 (Phase 11 build — `journal`, the last of the 4 newly-discovered screens)** — Re-grepped `data-screen=` first (file unchanged at 14 blocks, `journal` still lines 2641-2777, the final block) and read it in full, then read `dashboard-journal-tab.tsx` (699 lines), `useJournalData.ts`, and `dashboard-shadow-prompts.tsx` end to end before assuming anything about the mapping, per the explicit instruction that §4.3's grouping guesses had already been wrong twice. See §6.11 for the full gap/mapping table; summary of what shaped the build:
  **Structural finding**: not a detail drill-down (no shared-shell question with `dashboard-explore-detail-nova.tsx` applies) — a full tab-level screen with its own 3-way sub-nav (Write/Entries/Reflections), same shape as Life Areas/Plan. Only the "Write" pill is expanded in the mockup's static export, matching every prior "Explore umbrella" phase's one-sub-tab-captured pattern.
  **Token-safety finding that widened scope beyond just "Write"**: checked `dashboard-journal-tab.tsx`'s own `W` token map before reusing anything (the same check every phase runs) and found a real problem specific to this file — its `ink`/`inkMid`/`border`/`borderLt`/`surfaceMd` read `--panel-earth-dark`/`--panel-earth`/`--panel-tan`/`--panel-tan-light`/`--panel-hover`, the exact 5 custom properties the 2026-07-06 browser-QA round reverted (not redirected) after they broke selected-pill text elsewhere, while its `surface`/`card` tokens *are* redirected to Nova's dark surface for unrelated reasons. That specific mix — some tokens converted, some not — would render Classic's dark-brown entry text on top of an already-Nova-dark card background (illegible, confirmed by checking the actual hex values: `#3D352B` text on a `#2c2338` card), not the internally-consistent "light Classic island" look Phase 9/10's deferred sub-tab panels have (which keep both background *and* text on the unconverted Classic side, so they merely look out of place, not illegible). Concluded that deferring Entries/Reflections here — unlike every prior phase's deferred sub-tabs — would ship a real regression, so all three sub-tabs got a fresh Nova rebuild this phase, not just the mockup-captured one.
  **Real pre-existing bug found while reading `dashboard-shadow-prompts.tsx` end to end, unrelated to Nova but fixed while there**: every Tamil string in the file was mangled to literal `?????` (confirmed via `git log -p` — an old encoding mishap, commit `e8b7ebd9`, still live in Classic's own Reflections tab today). Fixed directly (same class of decision as Phase 8's `YOGA_DISPLAY` Tamil-placeholder fix): swapped the corrupted ternaries for `t()` calls, reusing the two real existing-but-unwired keys (`shadow_prompts_title`/`shadow_prompts_cta`) and adding 3 new ones (`shadow_prompts_desc`/`shadow_prompts_reflect_on`/`shadow_prompts_empty`) to `web/lib/i18n.ts`. Frontend-only, benefits Classic too, not scoped to Nova; `dashboard-shadow-prompts.tsx` itself still Classic-styled and still used as-is by Classic.
  **Real, already-computed data found for the mockup's dasha-context line** ("Moon dasa · Jupiter antaram"): `ChartSummaryData.currentMahadasha`/`.currentAntardasha` (`personal.chartSummary` — used directly rather than the member-switch-aware `personalChartSummary` variable other Nova tabs share, since Journal has never had a member switcher, matching Classic's own `chartId={personal.chartId}`), rendered through `tPlanetLord()` rather than Classic's own `dashboard-personal-tab.tsx:270-273`, which concatenates the raw lord code with no translation function at all (a small pre-existing Classic bug, left alone, not repeated here). Used the app's real 2-tier "Dasa"/"Bhukti" convention rather than the mockup's own "antaram" label for the antardasha tier — this codebase already reserves "antaram" for the 3rd tier (pratyantardasha) elsewhere (`dashboard-family-member-nova.tsx`/`dashboard-plan-transits-nova.tsx`), so reusing the mockup's word for a different tier would contradict already-shipped terminology.
  **"↻ New prompts" confirmed a genuine stub by reading `build_journal_prompts()` directly, not built**: its templates (`app/services/journal_service.py`) are fixed 3-item tuples keyed only by `(life_area, score_bucket)` — fully deterministic, so a "shuffle" control would visibly do nothing. Omitted, same precedent as Phase 1's "Add to my day"/Phase 10's "Remind me →"; the mockup's own real lever (changing the Life-area selector, which does return a genuinely different 3-prompt set) is kept, along with the real "Use →" click-to-insert behavior.
  Made 6 additive `export` keyword changes to `dashboard-journal-tab.tsx` (`LIFE_AREAS`, `LifeArea`, `AREA_KEY`, `CONTEXT_EVENT_TYPES`, `ContextEventType`, `CTX_TYPE_KEY` — zero behavior change, same class as every prior phase). Built `web/components/dashboard-journal-tab-nova.tsx` (new file): header (kicker/headline/subcopy, a 3-way Write/Entries/Reflections segmented pill with a live Entries count, and a streak chip reusing `useStreak()` directly — same per-component-hook precedent as Today Nova — with a real, derived "write your first note today" suffix shown only when no entry exists for today, not fabricated); the Write sub-tab (`.nova-grid-detail` 2-column layout, reusing the existing utility class) with a New-entry card (date/life-area fields, the dasha-context line, real prompts with "Use →", textarea+counter, Save, a new privacy note) and a right rail (a Pattern-insights card reusing the exact `journalCorrelations` rendering already built for Plan-transits' own "Journal patterns" card, §6.10, now with a real inline link to Transits using the exact `setPlanView("transits")`+`goToTab("plan")` mechanism Life Areas already established; an Upcoming-events card that's a straight Nova-token re-skin of Classic's already-fully-working context-events feature; a static "Why journal here?" nudge card); the Entries sub-tab (fresh Nova-token list/edit/archive/export, reusing the already-Nova-safe `Chip` primitive); and the Reflections sub-tab (`NovaShadowPromptsCard`, a fresh Nova-token rebuild of the same SHADOW-prompt fetch, correct Tamil text via the newly-fixed i18n keys; BEGINNER mode shows Classic's exact existing fallback copy unchanged).
  Wired into `dashboard-workspace.tsx`: added a `DashboardJournalTabNova` dynamic import and split the existing `activeTab === "journal"` render block into `uiVariant` branches (same pattern as every prior phase), threading `chartSummary={personal.chartSummary}`, `journalCorrelations={personal.journalCorrelations}` (both already fetched workspace-wide, zero new fetches) and a new `onGoToTransits` callback matching Life Areas' exact wiring.
  **Confirmed, not assumed, that Plan-transits' pre-existing "Journal patterns" teaser card lands correctly**: its `onGoToJournal={() => goToTab("journal")}` (built in §6.10, before this phase's Nova Journal tab existed) simply flips `activeTab`, and `goToTab` itself only clears `exploreReturnTab` and sets the tab — no Journal-specific state involved — so the now-added `uiVariant === "nova"` branch picks it up automatically with no changes needed there.
  Verified: `tsc --noEmit` clean project-wide (`pnpm --filter jothidam-ai-web exec tsc --noEmit`), `eslint` clean (0 errors/warnings, `--max-warnings=9999`) on all touched/new files (`dashboard-journal-tab-nova.tsx`, `dashboard-journal-tab.tsx`, `dashboard-shadow-prompts.tsx`, `dashboard-workspace.tsx`, `lib/i18n.ts`). No backend changes this phase (confirmed no Python files touched). Did not start or drive a dev server this session — same handoff as several prior phases; verification rests on `tsc`/`eslint`. **All 14 mockup screens are now built.** **Next:** user sign-off on `journal` in the browser (the Write sub-tab's dasha-context line and prompts, the Pattern-insights card's Transits link, the Upcoming-events card, Entries' edit/archive/export, Reflections' Shadow-work prompts and corrected Tamil text) — then, per §7's still-open items, a wider pass: Deep Dive completeness, the several sub-tab panels still deferred as Classic-styled across Life Areas/Plan/Journal, and the Phase 5 `dashboard-charts.tsx` chart-grid cosmetic gap.
- **2026-07-06 (`/dashboard/v2` retired and deleted)** — With all 14 mockup screens built and Today Nova long since signed off, the user chose to retire the old `/dashboard/v2` Today-tab A/B preview (§4.6) rather than keep it as a third parallel look. Deleted `web/app/dashboard/v2/page.tsx`, `web/components/dashboard-today-tab-v2.tsx`, `web/app/dashboard/v2/today-v2.css` (and the now-empty `web/app/dashboard/v2/` directory), and the `todayVariant`/`TodayVariant` prop path from `dashboard-workspace.tsx` (the dynamic import, the type, the prop itself, the `DashboardTodayTabV2` render branch, and the `todayVariant !== "v2"` gate on the household strip, collapsed to just `uiVariant !== "nova"`). Grepped first to confirm no other consumer (`web/app/dashboard/page.tsx` already called `<DashboardWorkspace />` with no props). `tsc`/`eslint` clean after. Also caught two stale doc references this same session that hadn't been updated when §4.6 itself was: §4.1's routes table still listed `/dashboard/v2` as a live route, and §4.5's styling list still listed `today-v2.css` — both removed.
- **2026-07-06 (Life Areas' 4 deferred sub-tab panels re-skinned)** — User picked 3 of 4 offered next-steps (skipping the browser-sign-off pause): retire `/dashboard/v2` (done above), re-skin Life Areas' 4 deferred panels, re-skin Plan's 4 deferred panels, and Deep Dive completeness — to be worked one at a time, starting with Life Areas. Read all 4 Classic panel files end to end (`dashboard-prediction-panel.tsx` 293 lines, `dashboard-yoga-dosham-panel.tsx` 1074 lines — re-confirmed, already read in full during Phase 8 — `dashboard-remedies-panel.tsx` 286 lines, `dashboard-jadhagam-report-panel.tsx` 405 lines) and grepped every `var(--...)` reference before writing any code, per the working style and the explicit instruction to verify Nova-safety rather than guess. Full findings and file-by-file reasoning in new §6.8.1.
  **Central fix, found while auditing Predictions**: `--cl-sage-border`/`--cl-rust-35`/`--cl-brand-edge` were near-miss siblings of the already-redirected `--cl-sage-edge`/`--cl-rust-edge`/`--cl-rust-30` trio (Phase 4's gap-fix) — never added to that block. Grepped app-wide first (per the explicit instruction not to repeat the 2026-07-06 browser-QA mistake of redirecting a shared token without checking all its consumers): both are used by several Classic-only components and 2 marketing pages that never set `[data-ui="nova"]`, so redirecting at the existing `[data-ui="nova"] .cd-shell {}` scope is safe. Fixed in `dashboard-nova.css` — incidentally also fixes two already-shipped, unnoticed border-color bugs (`dashboard-synastry-panel.tsx`'s Compatibility sub-view, `lib/reasoning.ts`'s `bandTone()` used by `LifeAreaCard`'s causalChain path).
  Built 4 new files: `dashboard-life-areas-predictions-nova.tsx` (`NovaPredictionsPanel`, reuses `confidenceTone`/`confidenceLabel`/`supportTone`/`supportLabel`/`predictionIsDeferred` via 5 new additive exports on the Classic file), `dashboard-life-areas-yogas-doshams-nova.tsx` (`NovaYogaDoshamPanel`, a full rebuild reusing `YOGA_DISPLAY`/`displayName`/`markerLabel`/`getWhat`/`buildWhyText`/`strengthBand`/`doshamSeverityScore`/`getDoshamPowerContext` plus 4 new additive exports — `getYogaPowerContext`/`YOGA_OUTCOMES`/`YOGA_HOW_TO`/`YOGA_REMEDIES`), `dashboard-life-areas-remedies-nova.tsx` (`NovaRemediesPanel`, a full rebuild reusing `CollapsibleSection` verbatim and §6.10's established per-planet color mapping instead of inventing a second one; deliberately does not reuse the shared `Button` primitive — found its inline style reads Classic-only `--panel-brand`/`-earth`/`-tan`/`-brand-border` unredirected, a latent bug flagged in §6.8.1 but out of scope to fix), and `dashboard-life-areas-report-nova.tsx` (`NovaJadhagamReportPanel`) — the last one a thin wrapper, not a rebuild, since `dashboard-jadhagam-report-panel.tsx` turned out ~95% Nova-safe already; added one additive `renderYogaDoshamPanel` prop to `JadhagamReportPanel` instead of duplicating ~400 lines, and fixed one literal hardcoded `#7a3412` (the same bug class Phase 9 found in `life-area-card.tsx`) directly in the Classic file, benefiting Classic too.
  Wired all 4 into `dashboard-life-areas-tab-nova.tsx`, replacing the Classic imports/JSX for the `predictions`/`yogas`/`remedies`/`report` sub-tabs; updated the file's own header comment. Hit one real syntax bug while writing the new files' JSDoc comments: a literal `--color-*/--radius-*` sequence in a comment contains `*/`, which closes a block comment early — caught immediately by `tsc`'s cascade of syntax errors, fixed by using commas instead of slashes; grepped all 8 touched/new files afterward to confirm no other stray `*/` sequences.
  Verified: `pnpm --filter jothidam-ai-web exec tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 8 touched/new files. No backend changes. Did not drive a real authenticated browser session — same handoff as every prior phase. **Next:** user sign-off on Life Areas' Predictions/Yogas & Doshams/Remedies/Full report sub-tabs, then Plan's 4 deferred sub-tab panels (Life Events/What-If/Best Dates & Muhurta/Decisions), then Deep Dive completeness (§7).
- **2026-07-06 (Plan's 4 deferred sub-tab panels re-skinned)** — Continued straight from Life Areas' re-skin in the same session, per the user's own sequencing (item 3 of the 4-item punch list: Plan's deferred panels). Read all 4 Classic panel files end to end plus the 4 heavier sub-components 3 of them embed (`dashboard-life-events.tsx`, `dashboard-decision-panel.tsx`, `dashboard-muhurta-picker.tsx`, `dashboard-muhurtham-naal.tsx`) and grepped every `var(--...)` reference before writing any code. Full findings in new §6.9.1.
  **Real, non-obvious finding: Life Events needed zero rebuild.** `dashboard-life-events.tsx` has no `var(--...)` usage at all — its styling is 100% CSS classes in `globals.css`, and reading those classes end to end showed they were already written dark-theme-first (`var(--color-text, #e5e7eb)` fallbacks, translucent white card overlays, bright saturated accent colors), unlike every other Classic component this pass touched. `PlanEventsPanel` is reused verbatim, unchanged — a genuine free reuse discovered by grepping, not assumed from precedent.
  The other 3 sub-tabs all needed fresh rebuilds, each reading the same reverted 5-token `W` map (`--panel-earth-dark`/`-earth`/`-tan`/`-tan-light`/`-hover`) this session already found unsafe twice over (Journal §6.11, Life Areas' Remedies §6.8.1). Built 6 new files: `dashboard-plan-whatif-nova.tsx` (`NovaPlanWhatIfPanel`, reuses `bandTone`/`bandPhrase` — Nova-safe after §6.8.1's central CSS fix this same session — and the Classic file's pure `verdictKey`/`strengthKey` i18n lookups, but not its Classic-token `verdictColor`/`strengthColor`), `dashboard-plan-decisions-nova.tsx` (`NovaPlanDecisionsPanel`, merges the thin `PlanDecisionsPanel` wrapper and `DecisionPanel`'s form/result into one file; found and fixed a literal hardcoded `#F9ECE7` — a second instance of the same bug class §6.8.1 already fixed once this session), `dashboard-plan-muhurta-picker-nova.tsx` (`NovaMuhurtaPicker`) and `dashboard-plan-muhurtham-naal-nova.tsx` (`NovaMuhurthamNaal`, also fixing two literal hardcoded hex fallbacks) as direct rebuilds of the two heavy widgets Best Dates & Muhurta embeds, and `dashboard-plan-muhurta-nova.tsx` (`NovaPlanMuhurtaPanel`) as the wrapper combining the overview card, Step 1's month-scan JSX, and the two Nova widgets above.
  Wired all 4 into `dashboard-plan-tab-nova.tsx` (Life Events kept as the existing `PlanEventsPanel` import, the other 3 swapped for their Nova equivalents), updated the file's own header comment, and grepped all 6 new/touched files for stray `*/`-in-comment sequences before running `tsc` (the exact bug hit once already this session while building Life Areas' Full-report file).
  Verified: `pnpm --filter jothidam-ai-web exec tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 6 new/touched files. `dashboard-plan-tab.tsx` itself was not touched (only imported from). No backend changes. Did not drive a real authenticated browser session — same handoff as every prior phase. **Next:** user sign-off on Plan's What-If/Best Dates & Muhurta/Decisions sub-tabs, then Deep Dive completeness (§7, item 4 of the punch list).
- **2026-07-06 (Deep Dive completeness, §7 resolved)** — Read `dashboard-personal-tab.tsx`'s 6 previously-unfolded sections (chart validation chip lines 287-319, `DashboardActivityTimingCard` lines 537-544, the Chart Context/Guidance/Gochar two-col lines 546-704, the Dasa-Bhukti-Antaram strip lines 706-723, `MorningGuidanceCard` line 985, the Prasna trigger lines 987-1008) end to end before writing any code, per the working style.
  **Real finding that changed the scope from "port 6 pieces" to "port 6 pieces, 3 of which need a fresh rebuild, not a verbatim reuse":** grepped every `var(--...)` reference in `DashboardActivityTimingCard`, `MorningGuidanceCard`, and `PrasnaWidget` before assuming "trivial, just import it" (the original punch-list note's own words for `MorningGuidanceCard`) — all three read the exact reverted 5-token `W` map (`--panel-earth-dark`/`-earth`/`-tan`/`-tan-light`/`-hover` and `--panel-brand`/`--panel-cream`/`--panel-hover`) that the 2026-07-06 browser-QA round found unsafe to redirect at shared `.cd-shell` scope (same finding Journal/Remedies/Plan's deferred panels already made this session, §6.8.1/§6.9.1/§6.11) — reusing them verbatim would have shipped dark-ink-on-dark-plum invisible text, not just an unstyled "Classic island." Rebuilt all three fresh with Nova tokens instead of deferring them Classic-styled (unlike Plan's Varshaphala/Life Areas' original 4 sub-tabs, these are small enough and visible enough on the Today tab that deferring them would recreate exactly the "white/cream leak" complaint item 3 of this session was raised to fix). The two-col Chart Context/Guidance/Gochar surfaces and the Dasa-Bhukti-Antaram strip, by contrast, turned out to already be 100% built from core `--color-*`/`Surface`/`Metric`/`Chip` tokens Nova already redefines — ported near-verbatim, only the confidence chip's own three literal Classic color refs (`--chart-d9-active`/`--panel-brand`/`--planet-saturn`) were remapped to Nova's existing `--color-high`/`--color-mid`/`--color-low` semantic triplet instead of carried over.
  Built one new file, `dashboard-today-deepdive-extras-nova.tsx`, exporting `NovaChartValidationChip`, `NovaChartContextGuidanceGochar`, `NovaDasaBhuktiAntaramStrip`, `NovaActivityTimingCard`, `NovaMorningGuidanceCard`, `NovaPrasnaTrigger`, and `NovaPrasnaWidget`. Made additive `export` changes (zero behavior change) to reuse pure data/logic instead of duplicating it: `GUIDANCE_REASON_KEYS` (`dashboard-personal-tab.tsx`), `ACTIVITY_OPTIONS`/`currentMonthIso`/`formatShortDate`/`formatWeekday`/`alignmentTone` (`dashboard-activity-timing-card.tsx`), `TIME_PRESETS`/`CHANNEL_OPTS` (`morning-guidance-card.tsx`), `QUESTION_AREAS`/`outlookLabel` (`dashboard-prasna-widget.tsx`). Reused `formatChandrashtamaWindowSummary` (already exported from `dashboard-calendar-tab.tsx` since Phase 2) for the Gochar surface's Chandrashtamam hint instead of re-deriving Classic's private inline IIFE, which wasn't separable into an export.
  **Two deliberate simplifications, not gaps:** (1) `formatScoreLabel` — Classic receives this as a prop from `dashboard-workspace.tsx` (a 2-line pure `${score}/100 – ${band.label}` formatter using `getScoreBand`); rather than thread a prop and risk a circular import (workspace already imports the Today-tab tree, which would then import back from workspace), the same 2-line formula is inlined directly in `NovaChartContextGuidanceGochar` using the already-imported `getScoreBand` — same output, no new prop surface for one trivial formatter. (2) The Guidance surface's "next 3 days" mini-strip needed `dailyGuidanceRange`, which **was** threaded through as a new optional prop (`dailyGuidanceRange={personal.dailyGuidanceRange}`, mirroring Classic's exact wiring) since it's real fetched data, not a formatter; the one behavioral difference from Classic is that Nova's version shows it unconditionally rather than gating on `!personalViewId`, since Nova's Today tab has no family-member switcher (`personalViewId` doesn't exist in its prop surface) — always-root-view makes the gate a no-op here, not a dropped feature.
  Added `dashaMaha`, `dailyGuidanceRange`, `onDateChange`, `onOpenPrasna`, `showPrasna`, `onClosePrasna` to `DashboardTodayTabNovaProps` and wired all 6 from `dashboard-workspace.tsx`'s existing Nova call site — `dashaMaha={personalDashaMaha}` (same value already computed at line 773 for Classic), `onDateChange={setSelectedDate}`, `onOpenPrasna`/`showPrasna`/`onClosePrasna` reusing the *same* `showPrasna` state Classic's branch already declares (no second copy of that state for the Nova render branch). All 6 pieces are nested inside the existing Deep Dive collapsed section (not the top-level page) — the confidence chip and PDF button now share one header row, then activity timing → chart context/guidance/gochar → Dasa strip → planet table (existing) → nakshatra card (existing) → vargas/shadbala/alt-dashas (existing) → classical timing (existing) → morning guidance → Prasna trigger/widget, mirroring Classic's own top-to-bottom order.
  Verified: `tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 7 touched/new files (`dashboard-today-tab-nova.tsx`, `dashboard-today-deepdive-extras-nova.tsx`, `dashboard-personal-tab.tsx`, `dashboard-activity-timing-card.tsx`, `morning-guidance-card.tsx`, `dashboard-prasna-widget.tsx`, `dashboard-workspace.tsx`). No backend changes. Confirmed `/dashboard` still compiles on the already-running dev servers (307 auth redirect, no 500). Did not drive a real authenticated browser session — same handoff as every prior phase. **Next:** Tools tab's first Nova pass (never built, punch-list item 2), then the white-background-leak audit (item 3), then header/footer/navbar consistency (item 4).
- **2026-07-06 (Tools tab, first Nova pass — punch-list item 2, §6.12)** — Read the `tools`/`tools-porutham` mockup screens in full, then the real Tools tab render (confirmed it had never had a `uiVariant` branch — the actual root cause of "Tools doesn't look like the new design"), `porutham-panel.tsx`, `chart-generate-inline-panel.tsx`, `dashboard-annual-wrapped.tsx`, `dashboard-retrospective-panel.tsx` end to end before writing anything. Full findings, mapping-guess corrections (§3/§4.3), and gap table in new §6.12.
  **Two real pre-existing Tamil-corruption bugs found and fixed** (same root commit as Phase 11's `dashboard-shadow-prompts.tsx` find): 3 validation/error-message strings across `porutham-panel.tsx`/`chart-generate-inline-panel.tsx` were mangled `??????` sequences; recovered the exact original Tamil text from `git log -p` and restored it verbatim. Swept all of `web/components` afterward — clean.
  **Closed the long-flagged "`dashboard-charts.tsx` chart-grid cosmetic gap from Phase 5"** (named in this doc's own top status line) — needed to safely reuse `RasiChart`/`JathagamKattam` in this phase's Porutham result and item 1's new Chart Context surface. Changed `dashboard-charts.tsx` to read new component-scoped `--chartgrid-*` names with same-value fallbacks instead of the shared, already-reverted `--panel-earth`/`-tan`/`-tan-light`/`-cream` set, then redirected only those new names under Nova — Classic is a byte-for-byte no-op, no risk of repeating the Phase-5 "selected pill" regression since no other component can ever reach these names.
  Built `dashboard-tools-porutham-nova.tsx` (`NovaPoruthamPanel`) — a full Nova-token rebuild of `PoruthamPanel`'s form+result flow (same `/api/v1/public/compare` + PDF-export calls, same context selector), restructured toward the `tools-porutham` mockup's verdict-hero + two-column (10-porutham table | cross-checks/next-steps) layout instead of Classic's single-column list. Added a real Sevvai-dosham cross-check (derived from both charts' already-fetched `doshams[]`, no new astrology) and left Dasa sandhi/Papasamyam as honest "not yet available" rows, matching this doc's own Phase-1 gap table which already flagged those two as a confirmed stub with no calculation anywhere in the codebase. `CompatibilityIntelligencePanel` (Porutham's MARRIAGE-context upsell report) is omitted this pass — not in the mockup's captured state, and reads a much larger disjoint `--cl-*` token set not worth extending the fix for.
  Built `dashboard-tools-tab-nova.tsx` — hub (header, hero Porutham card, a 6-card grid, a "coming soon" Recent-results stub) + a detail view swap, mirroring every other tab's "sub-view of one tab, not a new route" pattern. Introduced `ClassicToolIsland`, a small wrapper that re-pins ~19 Classic-literal tokens (the reverted 5, `--chartgrid-*`, `--chart-cell-default`/`-d9-active`/`-amber`, `--planet-saturn`, `--dignity-neecha`, `--ring-brand`, `--color-faint`) back to their exact hex values via inline `style`, used around `ChartGenerateInlinePanel`/`DashboardAnnualWrapped`/`RetrospectivePanel` (three real tools not shown in either mockup screen, 1,333 combined lines, judged not worth a full rebuild this pass) — the same variable-scoping trick `dashboard-personal-tab.tsx` already uses in reverse, applied here to keep a deferred Classic panel internally consistent (light card + dark text, both intentionally pinned light) instead of the broken "dark card assumed, dark text stays dark" combination a bare reuse would have shipped.
  Hoisted `activeTool`/`needsProfile`/`openTool`/`closeTool` derivation one level up inside the existing `activeTab === "tools"` IIFE in `dashboard-workspace.tsx` so both `uiVariant` branches share the exact same 4 `showX` booleans (no second copy of that state) — Classic's own render branch and `TOOL_LIST` are otherwise untouched.
  Verified: `tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 6 new/touched files. No backend changes. Confirmed `/dashboard` still compiles (307 auth redirect, no 500). Did not drive a real authenticated browser session — same handoff as every prior phase. **Next:** the white-background-leak audit (punch-list item 3), then header/footer/navbar consistency (item 4).
- **2026-07-06 (white-background-leak audit + header/footer/navbar check — punch-list items 3 & 4)** — No browser automation available in this environment (same as every prior phase), so this pass was a systematic static sweep: grepped every `*-nova.tsx` file for the known-unsafe token families (`--panel-*`/`--cl-*`/`--chart-*`/`--planet-*` minus what's already redirected), then — since that came back almost entirely clean — widened the sweep to every non-`-nova`-suffixed component that a Nova-reachable file actually imports (shared primitives, chrome, modals), since a leak doesn't require the leaking file to have "nova" in its name.
  **Two severity classes found, both real, neither previously caught:**
  1. **"Invisible text" (dark card, dark text)** — the exact same reverted 5-token `W`-map (`ink`/`inkMid`/`border`/`borderLt`/`surface`/`surfaceMd`, aliasing `--panel-earth(-dark)`/`--panel-tan(-light)`/`--panel-cream`/`--panel-hover`) turned up in **5 files reused directly (unwrapped) in Today Nova's Deep Dive section since Phase 1** — `dashboard-vargas-panel.tsx`, `dashboard-shadbala-panel.tsx`, `dashboard-yogini-dasha-panel.tsx`, `dashboard-ashtottari-dasha-panel.tsx`, `dashboard-kalachakra-dasha-panel.tsx` — none of which had ever been grepped for token safety (Phase 1's own note called them "existing detail panels reused verbatim," not "confirmed safe"). Their `surface`/`surfaceMd` backgrounds are redirected dark by the existing gap-fix block while `ink`/`border` are not — meaning the Vargas table, Shadbala breakdown, and all 3 alternate-dasha panels have been rendering with low/zero-contrast text under Nova since 2026-07-05, undetected until now. Same bug, same root cause, also found in 5 more files reachable regardless of the Look toggle (not gated by any `uiVariant` branch at all): the "Ask Vinaadi" topbar notifications dropdown (`dashboard-hero.tsx`, 4 spots), `dashboard-edit-member-modal.tsx`, `dashboard-edit-profile-modal.tsx`, `dashboard-settings-session-tab.tsx` (the Settings tab — where the Look toggle itself lives), and `dashboard-setup-tab.tsx` (the onboarding/profile-creation flow). Fixed all 10 with the same scoped-name-plus-fallback trick from this session's earlier `dashboard-charts.tsx` fix: a new shared `--deepdive-ink/-ink-mid/-border/-border-light/-surface/-surface-strong/-accent` name set (`dashboard-nova.css`), referenced as `var(--deepdive-ink, var(--panel-earth-dark))` etc. at each of the ~50 call sites across these files (only the `const W = {...}` object definition needed editing in the 5 dasha/varga panels and 4 of the 5 chrome/modal files, since everything downstream already reads through `W.ink` etc. — zero JSX changes). `--cl-sage-soft` (a "current period" highlight fill in the 3 dasha panels) was redirected directly rather than renamed, after confirming via grep that its only other consumers are Classic-only paths Nova never reaches. `dashboard-chart-explanation.tsx` (1502 lines, also reused directly since Phase 1) was checked and found **already fully safe** — its `strengthColor`/`relationshipColor`/`activationToneColor` helpers already use `var(--color-score-high, var(--chart-d9-active))`-style safe-primary-with-Classic-fallback expressions, so no fix was needed there; same for `dashboard-ask-vinaadi.tsx`/`dashboard-ask-vinaadi-widget.tsx`, which turned out to already be written with this exact safe-fallback convention throughout.
  2. **Literal white/cream background leak (the user's exact complaint, not just an accessibility contrast bug)** — `life-mode-picker.tsx` (the "what are you focused on" modal, opened either from Classic's Today tab or **automatically whenever the app decides the user's saved life-mode is stale**, regardless of the Look toggle or active tab) sets its own outer sheet `background: var(--panel-cream-light)` — a *different* token from `--panel-cream`, and one no prior phase had ever added to the redirect list. Confirmed via grep this token has zero Nova coverage anywhere. This is the one finding this session that matches the reported symptom literally (a cream/light sheet, not a dark-bg/dark-text combo) — likely the single most visible instance of "white background leak," since it can appear as a modal over *any* Nova page. Fixed with the safe-primary-with-fallback pattern (`var(--color-surface, var(--panel-cream-light))` etc.) throughout the file, including its two literal (non-variable) hex fallbacks (`#E4DAC6`/`#F7E8DA`), now wrapped as `var(--color-border, #E4DAC6)` / `var(--color-mid-bg, #F7E8DA)` so Nova gets a real value instead of inheriting the literal light hex unconditionally.
  **Header/footer/navbar consistency (item 4) — confirmed cause (a), not (b):** re-read `.cd-footer`/`.cd-tabnav`/topbar CSS and this doc's own Progress Log; found no evidence of a new CSS-variable-collision regression like Phase 5's (which was real and is already fixed). The two things that actually explain "chrome looks inconsistent across pages" are (i) Tools having been 100% unbuilt under Nova until this session (item 2) — the most visible, largest-surface-area instance of "a page still looks Classic" — and (ii) the topbar's own notifications dropdown (part of the *shared* chrome rendered on every page) carrying 4 of the unfixed `--panel-brand` text-color instances above, meaning the chrome itself intermittently showed a Classic terracotta accent instead of Nova gold depending on whether the notifications panel was open — both are now fixed by this same pass, not a separate bug. Re-verified `dashboard-nova.css`'s footer/tabnav gap-fix blocks are unchanged and still structurally sound (no duplicate selectors, no conflicting specificity introduced by this session's new `--chartgrid-*`/`--deepdive-*` blocks, which only add new variable names and never redefine `--panel-earth`/`--panel-tan`/`--panel-hover`/`--panel-cream`/`--panel-tan-light` at the shared/global level — the exact mistake that caused the Phase 5 regression).
- **2026-07-06 (user bug-report pass — 6 reports, 5 fixed, 1 scoped)** — User filed 7 observations from real use of Nova; investigated each against the actual code rather than assuming, per the working style.
  1. **Family member chart explanation: Dosham/Yogam headings render black** — `dashboard-yoga-dosham-panel.tsx` (the file behind `ChartExplanationPanel`, reused unwrapped in Family Nova's `MemberDetailExpanded` and Today Nova's Deep Dive) was never covered by the 2026-07-06 white-background-leak audit's file list, despite being the exact same bug class: its "Yogas"/"Doshams" section titles, yoga/dosham names, and body/remedy text all read `--panel-earth`/`--panel-earth-dark` (`#1A1612`, "darkest warm text — headings on light") directly, with a few borders on `--panel-tan`/`--panel-tan-light` and a remedy box on `--panel-warm-light`. Fixed with the established scoped-fallback pattern: new `--yogadosham-*` name set in `dashboard-nova.css`, `var(--yogadosham-ink-strong, var(--panel-earth-dark))` etc. at all ~19 call sites in the component file.
  2. **Family tab Refresh button does nothing** — `useFamilyData.ts`'s `refreshFamilyBundle()` called `queryClient.fetchQuery({ ..., staleTime: STALE.today })` (24h) — `fetchQuery` treats cached data younger than `staleTime` as fresh and returns it without calling `queryFn`, and the bundle is almost always still fresh (fetched when the tab opened), so the button silently no-opped while still showing "Family data refreshed." Changed the manual-refresh call's `staleTime` to `0` (background `useQuery` hooks elsewhere are untouched, so normal caching behavior is unaffected).
  3. **Compatibility button "not working" / content UI bad** — same root cause as #1, one file wider: `synastry-matrix.tsx`, `dashboard-synastry-panel.tsx` (whose outer wrapper sets `color: var(--panel-earth)`, so *every* unstyled paragraph inside inherited near-black text, not just the explicitly-colored spots), and `compatibility-intelligence-panel.tsx`'s own `W` palette all read the same unsafe token family — confirmed §6.3's "already var(--color-*)-driven" claim was wrong here too, same as Phase 4's chart-grid claim. The panel wasn't actually broken, it was rendering nearly invisible. Fixed with a new `--synastry-*` name set (also covers `--panel-brand`/`--panel-hover`/`--underline-brand`/`--border-error-soft`/`--cl-sage-06`/`--cl-sage-25`, including `--synastry-active-ink` for the active-Pill-text case where the *background* token flips light under Nova). Separately confirmed (not a bug, a gap): the Compatibility button only exists on the Family hub screen, not on the Family-member full-profile screen (`DashboardFamilyMemberNova`, Phase 5) — that screen has its own, different "In the family" relationship card instead. Flagged, not built — would need new prop plumbing, a scope call left to the user.
  4. **Explore page mostly links out to the marketing site** — confirmed as designed (§6.5's own header comment: "reuses those verbatim via real links... rather than duplicating an encyclopedia inside the dashboard"), but Phase 7/8 have since shipped in-app, fully-browsable equivalents for two of the six Library cards that didn't exist when that decision was made: `DashboardExploreNakshatramNova` (all 27 nakshatrams, wraps via `onNext`/`onPrev`) and `DashboardExploreDoshamNova` (the full per-chart dosham catalogue, not just present ones). Rewired the Natchathiram and Dosham Library cards to open those in-app screens when the user's own chart data is loaded (falling back to the marketing `href` otherwise, e.g. before chart data loads); Yogam/Pariharam/Temples and all 5 `/learn/*` articles still link out — no in-app equivalent exists for those, and building one is new content, not a bug fix.
  5. **Footer creeps up under short pages instead of staying at the bottom** — `.cd-main-content` had `min-height` only on the ≥1024px breakpoint and was never a flex column at any width, so `.cd-footer` (its last child) just stacked directly under whatever the active tab rendered — on a short tab, `min-height` padded empty space *after* the footer, not before it. Made `.cd-main-content` a flex column with `min-height: calc(100vh - 54px)` at all widths, and wrapped everything between it and the footer in a new `.cd-main-content__body` (`flex: 1 1 auto`) so the wrapper absorbs the slack instead — `.cd-footer`'s own `margin-top: 48px` is untouched, so long pages look byte-for-byte identical to before.
  6. **Root user's own name appears twice in every member-picker** (Today/Life Areas/Transits/Plan, Classic and Nova alike — the same "still unknown" root cause flagged in this doc's memory snapshot for Life Areas specifically) — found it: `relationshipToOwner` has a real, user-selectable `"self"` option (`dashboard-edit-member-modal.tsx`'s relationship dropdown), so a family-vault member row can legitimately *be* the owner. Every one of these picker UIs already renders a dedicated first pill from `birthDisplayName`/`"You"` *and* separately maps over `family.memberCharts` — a self-tagged member duplicated the owner's own name in that second list. Fixed at the single choke point, `useFamilyData.ts`: `memberChartsQuery`'s `members` now excludes any family member whose `relationshipToOwner` is `"self"` (cross-referenced against `familyBundleQuery.data.members`, the list that actually carries that field) before fetching/deriving member charts — `family.familyAggregate.members` itself (the Family tab's own roster/edit/delete list) is untouched, so a mistaken self-entry is still visible and fixable there.
  7. **Tools landing page matches Nova but every individual tool's detail page (`/dashboard/porutham`, `/dashboard/chart-generate`, `/dashboard/reports`, `/dashboard/wrapped`, `/dashboard/goals`, `/dashboard/daily-score`) still looks old** — confirmed, not yet fixed: these are separate Next.js routes (not tabs inside `DashboardWorkspace`), and none of the 6 `page.tsx` files reference `uiVariant`/`useUiVariant` at all — 100% Classic regardless of the user's Look setting. This is a phase-sized build (6 standalone pages, some large — chart-generate/reports/wrapped), not a bug fix, so left unbuilt pending the user's scope/sequencing decision, same working-style precedent as every other phase in this doc.
  Verified: `pnpm exec tsc --noEmit` clean project-wide after all fixes (items 1-6). No backend changes. Did not drive a real authenticated browser session — same handoff as most prior phases. **Next:** user browser sign-off on items 1-6, and a scoping decision on item 7 (which of the 6 tool pages to Nova-ify first, or all at once as a new phase).
- **2026-07-07 (three-item fix pass: marketing link-outs, week-ahead perf, floating-button overlap)** — User asked for three fixes, all verified against a real authenticated browser session and real curl timing (first time this doc can say that — every prior phase's verification note above ends with "did not drive a real browser session").
  1. **Dashboard must never navigate to the marketing site (closes item 4 above, superseding the 2026-07-06 "not a bug, by design" call)** — inventoried every remaining marketing link-out across the whole dashboard surface (not just Explore): Yogam/Pariharam/Temples library tiles + all 5 Learn articles (`dashboard-explore-tab-nova.tsx`, always `target="_blank"`), Tools' Indraiya Rasipalan card (`dashboard-tools-tab-nova.tsx`, same), Classic's Chandrashtama banner "Learn →" link (`dashboard-personal-tab.tsx`, not previously flagged), and the sitewide beta banner/welcome-modal (`beta-system.tsx`, rendered from root layout, predates Nova entirely). Natchathiram/Dosham tiles were already mostly fixed by the 2026-07-06 pass; the one remaining gap there (falls back to an external link during the brief window before chart data loads) is now a disabled loading-state tile instead.
     Checked how the marketing pages actually source their content before deciding an approach: Yogam/Pariharam/Temples all already share one generic, bilingual `GuideDetail` data shape (`web/lib/guide-detail-content.ts`, `YOGAM_DETAILS`/`PARIHARAM_DETAILS`/`TEMPLE_DETAILS` — 5/6/11 entries) behind a single marketing renderer (`GuideDetailPage`) that hard-wires `PublicNav`/`PublicFooter`/a signup-upsell CTA and cross-links to other marketing pages — reusing that renderer as-is would just relocate the problem. Built `dashboard-explore-guide-nova.tsx` instead: a new dashboard-native viewer reading the same `GuideDetail` records directly (breadcrumb + prev/next across every entry of the kind, hero, attribute band, sections, remedies/slokam/FAQ, Ask-entry chip — same shell pieces as the existing Nakshatram/Dosham detail screens), no marketing chrome, no signup CTA, "related" cross-links dropped (same precedent as the 2026-07-06 Nakshatram/Dosham marketing-link removal). Added one additive export, `getGuideSlugs(kind)`, to the data file for this. The 5 Learn articles are much smaller (4-6 heading/body pairs each, hand-authored, no shared schema) — collected into `dashboard-learn-content.ts` (data only, importing the exact bilingual strings from `web/lib/marketing-i18n.ts`, nothing re-authored) and rendered by `dashboard-explore-learn-nova.tsx` (Nova, same breadcrumb/prev-next pattern) and `dashboard-learn-article-modal.tsx` (a small overlay modal, used by Classic's Chandrashtama banner, which has no tab-swap mechanism to reuse). Indraiya Rasipalan turned out to already be a self-contained component (`RasippalanTool`, no `PublicNav`/`Footer` at all) — reused directly inside the existing `ClassicToolIsland` token-scoping wrapper (same trick as the other 3 Classic-styled tools in this tab), with a new `hideCta` prop to drop its "Get started free → /dashboard" footer CTA (circular/pointless for a user already in the dashboard). Its own `useLang()` is independent of the dashboard's lang toggle (reads the marketing site's shared `LangContext`, seeded from the account's stored `lang`, not the dashboard's own display-language state) — a known, accepted cosmetic inconsistency, not a crash or broken layout.
     Beta banner: added a pathname check (`usePathname()`) to `beta-system.tsx` — suppressed entirely on `/dashboard*` routes, unchanged everywhere else (confirmed via Playwright: still visible on `/`, absent on `/dashboard`).
     Verified live: registered a disposable account, clicked into a Yogam guide (breadcrumb + prev/next + FAQ all real, no dead ends), a Learn article, and the Rasipalan tool from the Tools tab — all render correctly with real data. Grepped the whole dashboard surface afterward for any remaining `target="_blank"`/`href="/…"` outside `/dashboard`: none, other than `/notifications` (a real authenticated app page, not marketing — no `PublicNav`/`Footer`) and `/dashboard/reports` (in-app).
  2. **`/api/v1/charts/{id}/week-ahead` ~7x slower than sibling endpoints** — root-caused with `cProfile` against a real chart rather than guessing: `_calc_ut` (the Swiss-Ephemeris/Moshier call — this environment has no `.se1` ephemeris data files, confirmed via the chart-calc warning, so every ephemeris call falls back to the slower Moshier series) gets called ~24k times computing one day's full panchangam (sunrise/sunset/tithi/yoga/nakshatra boundary root-finding). Tried parallelizing the existing per-day loop in `get_daily_guidance_range()` with a `ThreadPoolExecutor` first (per-thread DB sessions) since that's what the investigation was originally scoped to try — **measured slower, not faster** (GIL contention on CPU-bound Moshier calls plus extra per-thread session overhead), so reverted that and left a comment explaining why threading doesn't help here for the next person tempted to try it again.
     The actual bug, found by profiling: `get_week_ahead()` had its **own second loop** re-calling `calculate_daily_panchangam()` once per day with no `session` argument — `calculate_daily_panchangam` only consults `PanchangamCache` when `session is not None`, so this call unconditionally recomputed full panchangam from scratch for all 7 days, every single request, on top of the (correctly cached) computation `get_daily_guidance_range()` already did for the same 7 dates one line above. Fixed by replacing it with one `calculate_daily_panchangam_range()` call (the existing batched, session-aware helper — same one `calculate_daily_panchangam_range`'s own docstring already recommends over a per-day loop) before the loop, looked up per day from the returned dict. Also found and fixed a second, smaller duplication while in there: `get_week_ahead()` reloaded the full chart snapshot a second time (`load_persisted_chart_response`, ~1.2s of it is `_birth_panchangam_signature`) purely for its own dasha-theme lookup, right after `get_daily_guidance_range()` had already loaded the identical snapshot internally. Added an additive `chart_snapshot` passthrough parameter to `get_daily_guidance_range()` so `get_week_ahead()` loads it once and threads it through both uses.
     Verified with direct curl timing against a real chart (disposable account, same kind of measurement the bug report itself used): repeat/warm requests for an already-computed week went from ~8-14s to **~1.1-2s**, right in the ~1.3-2.5s range of the sibling single-day endpoints. A genuinely first-ever week (cold panchangam cache, nothing to reuse) is still ~7-13s — inherent cost of 7 real, independent Moshier computations with no possible redundancy left to remove, not a regression; the fix specifically targets the repeated-request case, which is what the reported symptom (family tab sitting on "Refreshing…" across multiple dashboard loads/tab switches) actually hits. (Hit and burned significant time on an unrelated environment landmine while verifying this: the venv's editable install (`pip install -e .`) was silently pointed at a stale, 8-day-old mirror of this repo in a different folder, so the running dev server kept serving pre-fix code no matter how many times it was restarted, while direct-script tests against the same DB correctly picked up the fix — re-ran `pip install -e .[dev]` from `D:\sanstro` to repoint it. Worth knowing if `dev.ps1`'s server ever seems to "not see" a backend edit again.)
  3. **Floating "Ask Vinaadi" pill + feedback FAB overlap page content in Nova (and Classic — same shared component/markup, not Nova-specific)** — reproduced live rather than guessing: Plan tab's Life Event Windows list, Journal's Upcoming-Events "Optional note" field, and Tools' Retrospective card icon all sat directly under the fixed buttons' screen footprint on initial load, no scrolling needed. Two contributing causes, both fixed: (a) the closed-state Ask-Vinaadi pill sat at `bottom: 196px` — 128px of empty dead space above the feedback FAB (`bottom: 24px`) for no evident reason — shrunk to `bottom: 80px`, directly above the FAB with a normal stacking gap (`dashboard-ask-vinaadi-widget.tsx`; the open chat panel's `bottom` adjusted to match). (b) `.cd-page` (the single shared width/centering container both Classic and Nova tab content renders through) had zero right-side clearance — its `width: min(1200-1240px, 100% - 2*space-4)` computation leaves only ~10-12px of gutter on a normal desktop viewport, i.e. full-width list rows/cards already render almost flush to where the fixed buttons sit. Added `padding-right: 150px` to `.cd-page` at `min-width: 640px` (`dashboard.css`) to reserve a real lane for the buttons — since `box-sizing: border-box` is a global reset, this shrinks the box's own content area rather than shifting/misaligning its centering. The bottom-left circular "N" badge the original bug report described overlapping the footer wordmark turned out to be **Next.js's own dev-mode indicator** (a framework-injected `NEXTJS-PORTAL` element, confirmed via `document.elementFromPoint` — not app code, not present in production builds); left alone.
     Verified live: re-screenshotted all three previously-broken spots after the fix — Life Event Windows rows, the Journal note field, and the Retrospective icon are all fully clear of the pill now.
  Verified end-to-end: `tsc --noEmit` clean project-wide, `vitest run` 62/62 passing, `nova-sweep.spec.ts` (all 9 Nova tabs + Life Area/Plan sub-tabs) 10/10 passing with zero console/page errors, plus the live click-throughs above. Backend change is additive/refactor-only (one new kwarg, no schema/migration changes).
- **2026-07-08 (Nova navbar restructure, day-ribbon legibility, Morning Guidance dedup, Deep Dive cross-links)** — Picked up the Nova WIP that had been sitting uncommitted since 2026-07-07 (`dashboard-nova.css`, `dashboard-hero.tsx`, `dashboard-ask-vinaadi-widget.tsx`) plus further same-theme edits across the Today-tab surface, reviewed each change against the live app, and closed it out.
  1. **Navbar restructure** — `dashboard-hero.tsx`'s tab strip is now a single `tabButtons` array shared by two homes: Classic keeps it in the existing below-header `cd-tabnav`; Nova now renders it inline inside the topbar itself (`cd-topnav`), and gets a new second row (`cd-subbar`) carrying the chart identity (name + moon/nakshatra/lagnam line, previously `cd-brand__sub`) plus the status pill and vault-name chip that Classic keeps in the topbar's right cluster. Nova's date control gained a display-only `cd-date-display` span (`"08 · Jul · 2026"`, locale-aware month via `toLocaleDateString`) layered over the still-functional native `<input type="date">` (click delegates to `showPicker()`), replacing the previous bare native input. "Ask Vinaadi" moved from a floating bottom-right pill into a `cd-ask-nav-btn` in Nova's topbar right cluster; `DashboardAskVinaadiWidget` gained a `hideLauncher` prop (Nova only) so the floating pill is suppressed while the chat panel itself still works — since the panel used to anchor from `bottom` relative to the (now nonexistent, under Nova) floating launcher, it now anchors from `top` when `hideLauncher` is set, so it still opens next to the button that triggered it instead of floating mid-screen. All new CSS lives under `[data-ui="nova"] .cd-topnav`/`.cd-subbar`/`.cd-ask-nav-btn`/`.cd-date-display` in `dashboard-nova.css` — Classic's `cd-tabnav`/`cd-topbar` markup and CSS are untouched.
  2. **Day-ribbon legibility (`dashboard-today-ribbon-nova.tsx`)** — segments previously rendered as unlabeled colored bars (title-only tooltip); now each segment carries its own visible badge (`RAHU KALAM`, `BEST` for the first Nalla Neram slot, `GOOD AM`/`GOOD DAY`/`GOOD EVE` for subsequent slots keyed off a new `partOfDay()` helper) plus a two-line legend (name + time range, previously one concatenated string). Separately fixed the Week Ahead mini-chart (`dashboard-today-glance-nova.tsx`): all "mid-band" days rendered as visually identical bars because Nova's `--color-score-mid` **is** its accent gold, the same color `scoreColor()` already used for the selected-day highlight — added `weekBarColor()`, a continuous low→mid→high `color-mix()` gradient keyed directly off the numeric score instead of the coarse 3-band tone, so a run of same-band days is now visually distinguishable; the selected-day highlight switched from a color swap to a `box-shadow` ring so it no longer collides with the new gradient.
  3. **Morning Guidance duplication removed** — the same notification-preference editor existed in three places (`morning-guidance-card.tsx`'s Classic Today-tab card, `NovaMorningGuidanceCard`/`NovaPill`/`NovaToggle` inside `dashboard-today-deepdive-extras-nova.tsx`, and the real settings-of-record in `dashboard-settings-session-tab.tsx`), all independently calling `PATCH /api/v1/settings/notifications` — editing one didn't refresh the others' local `useState` copies until a full remount. Cut it down to one editing surface (Settings → Notifications) plus one shared, read-only status pointer: rewrote `MorningGuidanceCard` to just fetch and display `prefs` (no local `enabled`/`time`/`channel` state, no `save()`), added an optional `onOpenSettings` callback, and deleted `NovaMorningGuidanceCard`/`NovaPill`/`NovaToggle` entirely from the deepdive-extras file in favor of reusing the same compact `MorningGuidanceCard` component both Classic and Nova now render. Wired `onOpenNotificationSettings` through `dashboard-personal-tab.tsx` and both `dashboard-workspace.tsx` call sites (`() => { goToTab("settings"); setSettingsSubTab("session"); }`) so the card's "manage" affordance actually lands on the right settings sub-tab instead of just the settings hub.
  4. **Deep Dive cross-links** — the Today Nova glance row's "Your dasa chapter" and "Life areas" cards were dead-end summaries; added optional `onGoToTransits`/`onGoToLifeAreas` props (small inline "Open →" buttons, shown only when the callback is passed) wired in `dashboard-workspace.tsx` to `() => { setPlanView("transits"); setActiveTab("plan"); }` and `() => setActiveTab("life-areas")` respectively — same navigation primitives every other cross-tab link in this file already uses, no new state.
  Verified: `tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 11 touched files, `vitest run` 62/62 passing. Ran `scripts/audit-color-literals.mjs` and confirmed this diff introduces zero new findings (grepped its output for all 11 touched files — none appear); the script's pre-existing 356-finding count is a stale baseline (line-number drift from earlier, unrelated commits, almost entirely in `mobile/*` and other untouched `web/app/dashboard/*` pages) that this diff did not create and does not fix — two dead baseline entries for `morning-guidance-card.tsx`'s now-deleted `Pill`/`Toggle` hex literals were incidentally retired by this change. Live-verified against the already-running dev servers with a throwaway Playwright spec (deleted before commit, per project convention): confirmed `nav.cd-topnav` + `.cd-subbar` render and the old `nav.cd-tabnav` is absent under Nova, the Ask Vinaadi navbar button opens its panel anchored near the top (not floating mid-screen), and the day ribbon shows a real `Rahu Kalam` badge — all passed on a fresh account. The Morning Guidance and deep-dive-link checks hit Playwright/environment flakiness (the page handle was reported "closed" mid-wait even though Playwright's own auto-captured accessibility snapshot at that exact moment showed a fully rendered, correctly-populated dashboard, including the topnav/subbar/glance content) — not reproducible as an app bug, so those two pieces were confirmed by code review instead of a passing live assertion. No backend changes.
  Verified: `tsc --noEmit` clean project-wide, `eslint --max-warnings=9999` clean (0 errors/warnings) on all 11 touched files (`dashboard-vargas-panel.tsx`, `dashboard-shadbala-panel.tsx`, `dashboard-yogini-dasha-panel.tsx`, `dashboard-ashtottari-dasha-panel.tsx`, `dashboard-kalachakra-dasha-panel.tsx`, `dashboard-hero.tsx`, `life-mode-picker.tsx`, `dashboard-edit-member-modal.tsx`, `dashboard-edit-profile-modal.tsx`, `dashboard-settings-session-tab.tsx`, `dashboard-setup-tab.tsx`) plus `dashboard-nova.css`. No backend changes. Did not drive a real authenticated browser session — same handoff as every prior phase; this pass's findings were derived from grep + token-flow reasoning, not visual diffing, so a user browser pass across Today's Deep Dive (Vargas/Shadbala/alternate dashas), the notifications dropdown, Settings, onboarding, and the stale-life-mode modal is the natural next confirmation step. **Next:** clean up and commit the session's work (Deep Dive completeness, Tools tab, this leak sweep).
