# Shell scripts and Docker entrypoints must use LF so they run inside Linux
# containers regardless of the contributor's core.autocrlf setting.
*.sh text eol=lf
docker/entrypoint.sh text eol=lf
Dockerfile text eol=lf
