// Re-export only. The implementation lives in `packages/shared` so the web
// hero, the dashboard ribbon and the mobile grid all promote limbs by the same
// rule — three surfaces printed the sunrise value flat before this existed, and
// three separate copies is how they would drift apart again.
export { limbNow } from "@vinaadi/shared";
export type { LimbNow } from "@vinaadi/shared";
